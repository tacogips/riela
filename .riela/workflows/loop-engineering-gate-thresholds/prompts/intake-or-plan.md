Return concise business JSON only. Preserve unrelated dirty worktree changes. Do not commit or push. Do not start nested Riela or Codex processes.

Plan a focused implementation slice for loop gate acceptance threshold evaluation. The slice should inspect `LoopEvidenceProjector`, `LoopGateResult`, and `LoopGateAcceptancePolicy`, then implement fail-closed required gate evaluation for authored `acceptWhen` decision, max high findings, and max medium findings.

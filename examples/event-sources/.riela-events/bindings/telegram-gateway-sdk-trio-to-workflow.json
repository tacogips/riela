{
  "id": "telegram-gateway-sdk-trio-to-workflow",
  "enabled": false,
  "sourceId": "telegram-gateway-personas",
  "outputDestinations": ["telegram-gateway-persona-replies"],
  "workflowName": "telegram-sdk-trio-chat",
  "match": {
    "eventType": "chat.message"
  },
  "inputMapping": {
    "mode": "template",
    "template": {
      "request": "{{event.input.payload.text}}",
      "history": "{{event.input.payload.history}}",
      "historySource": "{{event.input.payload.historySource}}",
      "attachments": "{{event.input.payload.attachments}}",
      "imagePaths": "{{event.input.payload.imagePaths}}",
      "attachmentText": "{{event.input.payload.attachmentText}}",
      "personaCandidates": ["yui", "mika", "rina"],
      "conversationId": "{{event.conversation.id}}",
      "threadId": "{{event.conversation.threadId}}"
    },
    "mirrorToHumanInput": true
  },
  "execution": {
    "async": false,
    "dedupeWindowMs": 86400000,
    "maxConcurrentPerKey": 1,
    "concurrencyKey": "{{event.sourceId}}:sdk:{{event.conversation.id}}"
  },
  "taskPlanning": {
    "enabled": false
  },
  "mailboxBridge": {
    "output": {
      "progress": {
        "mode": "none"
      }
    }
  }
}

Return concise business JSON only. Preserve unrelated dirty worktree changes. Do not commit or push. Do not start nested Riela or Codex processes.

Implement the requested `riela loop recover` slice. Keep the command a thin alias over session rerun, maintain existing session rerun behavior, add focused tests, and return changed files plus verification commands.

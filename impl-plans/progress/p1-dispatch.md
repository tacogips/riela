# P1 dispatcher implementation progress

- Workflow mode: `issue-resolution`; issue: `comm-000002`; plan: `p1-dispatch`.
- Implementation node: `nested-v1-b36e2affcb62ca2131fdba9a258e1badc61657f01c237fe46ef12ca760a61722`, Step 6.
- Dependency admission: runtime `acceptedPlanIds` contains `p1-sandbox`, `p1-reservation`, `p1-lifecycle`, and `p1-capabilities`. This is the integration admission decision for this branch.
- Working branch: `feat/remaining-impl-plans`; checkpoint: `fe4da6cdda88c4bbaeafcb5dbce49bce5dd302af`.
- Per-edit intent and verification evidence: `tmp/work-runtime-p1-dag-20260922-8286b20f/p1-dispatch/`.

## Implementation attempt 1

- P1-6a: added read-only dependency/placement preview, read-only pending-request lookup, atomic reservation bridge, one-use launch admission, exact reserved-session runner execution, selected local backend/model patch, terminal projection, and reconciliation. Remote host placement, called-workflow requirements, guard/completion application, and cancellation acknowledgment remain incomplete.
- P1-6b/c: added `task run`/dry-run and `task decide` parsing, shared-applier decisions, stable replay, and atomic rerun request consumption. Tests prove byte/row/sidecar invariance for seeded, absent, and corrupt-profile dry runs; two distinct reserved sessions across a rerun. Live cancellation routing remains incomplete.
- P1-6d: added `AgentDirector` bounded typed output validator and judged-work view with four tests. Director child reservation, durable output/escalation, judged-attempt acceptance, and accounting remain incomplete because the current model/store seams do not represent them safely.
- P1-7b: created replacement example bundles under `examples/task-repair-loop/` and `examples/task-agent-director/`; nine CLI integration/example tests pass, including real reserved task attempts, gate acceptance, capacity wait, and dry-run invariance. Gate recovery, guard stop, director child accounting, and invalid-output escalation remain unverified. Standalone CLI bundle commands fail before loading on mutable-registry access, matching an existing example baseline.
- P1-7a: legacy auto-improve remains until required equivalent dispatcher suites pass on the before-removal tree.

## Verification and review handoff

- `swift build --scratch-path tmp/work-runtime-p1/build/p1-dispatch`: exit 1 before compilation on denied default Clang cache; complete log `tmp/work-runtime-p1-dag-20260922-8286b20f/p1-dispatch/joined-attempt-1/build-exact.log`. The scratch retry fetched an unresolved dependency and exited 1 on DNS (`joined-attempt-2/build.log`). Plan-local module cache plus the resolved local checkout and `swift build --disable-sandbox --skip-update` exited 0 after the final source split (`joined-attempt-2/build-local.log`).
- Four required dispatcher suites: 20 run, 20 passed, 0 failed, exit 0 after the final source split (`joined-attempt-2/focused-tests.log`). Exact scratch test command exited 1 before discovery on the same cache issue (`joined-attempt-1/focused-tests-exact.log`).
- Reservation 23/23, lifecycle 73/73, capability 53/53 passed with exit 0 in `joined-attempt-1/reservation-tests.log`, `lifecycle-tests.log`, and `capability-tests.log`. Final selected-file strict SwiftLint exited 0 (`joined-attempt-2/swiftlint-changed.log`); repository lint exited 0 before the final split (`joined-attempt-1/swiftlint-repository.log`).
- Broad Work/CLI/Core gate exited 1: 1,928 tests, 499 failures, including many mutable-registry access errors, one reproducible runner admission failure, and example parity before legacy removal (`joined-attempt-1/work-cli-core-tests.log`, `runner-admission-tests.log`). Adapter/Server/GraphQL gate exited 1: 178 tests, 14 listener-permission failures (`adapter-server-graphql-tests.log`). These are failed required gates; no acceptance decision is claimed.
- Four standalone replacement bundle validate/run commands each exited 2 before bundle loading on mutable-registry access (`repair-validate.log`, `repair-mock.log`, `director-validate.log`, `director-mock.log`); an existing example showed the same access error in `examples-attempt-1/logs/existing-example-baseline.log`.
- `rg` legacy-reference audit exited 0 with retained auto-improve references; `git diff --check` and `git diff --cached --check` exited 0. Complete logs are under `joined-attempt-1/`.
- Author self-check: P1-6/7 is incomplete. No Step 7 test-integrity or adversarial acceptance has occurred, and the final combined-tree review is pending. Do not remove legacy behavior or advance this plan to accepted.

## Completion criteria

- [ ] Exact reserved-session execution, placement delivery, terminal projection, guard/completion, and interruption behavior verified.
- [x] Read-only dry-run and replay-safe human decisions verified with positive tests.
- [ ] One bounded nonrecursive director child verified.
- [ ] Real task-backed replacement examples and equivalent dispatcher regressions pass before auto-improve removal.
- [ ] Legacy auto-improve removed; plain workflow, loop, routine, specialist, and event regressions pass.
- [ ] Required build, focused and broad tests, selected-file SwiftLint, and example commands have complete logs, final statuses, and positive test counts.
- [ ] Step 7 integrity and material adversarial reviews accepted; final combined-tree review remains with `p1-finalize`.

## Implementation attempt 2 — 2026-09-23

- Workflow execution: `nested-v1-2a2a64db3a4346a4d914a2aa79fd521f8fb2da3d7e0bcd0766eaee2f8c8c31c6`, branch `p1-dispatch`; checkpoint `c1d779f12646e0cc4d76334c21d8d108229e46ee`. Runtime `dependsOn: []` supersedes the historical predecessor list above. No external issue number was supplied.
- Fresh audit confirmed accepted design §17.2–17.7 aligns with this single plan. Reservation (23 tests), lifecycle (79), capability (53), and canonical sandbox (2) prerequisite suites passed on the current tree before dispatch edits. The exact scratch-path sandbox command failed before compilation on denied default Clang module cache; local resolved-checkout retry used the plan-local writable cache.
- P1-6a partial: `TaskDispatch` now persists terminal causal evidence after projection, reconciles the exact reserved attempt, reconstructs completion from the store, assembles guard signals, persists all violations, and applies the ordered policy through `TaskGuardCoordinator` before returning. The store exposes `currentCompletionVerdict` for the reconciled attempt. Task-backed tests now assert automatic acceptance, guard stop with persisted evidence, capacity wait, and human rerun/replay. P1-6a is still open for called-workflow/remote host placement, fresh placement refresh, interruption and complete guard signals.
- P1-6d remains open: `.director` entry is rejected, `AgentDirector.validate` still escalates accept, and no judged-work linkage, child execution or accounting is wired. P1-6c live cancellation/acknowledgment is also open. P1-7b lacks task-backed gate recovery and director child accounting/escalation. P1-7a legacy auto-improve remains intentionally installed until equivalent replacement behavior passes before deletion. No Step 7 integrity or adversarial acceptance is claimed.
- Edited source/test SHA-256 after this attempt: `Sources/RielaCLI/TaskDispatch.swift` `9bee80fb42b9e4c6729da5a2bc12ee0818319d2db62e245eaffdb969ed7e45a7`; `Sources/RielaWork/WorkStore+Decisions.swift` `cab6efe976a455e235b7ab55fd4caeab73bb613c5ec3e0fc52933737df1a98a6`; `Tests/RielaCLITests/TaskDispatcherIntegrationTests.swift` `9cfe155b2ca7e1cd5b84cecfae3d3719f84eb7ac0dfd8aba3a70c0e1af78ba0c`; `Tests/RielaCLITests/TaskRuntimeExampleTests.swift` `517ef7ebef7c6e531448d8d59a26d38dd32f57f9e21f1ab2371b1abd933a6240`. Pre-edit bytes, hashes, intended hunks and post-edit checks are under `tmp/work-runtime-p1-resume-20260923-comm000006-fe4da6cd/p1-dispatch/intents/`.
- Final local fallback `swift build --disable-sandbox --skip-update` exited 0 (`attempt-1/build-local-final-2.log`); focused V1 exited 0, 23/23 tests (`attempt-1/focused-local-final.log`). Nonbudget warn violations now persist while satisfied completion still applies through the shared applier; a real task example regression covers this. Strict selected-file SwiftLint of all 73 changed plan-owned Swift files and repository SwiftLint exited 0 (`attempt-1/swiftlint-plan-swift-final-2.log`, `attempt-1/swiftlint-repository-final.log`). `git diff --check` and `git diff --cached --check` exited 0 before this final progress edit; rerun after it. All paths in this paragraph are under `tmp/work-runtime-p1-resume-20260923-comm000006-fe4da6cd/p1-dispatch/`.
- Broad V5 Work/CLI/Core exited 1: 1,930 tests, 490 failures (`attempt-1/work-cli-core-local.log`), including mutable-registry access and capability validation failures. The Adapter/Server/GraphQL/AppSupport command returned exit 0, but its XCTest log ends after `DistributedControllerSettingsTests.testProfileSwitchStopsOldListenerAndUsesNewCredentials` starts, with no terminal suite summary (`attempt-1/adapter-server-graphql-local.log`). A targeted rerun returns 0 with the same incomplete XCTest log and zero Swift Testing cases (`attempt-1/distributed-controller-settings-local.log`). This gate is incomplete, not passing. Four standalone example commands each exited 2 before loading on mutable-registry access (`attempt-1/repair-validate-local.log`, `repair-mock-local.log`, `director-validate-local.log`, `director-mock-local.log`). The exact scratch build and focused command each exited 1 before compilation on denied Clang cache (`attempt-1/build-exact.log`, `focused-exact.log`). These remain failed or incomplete required gates, not accepted verification.
- Author self-check: strict SwiftLint of all 73 surviving changed plan-owned Swift files exited 0 (`attempt-1/swiftlint-plan-swift-final-2.log`; manifest `changed-swift-files.nul`). Material open P1 findings remain: no bounded director child or judged-work acceptance, no live cancellation acknowledgment, no called-workflow/remote placement, incomplete task-backed example cases, and no before-removal equivalent replacement evidence. Cumulative wall-clock/repeated-finding signals still need dispatch-level coverage before accepting P1-6a. Preserve all completion boxes above as open except the previously verified dry-run/human replay box. Do not advance the plan/index or remove legacy auto-improve based on this partial attempt.

## Implementation attempt 3 — 2026-09-23

- Workflow execution: `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, branch `p1-dispatch`, mode `issue-resolution`; runtime `dependsOn: []`. The accepted design §17.2–17.7 and this plan remain aligned. Read-only subagent audits found the remaining P1-6a/c/d and P1-7 gaps; no new external issue was supplied.
- P1-PREREQ-L: task terminal guard evaluation now sums wall-clock duration from each exact durable attempt session, rejects missing or nonterminal session snapshots, and saturates at `Int.max`. `testSecondRunStopsWhenEarlierDurableSessionExhaustsWallClockBudget` proves a prior five-second session triggers a persisted wall-clock violation and task stop after a later rerun. Repeated-finding and inactivity dispatch inputs remain open.
- P1-6a partial: task preview now uses the existing reachable workflow/add-on maps from its selected entry, including callee backend and environment requirements, and forwards local add-on availability to the host resolver. `testDryRunIncludesReachableCalleeBackendBeforeReservation` proves an unavailable callee-only backend yields capacity wait and zero attempts. Configured workers, fresh placement before reservation, and selected-host/callee runner delivery remain open.
- Final-source focused V1 fallback exited 0 with 25/25 tests and zero failures (`tmp/p1-dispatch-step6-attempt3/logs/focused-v1-local.log`). The narrower TaskDispatcherIntegrationTests run exited 0 with 8/8 tests and zero failures (`logs/integration-local-3.log`). Exact scratch-path test exited 1 before compilation on denied default Clang cache (`logs/integration-exact.log`); the writable module-cache and resolved-checkout fallback was used. An intermediate new-fixture run failed validation because its synthetic codex-agent payload omitted `agentSandbox` (`logs/integration-local-2.log`); corrected fixture rerun passed. Selected-file strict SwiftLint exited 0 (`logs/swiftlint-selected-final.log`; manifest `changed-swift-files.nul`). All attempt-3 evidence paths are under `tmp/p1-dispatch-step6-attempt3/`; per-edit intents are in its `intents/` directory.
- P1-6c live cancellation and durable terminal acknowledgment; P1-6d linked bounded director child, judged-work acceptance and accounting; P1-7b task-backed gate recovery and director cases; P1-7a replacement-first legacy removal; and broad final-source verification remain incomplete. Prior broad mutable-registry failures and truncated XCTest aggregate remain unresolved. No integrity, adversarial, or combined-tree acceptance is claimed. No completion checkbox or index status advances from this partial attempt.
- Final-source combined V2–V4 prerequisite filter exited 0 with 155/155 tests and zero failures (`tmp/p1-dispatch-step6-attempt3/logs/prereq-v2-v4-local.log`). This covers the retained reservation, lifecycle, and capability suites after attempt-3 edits; it does not certify the still-open runner and director contracts.

## Implementation attempt 4 — 2026-09-23

- Workflow mode `issue-resolution`, plan `p1-dispatch`, Step 6 execution `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`. Runtime `dependsOn: []`; accepted design §17.7 and plan remain aligned. Read-only `dispatch_audit`, `director_audit`, and `verification_audit` Codex agents confirmed remaining paths. Per-edit snapshots and intent are under `tmp/work-runtime-p1/p1-dispatch/attempt-4/intents/`.
- `WorkflowRunCommand+TaskReservation.swift` now limits the one-use task launch token to the exact reserved root session; nested callee sessions receive ordinary process admission. `TaskGuardCoordinator.swift` now derives gate visits and consecutive repeated-finding rounds from durable results using the existing `LoopConvergenceTracker`; `TaskDispatch.swift` feeds both guard signals. The callee fixture in `TaskDispatcherIntegrationTests.swift` now declares `agentSandbox: .readOnly`. `WorkGuardDispatcherTests.swift` checks repeated rounds and reset. These are bounded repairs, not acceptance of selected-host/callee execution or live inactivity monitoring.
- Exact focused scratch command exited 1 before compilation on denied default Clang cache (`tmp/work-runtime-p1/p1-dispatch/attempt-4/logs/focused-exact.log`). Fallback with plan-local `CLANG_MODULE_CACHE_PATH`, `SWIFTPM_MODULECACHE_OVERRIDE`, `--disable-sandbox --skip-update` exited 0, 22/22 tests, zero failures (`logs/focused-local.log`). Strict selected-file SwiftLint of 74 changed/untracked Swift files exited 0 (`logs/swiftlint-selected.log`, `changed-swift-files.nul`).
- The four-suite V1 fallback exited 0, 25/25 tests, zero failures (`logs/focused-v1-local.log`). Combined V2–V4 fallback exited 0, 156/156 tests, zero failures (`logs/prereq-v2-v4-local.log`). These certify the tested retained behavior, not the still-missing integration paths.
- A real-reservation admission regression confirms the root token is consumed once while a child session passes process admission; the final narrow filter exited 0, 23/23 tests (`logs/admission-guard-final.log`). Final four-suite V1 on this test tree exited 0, 26/26 tests (`logs/focused-v1-final.log`).
- Material P1-6a/c/d and P1-7b/a contracts remain open: selected remote/callee host delivery, live cancellation and durable acknowledgment, bounded director child and judged-work acceptance/accounting, task-backed replacement cases and the before-removal barrier. Final V0–V11, broad gates, integrity/adversarial reviews, docs/index and final commit/push are not complete. Prior mutable-registry failures and incomplete XCTest aggregate remain unresolved. No completion criterion advances and legacy auto-improve stays installed.

## P1-6b Step 6 attempt — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6b` (no GitHub number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, branch `p1-dispatch`, runtime `dependsOn: []`. Step 3 design and Step 5 plan were accepted at `comm-000004` and `comm-000006`; implementation acceptance remains pending. Read-only Codex agents `cli_audit` and `readonly_audit` identified concrete result and SQLite sidecar defects. Per-edit snapshots and intents are under `tmp/work-runtime-p1-6b-20260924-2f10916-comm000006/plans/p1-dispatch/attempt-1/intents/`.
- Retained P1-6a dispatcher and runner. `TaskDispatch.swift` now emits typed status, exact reserved IDs and diagnostics after admitted failures, a structured pre-admission failure envelope, and text placement. `WorkStore.swift`/`TaskDispatcher.swift` use strict reads; `HostCapabilityResolver.swift` diagnoses a corrupt active-profile selection. A new bounded `SQLiteDatabase.swift` path expansion treats a zero-byte WAL as idle for immutable reads; dry-run rejects a nonempty WAL before opening the store. This expansion needs independent review. Parsing and focused result/read-only tests were added; integration assertions now read the structured failure channel. No schema, example, or later-slice behavior was changed.
- Final-source Xcode SwiftPM fallback (`CLANG_MODULE_CACHE_PATH` and `SWIFTPM_MODULECACHE_OVERRIDE` under this attempt, `--disable-sandbox --skip-update`) build exited 0 (`logs/build-local-typed-final.log`); exact scratch build exited 1 before compilation on denied `/Users/taco/.cache/clang/ModuleCache` (`logs/build-exact-typed-final.log`). The final safe regression filter exited 0 with 165/165 tests (`logs/regression-safe-final.log`); the final non-network integration filter exited 0 with 15/15 tests (`logs/integration-safe-final-2.log`). Strict changed-file SwiftLint exited 0 (`logs/swiftlint-typed-final.log`, manifest `changed-swift-files.nul`). `git diff --check` and `git diff --cached --check` exited 0 in `logs/diff-check-final.log` and `logs/cached-diff-check-final.log` before this progress correction; repeat after the final edit.
- The broader V1/V11-style filter exited 1 with 194 tests and 28 failures on an intermediate source (`logs/regression-final.log`). Four structured-error assertions were updated and their final non-network integration rerun passed 15/15. The remaining listener failures report `Network.NWError error 1 - Operation not permitted` from `RielaLocalHTTPServer`; eight selected-host integration cases and eight `DistributedWorkerHTTPTests` cannot be accepted from this sandbox run. Do not count the failed aggregate as passing. Exact scratch focused tests also exited 1 before compilation on the Clang cache; local fallback focused test runs passed (`logs/focused-local-4.log`, 12/12 on the then-current source). Final 165/165 and 15/15 logs supersede the affected focused evidence.
- Final source SHA256: `TaskDispatch.swift` `55bf75de311d17aaf5706ab47d59b74919f1f0a73706558bd48a3730a07845ef`; `HostCapabilityResolver.swift` `53422a9cd3f265578ccdb9c9279cbd545628885e5f1f9a2cab9322328518d2c9`; `TaskDispatcher.swift` `0f566d6e173332cd701c8b3805d6d358923131821ad907457a5578b114813564`; `WorkStore.swift` `6060163acf96500ef4b40ce1be8ebd6861965e6af71c1fb2bec60809cf824497`; `SQLiteDatabase.swift` `f612ea5a5c01d232d42d9069dffee093778e56fa767a95e1fa8cce8d3951701c`. Test hashes are recorded in attempt evidence. P1-6b read-only matrix final verification and independent integration/adversarial decisions remain open. No commit, push, or parent P1 acceptance is claimed.

## P1-6b operator-host verification — 2026-09-24

- After Step 6 terminated, the complete selected aggregate passed 194/194 on a listener-capable host with exit 0. This includes the P1-6b dry-run/result tests, reservation and capability suites, `TaskDispatcherIntegrationTests`, and `DistributedWorkerHTTPTests`. Exact command, full terminal log, 9 before/after source hashes and limitations are in `tmp/work-runtime-p1-6b-host/evidence.json` and `aggregate.log`. The nine hashes match the Step 6 source evidence; `git diff --check` exited 0. The earlier sandbox aggregate remains a failed sandbox run, not a pass.
- This removes the source-matched V11 host verification gap. Independent adversarial/integration review, the bounded `SQLiteDatabase.swift` scope decision, docs, exact-file commit and non-force push remain pending. P1-6c/d, P1-7a/b and parent P1 remain open.

## P1-6b independent Step 6 repair — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6b` (no GitHub issue number); workflow `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`, step `step6-implement`, plan `p1-dispatch`, `dependsOn: []`. Per-edit intentions are in `tmp/work-runtime-p1-6b-review-20260924-f8c9188-comm000008/plans/p1-dispatch/attempt-1/intents/`; final source hashes and complete foreground logs are in the same attempt directory.
- Test-integrity, adversarial and Astra combined-tree reviewers rejected the prior zero-byte-WAL shared-helper amendment: a live immutable reader could miss a concurrent WAL commit. The amendment was reverted, so `Sources/RielaSQLite/SQLiteDatabase.swift` has no diff. `TaskDispatch.swift` now reads private copied stores in canonical first-match order, rejects a nonempty WAL and compares original main/WAL/SHM bytes before ready/wait output. `WorkStore.swift` and `TaskDispatcher.swift` retain baseline live read mode and use immutable fallback only for explicitly private preview copies. New tests cover a concurrent writer, later-root active WAL, text wait, JSON ready placement, and text/JSON admitted failure with durable IDs. All three source reviews now accept the bounded repair without a remaining material code finding; Astra withholds overall verification acceptance until a final-source listener-capable aggregate passes.
- Current-source `swift test --disable-sandbox --skip-update` safe filter exited 0, 169/169 (`logs/regression-safe-final-source.log`); the selected 15-case integration filter exited 0, 15/15 (`logs/integration-safe-final-source.log`). Strict changed-file SwiftLint on the eight-file NUL manifest exited 0 (`logs/swiftlint-final-source.log`). The full selected aggregate exited 1 after 198 tests with 16 listener denials and eight dependent unwaited expectation failures (`logs/aggregate-final-source.log`); this is a failed sandbox gate. The earlier host 194/194 pass matches only three of the current nine Swift hashes and does not certify this repair. Exact current hashes are in `verification-evidence-final-source-v2.json` under the attempt directory.
- Final-source listener-capable aggregate, serial acceptance, exact-file commit and non-force push are pending. No publication is claimed. P1-6c/d, P1-7a/b and parent P1 remain active; keep this plan active.

## P1-6b final-source Step 6 acceptance — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6b` (no GitHub issue number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, plan `p1-dispatch`, `dependsOn: []`, checkpoint `7de135fe`. `python3 tmp/work-runtime-p1-6b-finalize-20260924-aeb44fb8/verify_sources.py` exited 0: all nine current Swift source/test SHA-256 values match both `tmp/work-runtime-p1-6b-host-final/evidence.json` and `tmp/work-runtime-p1-6b-review-20260924-f8c9188-comm000008/plans/p1-dispatch/attempt-1/verification-evidence-final-source-v2.json`. The current check's complete log, exit and hash inventory are in `tmp/work-runtime-p1-6b-finalize-20260924-aeb44fb8/attempt-1/verification-current.json` and `hash-check.log`.
- Reused source-matched foreground verification: the exact host command and environment in `tmp/work-runtime-p1-6b-host-final/evidence.json` exited 0, 198/198 tests, zero failures; `aggregate.log` contains the terminal summary and listener-backed selected-host/HTTP cases. The final-source Step 6 safe commands in `verification-evidence-final-source-v2.json` exited 0 with 169/169 and 15/15 tests, zero failures, and strict changed-file SwiftLint exited 0; their complete logs are under that attempt's `logs/`. Its separate sandbox aggregate exited 1 after 198 tests with 24 listener/dependent failures and remains failed. This documentation update did not change any of the nine Swift hashes or rerun the host aggregate.
- The preserved Step 6 payload at `tmp/work-runtime-p1-6b-finalization/artifacts/nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e/runtime-snapshot.json` records `/root/review_tests` test-integrity acceptance, `/root/review_adversarial` adversarial acceptance and `/root/astra_integration` source acceptance with overall verification withheld for the host gate. Their separate verbatim transcripts were not retained. Fresh read-only Codex `source_review` test-integrity and read-only decisions and `astra_combined` combined-tree decision accept the current private-copy first-match/read-only repair and result/reservation behavior with no material finding. The final-source host 198/198 closes the recorded verification condition.
- The eight reviewed Swift changes are `Sources/RielaCLI/HostCapabilityResolver.swift`, `Sources/RielaCLI/TaskDispatch.swift`, `Sources/RielaWork/TaskDispatcher.swift`, `Sources/RielaWork/WorkStore.swift`, `Tests/RielaCLITests/TaskCommandParsingTests.swift`, `Tests/RielaCLITests/TaskDispatcherIntegrationTests.swift`, `Tests/RielaCLITests/TaskDryRunReadOnlyTests.swift`, and `Tests/RielaCLITests/TaskRunResultTests.swift`. The exact downstream publication allowlist adds `design-docs/specs/design-work-runtime-consolidation.md`, `impl-plans/active/work-runtime-p1-dispatcher-guard-director.md`, and this progress file; unchanged `Sources/RielaSQLite/SQLiteDatabase.swift` is excluded. Step 6 performed no Git staging, commit or push. Serial Step 9–11 exact-file commit and non-force push to `origin/feat/remaining-impl-plans` remain pending. P1-6c/d, P1-7a/b and parent P1 remain open; this plan stays active.

## P1-6b Step 8 documentation refresh — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6b` (no GitHub issue number); workflow `codex-design-and-implement-review-loop-session-1`, step `step8-docs-refresh`, incoming `comm-000018` from `step7b-e2e-evidence`. Step 7b skipped browser E2E because no `web/` path changed, and accepted the skip as non-blocking. Step 8 reviewed `README.md` and `.codex/skills/riela-impl-workflow/SKILL.md` against accepted §17.5, the active plan, implementation and adversarial decisions. The skill's Step 8 contract was already aligned and remains unchanged.
- Updated the README Work Runtime section to state the accepted read-only private-copy dry-run, active-WAL/concurrent-change errors, ready/wait identity behavior and exact admitted IDs. This directly affected user-facing amendment extends the downstream exact-file publication allowlist with `README.md`; the earlier Step 6 allowlist above is superseded for Steps 9–11. No Swift source/test bytes changed. P1-6c/d, P1-7a/b and parent P1 remain open; this plan stays active.

## P1-6b publication receipt — 2026-09-24

- The immutable user-scope `codex-design-and-implement-review-loop` completed after accepted test-integrity, adversarial and Astra integration reviews. Its exact-file commit `7d8fc121a4f4469de7a40495282b53c9d813b8d4` contains the eight reviewed Swift changes, README, design, active plan and this progress record. The native `git-push` addon initially returned exit 128; a non-force `git push origin feat/remaining-impl-plans` succeeded, and `riela session resume ... --retry-failed-step` then passed the push and base-branch-integrate gates. `git ls-remote --heads origin feat/remaining-impl-plans` returned the same commit; the branch was clean afterward. The complete workflow records are under `tmp/work-runtime-p1-6b-host-final/`.
- This receipt closes only P1-6b finalization. P1-6c/d, P1-7a/b and parent P1 remain open in this active plan. Historical sections above retain their as-of-stage pending statements rather than claiming they are current status.

## P1-6c Step 6 partial implementation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number); workflow `nested-v1-2a2a64db3a4346a4d914a2aa79fd521f8fb2da3d7e0bcd0766eaee2f8c8c31c6`, branch `p1-dispatch`, plan `p1-dispatch`, `dependsOn: []`, checkpoint `0a74a070670a5cb73f6cd18e035adf61732a0b07`. Read-only Codex agents `store_audit`, `runner_audit` and `host_audit` inspected the coupled seams; the single owner made the edits. Their audit identified the missing cross-process cancellation read, absent live dispatch observation/acknowledgment, direct pre-decision Ctrl-C and selected-host cancellation without worker-stop proof.
- Changed `Sources/RielaWork/WorkStore+Reservation.swift` to read exact task/attempt/session cancellation state and `Tests/RielaWorkTests/WorkStoreCancellationTests.swift` to verify pending and acknowledged reads on a reopened store and reject a foreign session. Per-edit preimages, hashes and intentions are under `tmp/work-runtime-p1-6c-2c7cf9334b26/intents/`; final Swift hashes are in `source-hashes.txt`. This is a safe store seam only; it does not alter runner interruption or release the fence.
- Exact V0 scratch-path build exited 1 before compilation because the default Clang module cache was denied (`V0.log`). The same build with plan-local `CLANG_MODULE_CACHE_PATH` and `SWIFTPM_MODULECACHE_OVERRIDE`, `--disable-sandbox --skip-update`, exited 0 (`V0-retry.log`). The final focused test exited 0, 1/1 (`focused-final2.log`); selected V2 retry exited 0, 23/23 (`V2-retry.log`). Strict changed-file SwiftLint exited 0 (`lint-final2.log`), and `git diff --check` exited 0 (`diff-final.log`). All logs are complete foreground command logs under the same evidence root. The earlier lint and focused-test attempts failed, then were repaired; their logs remain there.
- P1-6c remains incomplete: live local and selected-host interruption, request-before-signal Ctrl-C, durable exact terminal acknowledgment in dispatch, prelaunch cancellation, persistence/acknowledgment-loss and replacement tests, final-source V1/V11/decisions/live/compatibility/aggregate gates, and independent test-integrity/adversarial/Astra reviews are outstanding. No P1-6c review acceptance, commit, push or parent/later-slice closure is claimed.

## P1-6c Step 6 continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number), workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch and plan `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `audit_dispatch`, `audit_runner` and `audit_store` inspected the accepted §17.2/§17.5 seams. The single owner edited `Sources/RielaCLI/TaskDispatch.swift`, this log and the active plan; the earlier uncommitted store/test WIP remains preserved. Per-edit preimages, intentions and hashes are under `tmp/work-runtime-p1/p1-6c/continuation/intents/`.
- Dispatch now reads the exact durable cancellation before projection or generic reconciliation. Pending cancellation returns a fenced error rather than projecting or clearing it; acknowledged replay requires the stored attempt outcome to match the reserved terminal snapshot and skips duplicate transition/guard evaluation. The first acknowledgment remains intentionally unwired because the selected worker has no durable stop proof. The prelaunch runner can reject authorization without persisting a cancelled snapshot; Ctrl-C still cancels before writing a decision; no cross-process observer is installed. These are material incomplete P1-6c requirements, not accepted residual risks.
- Exact V0 build failed before compilation on denied default Clang module cache (`continuation/V0.log`, exit 1); an initial local-cache retry used a relative SwiftPM cache path and failed validation (`V0-retry.log`, exit 1). The corrected absolute-cache retry with `--disable-sandbox --skip-update` built successfully (`V0-retry-absolute.log`, exit 0). V1 selected 41 tests and failed 16 listener-dependent assertions on `Network.NWError error 1 - Operation not permitted` (`V1.log`, exit 1); this is a failed host gate, not a pass. V2 passed 23/23 (`V2.log`, exit 0), and parsing/decisions passed 27/27 (`decisions.log`, exit 0). These selected tests do not cover live cancellation; final-source V11/live/compatibility/aggregate and capable-host evidence remain open. Exact commands and complete foreground logs are in the same continuation directory.
- Independent test-integrity, adversarial and Astra combined-tree reviews have not accepted this continuation. No P1-6c commit, push or closure is claimed; P1-6d, P1-7a/b and parent P1 remain active.

## P1-6c Step 6 second continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number), workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `audit_local`, `audit_remote` and `audit_store` inspected accepted §17.2/§17.5 and current seams; `/root` exclusively integrated changes. Per-edit preimages, intentions and complete logs are under `tmp/work-runtime-p1/p1-6c/continuation-2/`.
- `Sources/RielaCLI/TaskRunCancellation.swift` now reads exact durable requests before and during an owned run, interrupts the runner once, retries transient read failures, and joins its observer. `TaskDispatch.swift` routes an exact cancelled local snapshot through the shared atomic acknowledgment after runner return, retaining the earlier acknowledged replay and generic-reconcile fence. The new `TaskCancellationIntegrationTests.swift` proves a separately issued CLI decision interrupts an active local sleep, persists a cancelled snapshot, acknowledges once and leaves one attempt and one decision.
- Selected-host changes in `DistributedWorkerModels.swift`, `DistributedJobController.swift`, `DistributedNodeExecution.swift`, `DistributedWorkerProtocol.swift`, `DistributedWorkerHTTPRouter.swift` and `DistributedWorkerLoop.swift` add a durable stop receipt bound to original worker incarnation and lease token. The worker sends it after its execution group joins; the queued executor awaits queued nonexecution or receipt for a bounded period when cancelled. Controller and router tests reject foreign/stale proof and preserve idempotent replay and lease-loss fencing. `DistributedProcessCancellationTests.swift` now asserts a receipt follows leader and child stop. `TaskDispatch.swift` intentionally retains the selected-host fence because its terminal acknowledgment path does not yet consume durable remote proof.
- The author self-check found and repaired a retention race: cancelled claimed jobs now remain in the controller snapshot until the stop receipt is durable, then archive and replay it safely. A zero-retention controller regression covers that case. Exact scratch build exited 1 before compilation on denied default Clang module cache (`build-exact.log`); same scratch path with plan-local `CLANG_MODULE_CACHE_PATH`, `SWIFTPM_MODULECACHE_OVERRIDE`, `--disable-sandbox --skip-update` built successfully (`build-fallback.log`, exit 0; later source compiled in final test). Final-source safe selected tests passed 92/92, zero failures (`final-safe-3.log`, exit 0). The live local test passed 1/1 (`live-local-3.log`, exit 0). The real process test failed 1/1 before execution at local listener startup with `Network.NWError error 1 - Operation not permitted` (`process-test.log`, exit 1); this is a host-capability failure, not a pass. Strict exact changed-file SwiftLint passed (`lint-final-4.log`, exit 0; manifest `changed-swift-files.nul`). `git diff --check` and `git diff --cached --check` both exited 0 (`diff-check-final.log`, `cached-diff-check-final.log`).
- P1-6c remains incomplete: prelaunch cancellation lacks a canonical cancelled snapshot, task-backed Ctrl-C still cancels before a durable decision, selected-host acknowledgment does not yet consume the durable stop receipt, and the full failure/replay/replacement matrix and final-source V1/V11/aggregate capable-host gates remain open. No independent test-integrity, adversarial or Astra combined-tree acceptance, commit or push is claimed. P1-6d, P1-7a/b and parent P1 remain active.

## P1-6c Step 6 third continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number), workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `prelaunch_audit`, `signal_audit` and `remote_proof_audit` inspected the remaining seams; `/root` made all edits. Preimages and intentions are under `tmp/work-runtime-p1/p1-6c/continuation-3/intents/`.
- `WorkStore+Reservation.swift` now persists the exact prelaunch cancelled session in the reservation transaction; `TaskRunCancellation.swift` saves the corresponding CLI record and prevents runner launch. `EntryPoint.swift`, `TaskCommands.swift` and `TaskDispatch.swift` route task-run signals through one stable shared-applier cancel decision before interrupting the owned run. Pre-reservation and pre-authorization signal tests and live local signal tests passed. Other commands retain their signal path.
- `DistributedJobController.swift` now checks root and durable nested child session jobs and requires a joined worker-stop receipt for claimed cancelled work. `TaskDispatch.swift` consumes that proof before selected-host exact acknowledgment and holds the fence for lost, queued, leased or unproven cancelled work. Controller reopen/proof tests passed. Real selected-host dispatch and process-tree stop remain unverified on this host.
- The corrected fallback build exited 0 (`continuation-3/remote-build.log`). Focused final-source tests passed 49/49, zero failures (`continuation-3/final-selected-tests.log`); V2/decisions/compatibility selected tests passed 61/61, zero failures (`continuation-3/v2-decisions-compat.log`). The selected V1/V11/HTTP/process suite failed 25 of 54 tests because this host denied local listener startup before their worker assertions (`continuation-3/v1-v11-selected.log`, exit 1). Strict changed-file SwiftLint passed (`continuation-3/lint-3.log`). Exact commands and final statuses are stored alongside these complete logs. Required capable-host V1/V11, full aggregates, live selected-host, persistence/acknowledgment-loss and replacement gates remain open. Independent test-integrity, adversarial and Astra combined-tree reviews have not accepted; no P1-6c commit or push was made. P1-6d, P1-7a/b and parent P1 remain active.
- The affected CLI/Work/Core/Server aggregate reached terminal exit 1 with 2,040 tests and 533 failures (`continuation-3/aggregate.log`). Its failures include listener denial and existing Git/workflow fixture errors; this is failed broad evidence, not a passing gate or a source-matched capable-host classification. Full failure triage and capable-host rerun remain open.
- Author self-check found that execution setup or persistence errors after reservation escaped the per-attempt result envelope. `TaskDispatch.swift` now returns the reserved attempt/session IDs while leaving the fence held; `testReservedExecutionFailureReturnsIdentityAndKeepsFence` passes. The focused final-source rerun passed 7/7 (`continuation-3/selfcheck-tests-2.log`, exit 0). The initial test variant used the wrong JSON date strategy and failed before asserting behavior (`selfcheck-tests.log`, exit 1); it was corrected and rerun.
- After that repair, the final Swift-source safe selection passed 89/89, zero failures (`continuation-3/final-source-safe.log`, exit 0). Strict exact changed-file SwiftLint passed (`lint-final-3.log`, exit 0; `changed-swift-files-final.nul`). The prior failed aggregate and V1/V11 command ran before the final error-envelope repair; both remain failed evidence and require fresh capable-host final-source verification.

## P1-6c Step 6 fourth continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `failure_matrix_audit` and `remote_evidence_audit` found material fence gaps; `/root` made the serial edits. Per-edit preimages and intentions are under `tmp/work-runtime-p1/p1-6c/continuation-4/intents/`.
- `TaskDispatch.swift` now persists deterministic terminal evidence/findings before first cancellation acknowledgment; a failed projection leaves the attempt fenced, while acknowledged replay validates the exact stored outcome before projecting. Signals received before capacity wait commit through the shared applier. `DistributedJobController.swift` requires each persisted started admitted remote execution to have its exact controller job, so an empty or repointed controller cannot prove stop; queued never-claimed jobs remain eligible after cancellation. The new CLI/Core regressions pass.
- `WorkStoreCancellationTests.swift` now uses the shared applier to prove a forced SQLite acknowledgment failure keeps task, lease and request pending across reopen, then exact retry and replay create no duplicate decision. A live rerun request remains fenced until acknowledgment and consumes one replacement after reopen. A CLI terminal-projection failure test confirms the error retains reserved IDs and the fence.
- Final Swift-source safe selected suites passed 94/94, zero failures (`continuation-4/final-safe.log`, exit 0). The final-source V1/V11/HTTP/process selection reached exit 1, 54 tests, 25 listener-startup/dependent failures (`continuation-4/final-v1-v11.log`); the local host denied listener creation before worker assertions. Strict exact changed-file SwiftLint passed (`continuation-4/lint-final-2.log`, exit 0; `changed-swift-files-final-2.nul`). Full affected aggregate has not been rerun on this source; the prior 2,040-test aggregate failed and is not acceptance evidence. Real selected-host interruption/process stop, complete race/failure accounting, capable-host V1/V11 and aggregate evidence, independent test-integrity/adversarial/Astra reviews, docs finalization, commit and push remain open. P1-6d, P1-7a/b and parent P1 stay active.
- The exact final-source scratch-path build failed before compilation on denied default Clang module cache (`continuation-4/build-exact.log`, exit 1). The same scratch path with plan-local Clang/SwiftPM module caches and `--disable-sandbox --skip-update` built successfully (`continuation-4/build-fallback.log`, exit 0). Complete command logs and exit files remain in that evidence directory.

## P1-6c Step 6 further continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `test_gap`, `aggregate` and `code_path` investigated independent seams; `/root` made the serial edits. Per-edit preimages, intentions and hashes are under `tmp/work-runtime-p1-6c-continue-20260924-6d8d200-comm000006/intents/`.
- `TaskCancellationIntegrationTests.swift` now issues the live local cancellation decision from an owned `riela` subprocess against the same session store. `TaskDispatcherIntegrationTests.swift` gives only this subprocess test a longer in-memory sleep node, so process startup cannot let the example finish first. The cross-process observer, exact decision/attempt/session, cancelled snapshot and once-only assertions passed. The final focused suite passed 8/8 with zero failures (`logs/cancellation-suite-final.log`, exit 0); strict exact changed-file SwiftLint passed (`logs/lint-final.log`, exit 0, `changed-swift-files.nul`). The sorted final Swift source/test manifest is `source-test-sha256-final.txt`, SHA-256 `868360ff316608629361f394cf8096413d33f3594ce0418ee313c7e99ab6a3bd`; HEAD is `c07910feb738d1f218d8dddff2a061f437b4f886`.
- The exact focused command failed before compilation because the default Clang module cache is not writable (`logs/local-process-exact.log`, exit 1). A plan-local-cache retry compiled, exposed the original one-second fixture race, and failed 1 test (`logs/local-process-fallback.log`, exit 1). After extending only the test's in-memory sleep, the focused rerun passed 1/1 (`logs/local-process-fallback-2.log`, exit 0); the final 8/8 suite supersedes it. Full command output and terminal exit files are in the same evidence directory.
- The operator-host historical aggregate under `tmp/work-runtime-p1/p1-6c/operator-host-listener/final-source-aggregate.log` failed 2,046 tests with 24 assertion failures (7 unexpected). Its `TaskProjectionProofTests.swift:105` mismatch is the evaluator's `.acceptanceNotMet` for that fixture's failed session, on unchanged projection/evaluator source; it does not establish a P1-6c cancellation defect. Other Doctor, parity and workflow fixture failures are outside the demonstrated slice. This aggregate remains failed and predates the current test edits; no source-matched final aggregate or capable-host live selected-host acceptance is claimed.
- The task-backed live selected-host test with a real worker/process, delayed stop proof and separately owned CLI process remains absent; remote lease/heartbeat, acknowledgment-response-loss and independent replacement-contender evidence remain incomplete. Final-source V1/V2/V11, decisions, compatibility, aggregate and capable-host gates, independent test-integrity/adversarial/Astra combined-tree acceptance, docs finalization, exact-file commit and non-force push remain open. No review or publication acceptance is claimed. P1-6d, P1-7a/b and parent P1 stay active.

## P1-6c Step 6 second productive continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `remote_fixture`, `race_matrix` and `host_review` investigated independent seams; `/root` alone edited tests and this log. Per-edit preimages, intentions and hashes are in `tmp/work-runtime-p1-6c-continue-20260924-6d8d200-comm000006/intents/`.
- `TaskCancellationIntegrationTests.swift` and new `TaskCancellationIntegrationTests+Fixtures.swift` now define a task-backed selected-host test with an authenticated worker, a real shell process and child, an owned separate `riela task decide` process, and a gate that delays the worker's durable stop receipt after executor exit. The assertions require the pending decision and lease to stay fenced before proof, then exact reserved cancelled persistence, acknowledgment, durable controller reopen and idempotent CLI decision replay. The test compiled, but this sandbox denied `RielaLocalHTTPServer` listener startup before those assertions (`logs/remote-live-final.log`, exit 1, 1 test/1 failure). It is not a behavioral acceptance pass. The test fixture was adjusted so listener denial produces no secondary unwaited-expectation failure.
- `WorkStoreCancellationTests.swift` now proves a successful acknowledgment whose response is discarded survives reopen; two independent store connections then race for the same pending replacement and exactly one consumes it. The focused test passed 1/1 (`logs/store-race-1.log`, exit 0). On the resulting source, V0 fallback build passed (`logs/v0-final.log`, exit 0), V2 passed 26/26 (`logs/v2-final.log`, exit 0), decisions passed 27/27 (`logs/decisions-final.log`, exit 0), and local cross-process plus compatibility passed 12/12 (`logs/local-compat-final.log`, exit 0). Strict exact changed-file SwiftLint passed (`logs/lint-final-2.log`, exit 0; `changed-swift-files-final.nul`). Complete foreground logs and exit files are in the same evidence directory.
- Final-source V11 selection failed 54 tests with 24 listener-startup/dependent failures (`logs/v11-final.log`, exit 1); the named live selected-host test failed before worker registration for the same `Network.NWError error 1 - Operation not permitted`. The complete `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` manifest is `source-test-all-final-2.sha256`, manifest SHA-256 `a080f90afd1018ac2d69325bfe29c015934fa5554084ef8ef9f214fd3d87c388`, HEAD `c07910feb738d1f218d8dddff2a061f437b4f886`. Historical operator-host 54/54 and failed aggregate are not source-matched to this tree and do not validate the new live test.
- The selected-host end-to-end path, lease/heartbeat uncertainty matrix, final-source V1 and affected aggregate, and source-matched capable-host acceptance remain open. Independent test-integrity, adversarial and Astra combined-tree reviews have not accepted; docs finalization, exact-file commit and non-force push remain pending. Keep P1-6c, P1-6d, P1-7a/b and parent P1 active.

## P1-6c Step 6 terminal handoff — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, plan/branch `p1-dispatch`, `dependsOn: []`. Read-only Codex agents `fixture_integrity` and `source_relevance` inspected only the new test and host evidence. `fixture_integrity` found a material test reliability issue: immediate PID checks could misread recently stopped descendants. `/root` made the serial bounded-poll and Linux zombie-aware repair in `Tests/RielaCLITests/TaskCancellationIntegrationTests+Fixtures.swift`; preimage, intent and hashes are under `tmp/work-runtime-p1-6c-continue-20260924-6d8d200-comm000006/intents/fixture-review-1/`. No production file changed in this continuation.
- On the repaired source, the exact selected-host test compiled and ran 1 test with 1 listener-startup failure before worker registration (`logs/remote-live-terminal.log`, exit 1). The source-matched safe selection passed 65/65, zero failures (`logs/safe-terminal.log`, exit 0); strict exact changed-file SwiftLint passed (`logs/lint-terminal.log`, exit 0; `changed-swift-files-final.nul`). The complete `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` manifest is `source-test-all-terminal.sha256`, SHA-256 `321b9d2bb81e4c9bee63245d78dacc5626d44f354172427bc8046075cda890f3`, HEAD `c07910feb738d1f218d8dddff2a061f437b4f886`.
- Before that fixture-only repair, the V1 selection completed 41 tests with 16 listener-dependent failures (`logs/v1-terminal.log`, exit 1); `TaskProjectionProofTests` completed 2 tests with 4 mutable-registry fixture failures in this sandbox (`logs/projection-terminal.log`, exit 1); and the affected aggregate completed 2,048 tests with 534 failures, 181 unexpected (`logs/aggregate-terminal.log`, exit 1). These complete foreground logs are failure evidence on the immediately preceding test source, not passing or source-matched final acceptance. `source_relevance` confirmed the historical operator-host aggregate's extra `.acceptanceNotMet` reason arises from an unchanged failed-session fixture/evaluator expectation, not a demonstrated P1-6c cancellation regression. The old operator-host 54/54 cannot validate the new test or current manifest.
- The bounded Step 6 continuation limit is exhausted. Task-backed selected-host stop/proof/persistence/acknowledgment remains behaviorally unverified because this sandbox consistently denies listener creation with `Network.NWError error 1 - Operation not permitted`. A listener-capable host must run the exact new test and affected V1/V11/aggregate on the current manifest with complete exits and positive counts, then address any material source failure. Lease/heartbeat uncertainty evidence also remains incomplete. Independent test-integrity, adversarial and Astra combined-tree reviews, docs finalization, exact-file commit and non-force push remain pending. No P1-6c acceptance or publication is claimed; P1-6d, P1-7a/b and parent P1 remain active.

## P1-6c decision-identity Step 6 continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`, checkpoint HEAD `30601aa89485436440728bf417b0156ea477f25d`. Read-only Codex agents `decision_audit`, `code_audit` and `test_audit` traced the distinct decisions; `/root` made the sole edit to `Tests/RielaCLITests/TaskCancellationIntegrationTests+Fixtures.swift`. Preimages and per-edit intent are in `tmp/work-runtime-p1-6c-decision-20260924-c07910f-comm000006/intents/`.
- The reservation transaction commits a generated `.start` decision with `.policy(rule: "task-run")`, the reserved attempt ID and no `causedBy` evidence before execution. The separate CLI process applies exact `decision-remote-live-cancel`, `.cancel`, `.human(principal: "operator")`, with the latest persisted same-attempt evidence as its cause. Source inspection supports two legitimate rows, not duplicate cancellation; the earlier host log did not retain their actual generated ID or causal evidence and test teardown removed the database. The fixture now captures and checks the reservation decision before cancellation, checks both records, their order, resolved cause and cancellation-record link after acknowledgment, prints their runtime IDs for the capable-host log, and requires the exact two-decision history plus evidence/outcome to survive replay. It retains the worker-stop, cancelled snapshot, lease, attempt and fence assertions. No production decision row or behavior was changed.
- On this tree the exact selected-host filter compiled, then failed 1/1 before worker registration because the sandbox denied `RielaLocalHTTPServer` listener startup with `Network.NWError error 1 - Operation not permitted` (`selected-host-test-absolute.log`, exit 1). The first cache retry used a relative path rejected by SwiftPM (`selected-host-test.log`, exit 1); the corrected command uses absolute plan-local module caches. The local separate-process cancellation test passed 1/1, zero failures (`local-test.log`, exit 0). Strict exact changed-file SwiftLint passed (`lint.log`, exit 0; `changed-swift-files.nul`). Full foreground logs and exit files are in `tmp/work-runtime-p1-6c-decision-20260924-c07910f-comm000006/`. The `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` manifest there has SHA-256 `e5585daf0db06615f2d01ed61be3631f3577c43b94700c3b23f5b87518b8ac7a`.
- Completion criteria remain open: a listener-capable host must run the corrected test on this exact source identity and record both actual IDs/causes with a pass. Final-source V0/V1/V2/V11, live, decisions, compatibility, projection classification and affected aggregate gates; complete lease/heartbeat/failure matrix; independent test-integrity, adversarial and Astra combined-tree reviews; docs finalization, exact-file commit and non-force push are still pending. This Step 6 result is incomplete and does not enter review or publication. P1-6d, P1-7a/b and parent P1 remain active.

## P1-6c decision-identity Step 6 second continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`, checkpoint HEAD `30601aa89485436440728bf417b0156ea477f25d`. Read-only Codex agents `matrix_gap` and `evidence_gap` checked only remaining matrix and source-matched verification. `/root` edited `Tests/RielaCLITests/TaskCancellationIntegrationTests+Fixtures.swift`, `Tests/RielaCoreTests/DistributedJobControllerTests.swift`, this log and active-plan status. Preimages and intended hunks are under `tmp/work-runtime-p1-6c-decision-20260924-c07910f-comm000006/continuation-2/intents/`.
- The delayed selected-host stop gate now advances controller observation beyond job lease and worker heartbeat and checks offline status, absent stop proof, pending cancellation and retained work lease. A listener-free controller regression checks the cancelled claimed job retains its original lease and no stop receipt after the same time advance, then accepts only the original joined-worker receipt across reopen. The fixture's decision assertions moved into a focused helper to satisfy the repository's strict SwiftLint function-length rule without changing their checks.
- On the final Swift source, the listener-free selected suite passed 84/84, zero failures (`continuation-2/safe-final-2.log`, exit 0), including local separate-process cancellation, WorkStore persistence/acknowledgment/replacement races and controller stop proof. The focused controller regression passed 1/1 (`controller-proof.log`, exit 0). Strict exact changed-file SwiftLint passed (`lint-final.log`, exit 0; `changed-swift-files.nul`). The live selected-host filter compiled but failed 1/1 at sandbox listener startup before worker registration (`selected-final-2.log`, exit 1). `TaskProjectionProofTests` ran 2 tests with 4 failures because the sandbox rejected mutable registry access before either projection assertion (`projection.log`, exit 1); this is failed environment evidence, not a pass. Complete foreground logs and exit files are under `tmp/work-runtime-p1-6c-decision-20260924-c07910f-comm000006/continuation-2/`. The final `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` manifest there has SHA-256 `6d3ef8ee373d32a7ed1deac12a969a5366b71117c9019031869a3759debf5e96`.
- P1-6c remains incomplete. No capable-host run matches the final source manifest, so the generated reservation ID, exact cancel causal evidence, selected-host stop/acknowledgment/replay, V1/V11 and affected aggregate cannot be accepted. Projection classification also lacks source-matched capable-host evidence. Independent test-integrity, adversarial and Astra combined-tree reviews, serial docs, exact-file commit and non-force push remain pending. P1-6d, P1-7a/b and parent P1 stay active.

## P1-6c decision-identity Step 6 terminal handoff — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `p1-dispatch`, `dependsOn: []`, checkpoint HEAD `30601aa89485436440728bf417b0156ea477f25d`. Read-only Codex agent `aggregate_classify` classified final-source failures; `/root` only updated plan/progress status. Per-edit preimages and intentions are under `tmp/work-runtime-p1-6c-decision-20260924-c07910f-comm000006/continuation-3/intents/`.
- The final Swift source matches `continuation-2/source-test.sha256` (manifest SHA-256 `6d3ef8ee373d32a7ed1deac12a969a5366b71117c9019031869a3759debf5e96`; `continuation-3/source-check.log`, exit 0). V0 built (`v0.log`, exit 0). V1 ran 41 tests with 16 listener-related failures (`v1.log`, exit 1); V11 ran 54 with 24 listener-related failures (`v11.log`, exit 1). Live ran 10 with two failures, both at listener startup before selected-host/process-stop assertions (`live.log`, exit 1). The affected aggregate ran 2,048 tests with 534 failures, 181 unexpected (`aggregate.log`, exit 1). Its selected-host and distributed-worker failures include the same listener denial; projection failures arise from inaccessible mutable registry files. Numerous workflow and Git add-on failures are outside the demonstrated P1-6c slice, but are not certified baseline failures by a source-matched comparison. All logs are complete foreground outputs with terminal `.exit` files under `tmp/work-runtime-p1-6c-decision-20260924-c07910f-comm000006/continuation-3/`.
- The bounded Step 6 continuation limit is exhausted with material behavioral verification still open. A listener-capable host must run the corrected selected-host test and required V1/V11/live/projection/affected aggregate against this exact source manifest, record the generated reservation decision ID and cancel causal evidence, and resolve any material source failure. Independent test-integrity, adversarial and Astra combined-tree reviews cannot accept this incomplete tree; serial docs, exact-file commit and non-force push remain pending. No P1-6c review, publication or closure is claimed. P1-6d, P1-7a/b and parent P1 stay active.

## P1-6c host classification Step 6 attempt — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `feat/remaining-impl-plans` / `p1-dispatch`, checkpoint `c16e97501b5be5b6b1904c100b682c965eebba44`, `dependsOn: []`. `/root` owns the serial edits. Read-only Codex agents `classify`, `catalog_audit`, and `projection_audit` investigated disjoint evidence; no independent review acceptance is claimed. Per-edit preimages, hashes and intentions are under `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/intents/`.
- The original source manifest check exited 0 before edits. The supplied capable-host receipt records selected-host 1/1, V11 plus live 55/55 and V1 plus cancellation 50/50, each exit 0, with one policy start, one separate-process human cancel, causal evidence and unchanged replay history. The original broad aggregate remains **failed**: 2,048 tests, 24 assertions, seven unexpected. The assertion-level inventory in `reviews/evidence/failures.json` classifies five direct catalog/projection assertions and 19 other assertions (Doctor 7, P1-7b examples 1, workflow commands 8, runner admission 1, temporary workflow 2); these other failures remain explicit follow-up, not passing baseline evidence. The inventory needs final-source focused reproduction and independent slice-only disposition.
- Added `task run` and `task decide` catalog rows in `Sources/RielaCore/SurfaceCatalog+RowsCLI.swift`, mapped their actual typed parser options in `Sources/RielaCLI/CLISurfaceEnumeration.swift`, and updated structural/parity tests. `TaskProjectionProofTests.swift` now expects the existing evaluator's first `.acceptanceNotMet` reason for a failed session, preserving its gate and finding reasons. No cancellation, worker or evaluator production behavior changed. The repaired source/test manifest is `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/final-source.sha256` (SHA-256 `561db1140f6d0826250203a2746708a8f4a8b6b59265b57ac85a8ce97dea861f`); five Swift paths changed relative to the original host manifest, so the prior host receipt is historical evidence and is not represented as final-source acceptance.
- Exact scratch focused test exited 1 before compilation on denied default Clang cache (`catalog-projection.log`). The first local-cache retry exited 1 because a relative module-cache path was rejected (`catalog-projection-retry.log`). The absolute-cache retry compiled and ran 17 tests: catalog 7/7 and parity 8/8 passed, while both projection examples failed before their verdict assertions on sandbox mutable-registry access (`catalog-projection-retry-2.log`, exit 1, four assertions/two unexpected). A separate catalog/parity command passed 15/15, zero failures (`catalog-parity.log`, exit 0). Strict selected-file SwiftLint over the 23 existing/new Swift WIP paths passed (`changed-swift-files.nul`, `swiftlint-changed.log`, exit 0); `git diff --check` exited 0. All logs are complete foreground outputs under the attempt root.
- P1-6c remains incomplete. A capable host must rerun final-source projection and affected classification/aggregate checks; original selected-host/V1/V11 evidence needs dependency-relevance review against the five changed paths. The plan's final regression matrix, independent test-integrity/adversarial and Astra combined-tree decisions, accepted docs, exact-file commit and non-force push are pending. No Step 6 Git staging or publication occurred. P1-6d, P1-7a/b and parent P1 remain active.
- Final-source V2 store command passed 26/26 in `v2-store.log` (exit 0); `WorkStoreCancellationTests.swift` extends the `WorkStoreReservationTests` suite. The old filter selected no budget tests because their suite is actually `BudgetAdmissionDecisionApplierStoreTests`. A supplemental exact-suite command passed 5/5 in `budget-store.log` (exit 0), and the active V2 command now names that suite. These store results do not close host projection or broad regression gates.

## P1-6c host classification Step 6 continuation 2 — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, communication `comm-000010`, step `step6-implement`, plan `p1-dispatch`, `dependsOn: []`. `/root` retained serial edit ownership. Read-only Codex agents `host_relevance` and `classification_review` checked historical source relevance and assertion evidence; these are investigative decisions, not the pending independent test-integrity, adversarial or Astra acceptance. New intent/preimage records are under `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/intents/edit-007/` and `edit-008/`.
- The 24-record inventory remains complete and the host aggregate remains **failed** at 2,048 tests, 24 assertions, seven unexpected. `classification_review` found that eight WorkflowCommand assertions had been labeled preexisting using a prior sandbox run with a different failure cause. Their records now say `unresolved_non_slice_candidate`, preserve the observed capable-host failures, cite unchanged workflow dependencies and task-run-only EntryPoint gating, and require focused capable-host reproduction. Two truncated records now carry their full aggregate lines. The current-source classification filter exited 1 after 174 tests and 174 failures (`classification-final-source.log`); sandbox mutable-registry errors obscure equivalent host validation behavior. No baseline or slice-only acceptance is claimed for those eight assertions.
- `host_relevance` compared the prior 962-entry host manifest with the current 973-entry manifest: exactly five Swift hashes changed and 11 ancillary paths were newly included; no prior path disappeared. The changed production files add CLI surface catalog/parity behavior and have no references in the targeted cancellation, dispatcher or worker execution paths. The prior selected-host 1/1, V11 55/55 and V1 50/50 receipts remain relevant historical evidence for unchanged behavior, but the differing manifest prevents an exact final-source pass. Final-source manifest recheck exited 0 (`continuation-2-source-check.log`).
- Current-source V0 build exited 0 (`v0-final-source.log`). Sandbox V1 exited 1 after 50 tests and 17 failures (`v1-final-source.log`); sandbox V11 exited 1 after 55 tests and 25 failures (`v11-final-source.log`). The selected-host listener and related worker tests fail at `RielaLocalHTTPServer.swift:128` with `Network.NWError error 1 - Operation not permitted`, before worker registration; unwaited fixture expectations follow. These are complete failed logs, not passing cancellation evidence. Selected-file SwiftLint and catalog/store passes from the preceding attempt remain on identical source hashes.
- P1-6c remains incomplete: a capable host must run final-source projection, affected classification and broad gates, and either rerun selected-host/V1/V11 or obtain explicit independent relevance-based acceptance with the manifest mismatch recorded. Eight workflow-command assertions remain unresolved. Independent test-integrity, adversarial and Astra combined-tree reviews, accepted docs, exact-file commit and non-force push remain pending. P1-6d, P1-7a/b and parent P1 stay active.
- The final-source sandbox affected aggregate reached terminal exit 1: 2,048 tests, 530 failures, 181 unexpected (`aggregate-final-source-sandbox.log`, complete). `SurfaceParityCLITests` passes; the selected-host test fails before worker registration at listener startup, and both `TaskProjectionProofTests` examples fail before verdict assertions on mutable-registry access. The source/test manifest recheck after the aggregate exited 0 (`after-aggregate-source-check.log`). This is a failed broad gate and does not replace a capable-host run.
- Targeted GraphQL catalog/parity checks passed on the same source: `SurfaceParityGraphQLTests|SurfaceParityDTOSchemaTests|SurfaceParityExecutorCoverageTests` executed 11 tests, one skipped, zero failures (`graphql-parity.log`, exit 0). No GraphQL task binding was added; P5 remains deferred.

## P1-6c host classification Step 6 terminal continuation — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, communication `comm-000012`, step `step6-implement`, plan `p1-dispatch`, `dependsOn: []`, checkpoint `c16e97501b5be5b6b1904c100b682c965eebba44`. `/root` retained serial edit ownership; read-only Codex agents `registry_probe` and `host_triage` investigated disjoint registry and historical baseline evidence. They are not the pending independent test-integrity, adversarial or Astra reviewers. New preimage/intention records are under `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/intents/edit-011/` and `edit-012/`.
- `registry_probe` traced the sandbox projection failure to a mutable-registry lock opened for writing under the default user HOME. A dedicated writable HOME under repository `tmp/` let the unchanged final-source `TaskProjectionProofTests` pass 2/2 and the full `SurfaceCatalogTests|SurfaceParityCLITests|TaskProjectionProofTests` gate pass 17/17, zero failures (`projection-home-test.log`, `catalog-projection-home-test.log`, both exit 0). No production registry change was made.
- `host_triage` found the same eight WorkflowCommand failure assertions before P1-6c: IDs 15–21 in the preimplementation capable-host log at commit `39d08a4` and ID 14's identical sandbox-validation error at pre-P1-6c checkpoint `cbe1dd1`. Relevant WorkflowCommand source/tests have no P1-6c diff. With writable HOME, the final-source classification filter ran 174 tests with 19 failures, seven unexpected (`classification-home-test.log`, exit 1), reproducing the eight workflow-command assertions and other non-slice categories. Their exact historical cause is unspecified for IDs 16–21; the owning workflow-command area retains follow-up. `reviews/evidence/failures.json` now has exactly 24 assertion records, each with source history and final-source focused command/log/exit/count: five direct P1 assertions resolve in the 17/17 gate, and 19 other assertions remain failed with explicit follow-up. The original 24/2,048 capable-host broad gate remains **failed**.
- The host selected-test log line 454 records `decision-3705b38f-a462-4551-beb1-31ab42162301` as policy `task-run` start with no cause, followed by `decision-remote-live-cancel` as human `operator` cancel caused by `evidence-placement-attempt-04fdcb91-ff2e-49e5-9c5a-fe7cdd4b5a4c`. `TaskCancellationIntegrationTests+Fixtures.swift:190-249` asserts same task/attempt identity, exact cancellation acknowledgment and terminal snapshot, stopped leader/child, two-row ordered decision history, causal evidence, and unchanged decision/evidence/outcome after replay. No decision fixture or producer repair was needed. The receipt remains historical for its original manifest because five Swift hashes have changed.
- The final-source affected aggregate with writable `tmp/` HOME reached terminal exit 1 after 2,048 tests, 240 failures, 90 unexpected (`aggregate-home-final-source.log`, complete). Catalog parity and projection pass in that aggregate; listener-backed selected-host/worker cases still fail before registration, and unrelated Git/workflow assertions remain. This is a **failed broad gate**. The source manifest recheck after the aggregate exited 0 (`post-home-aggregate-source-check.log`). Strict selected-file SwiftLint from the first attempt remains applicable to unchanged Swift bytes; no Swift source/test edits occurred in this continuation.
- The bounded Step 6 continuation leaves P1-6c incomplete. A capable host must run the final-source selected-host/V1/V11 and affected broad checks, or independent reviews must explicitly accept relevance-based reuse of the old listener-backed receipt despite the manifest mismatch; the accepted plan currently requires source-matched verification. Independent test-integrity, adversarial and Astra combined-tree acceptance, README/design status refresh, exact-file commit and non-force push remain pending. No Step 6 Git staging or publication occurred. P1-6d, P1-7a/b and parent P1 remain active.

## P1-6c final-source review Step 6 — 2026-09-24

- Mode `issue-resolution`; issue `Review and finalize P1-6c against final-source capable-host evidence` (no issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, fanout branch/plan `p1-dispatch`, `dependsOn: []`, checkpoint `133fdf94fe7ab249afc24884471b3d182892d42e`. `/root` retained serial edit ownership; Codex agents `evidence`, `integrity` and `adversarial` performed independent read-only audits. Per-edit preimages and intentions are under `tmp/work-runtime-p1-6c-final-review-20260924-c16e975-comm000006/intents/`. No Swift source or test bytes changed in this attempt.
- The 973-entry final-source manifest `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/final-source.sha256` has SHA-256 `561db1140f6d0826250203a2746708a8f4a8b6b59265b57ac85a8ce97dea861f`; current checksum verification exited 0. Complete source-matched host logs under `tmp/work-runtime-p1-6c-host-resume/` show catalog/parity/projection 17/17, V1 plus cancellation 50/50 and V11 plus real selected-host 55/55, all exit 0. The V11 receipt records distinct policy-start and human-cancel decisions with causal attempt evidence, and the selected-host test asserts stop proof, exact cancelled persistence, acknowledgment, retained fence under uncertainty and replay stability. Earlier interrupted aggregate exit 130 is excluded.
- The serial host aggregate `capable-host-aggregate-final-source-serial.log` exited 1 after 2,048 tests with 19 assertion failures, seven unexpected. Its 19 assertion sources match all 19 current non-slice records in `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/reviews/evidence/failures.json`: IDs 1–7 CLI doctor/backend-capability owner, ID 8 P1-7b examples owner, IDs 14–21 workflow-command/host-readiness owner, ID 22 workflow runner admission owner, IDs 23–24 temporary workflow/host-requirements owner. The five repaired catalog/projection assertions, IDs 9–13, are absent. The broad gate remains **FAILED**; the inventory does not waive it.
- Read-only test-integrity and adversarial audits found no material P1-6c defect and recommended slice-only acceptance on these exact source/test bytes, subject to formal workflow review and Astra combined-tree acceptance. They inspected `Sources/RielaCLI/TaskRunCancellation.swift`, `Sources/RielaCLI/TaskDispatch.swift`, `Sources/RielaWork/WorkStore+Reservation.swift`, `Sources/RielaCore/DistributedJobController.swift`, `Sources/RielaServer/DistributedWorkerLoop.swift` and `Tests/RielaCLITests/TaskCancellationIntegrationTests+Fixtures.swift`. They are advisory Step 6 audits, not downstream gate decisions. No repair or new behavioral test was indicated; prior source-matched build, store, decision, live, compatibility and selected-file SwiftLint receipts remain applicable to unchanged Swift bytes.
- A current 973/973 checksum check exited 0 (`tmp/work-runtime-p1-6c-final-review-20260924-c16e975-comm000006/source-match.log`), and manifest membership exactly matches all 973 current `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` files. Strict SwiftLint on the exact 23-path NUL manifest `changed-swift-files.nul` exited 0 (`swiftlint.log`, complete) with Xcode's toolchain. `git diff --check` and `git diff --cached --check` exited 0. Step 6 source/test implementation is ready for formal independent review. Astra exact combined-tree review, accepted README/design/progress status, exact-file commit and non-force push remain pending downstream; no Step 6 Git staging or publication occurred. P1-6d, P1-7a/b, parent P1 and all 19 broad follow-ups remain open.

## P1-6c formal test-integrity revision — 2026-09-24

- Mode `issue-resolution`; issue `Review and finalize P1-6c against final-source capable-host evidence` (no issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, plan `p1-dispatch`, `dependsOn: []`. Formal test-integrity step `step6-test-integrity-check-attempt-1-exec-12` returned `comm-000011`, `accepted: false`, one mid finding in `Tests/RielaCLITests/TaskCancellationIntegrationTests.swift:121`: existing signal tests called `TaskRunSignalState.request(2)` and did not exercise real EntryPoint SIGINT routing. This supersedes the advisory Step 6 integrity recommendation above. `/root` made the sole test edit; read-only Codex agents `signal_path` and `test_fixture` investigated the bounded fixture. Preimages, intended hunks and postimages are under `tmp/work-runtime-p1-6c-final-review-20260924-c16e975-comm000006/intents/edit-004/` through `edit-009/`.
- Added `testTaskRunSubprocessSIGINTCommitsAndAcknowledgesCancellation`: a real `riela task run` subprocess executes an isolated 20-second sleep workflow, receives SIGINT after `.nodeStarted`, exits within a bounded wait, and leaves one linked durable `cli-signal` cancel decision, exact failed/cancelled session, acknowledged cancellation, reconciled attempt, cancelled task and no lease. The fixture keeps its workflow, registry home and output under `tmp/`; failure teardown kills and reaps the owned process. Diagnostic runs `sigint-test-1.log` through `sigint-test-4.log` exited 1 before reservation on fixture registry/backend admission and were corrected. `sigint-test-5.log` passed 1/1, exit 0; a three-case run exposed a bounded startup timing failure (`sigint-local-selected.log`, exit 1, 2/3), so startup allowance was raised to 12 seconds. Final `sigint-local-selected-2.log` passed 3/3, exit 0, including the new real SIGINT case, in-memory live cancellation and preauthorization cancellation.
- Current manifest `tmp/work-runtime-p1-6c-final-review-20260924-c16e975-comm000006/final-source-after-sigint.sha256` has 973 entries, SHA-256 `d2b675f1bd3fded858997d8345bccaa70e6c19cae8d72b09f977422fb5259140`, and its full checksum log exits 0. Compared with the earlier host manifest `561db1140f6d0826250203a2746708a8f4a8b6b59265b57ac85a8ce97dea861f`, exactly `Tests/RielaCLITests/TaskCancellationIntegrationTests.swift` changed; no files were added or removed. Strict SwiftLint on all 23 selected Swift WIP paths exited 0 (`changed-swift-files-after-sigint.nul`, `swiftlint-after-sigint.log`). `git diff --check` and `git diff --cached --check` exited 0.
- The older host 17/17, 50/50, 55/55 passing logs and the 2,048/19 failed aggregate remain complete historical evidence, but their manifest does not certify this revised test tree. The 19 non-slice follow-up owners remain recorded above; the broad gate is still **FAILED**. Source-matched affected capable-host verification, renewed formal test-integrity and adversarial review as needed, Astra exact combined-tree acceptance, documentation finalization, exact-file commit and non-force push remain pending. P1-6d, P1-7a/b and parent P1 remain open. No staging, commit or push occurred in Step 6.

## P1-6c Step 6 host-readiness continuation — 2026-09-24

- Mode `issue-resolution`; issue `Review and finalize P1-6c against final-source capable-host evidence` (no issue URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, plan `p1-dispatch`, `dependsOn: []`, communication `comm-000013` (`implementation_continue: true`). `/root` retained serial documentation ownership and made no source or test edit. The per-edit preimage and intent are under `tmp/work-runtime-p1-6c-final-review-20260924-c16e975-comm000006/intents/edit-012/`.
- Current `final-source-after-sigint.sha256` remains SHA-256 `d2b675f1bd3fded858997d8345bccaa70e6c19cae8d72b09f977422fb5259140`; all 973 entries passed the before/after checksum commands (`continuation-source-check.log`, `continuation-source-check-after-probe.log`, exits 0). The current-source selected-host probe `swift test --disable-sandbox --skip-update --scratch-path tmp/work-runtime-p1/build/p1-dispatch --filter 'TaskCancellationIntegrationTests/testTaskBackedSelectedHostCancellationWaitsForWorkerStopProof'` reached terminal exit 1 after one test and one unexpected failure: `RielaLocalHTTPServer.swift:128` failed before worker registration with `Network.NWError error 1 - Operation not permitted`. Its complete foreground log is `tmp/work-runtime-p1-6c-final-review-20260924-c16e975-comm000006/continuation-host-probe.log`. This is a failed environment-limited check, not stop-proof evidence or a passing host gate.
- The listener denial prevents source-matched V11/selected-host acceptance here. The 2,048-test broad aggregate was not rerun in this listener-denied sandbox; its historical exit 1 and all 19 explicit non-slice follow-up owners remain open. `git diff --check` and `git diff --cached --check` exited 0. Resume on a listener-capable host using the exact current manifest, run affected focused checks and the required serial aggregate, preserve complete terminal logs, then renew formal test-integrity and affected review before Astra combined-tree disposition. No Step 6 commit, push or slice acceptance is claimed; P1-6d, P1-7a/b and parent P1 remain open.

## P1-6c post-SIGINT Step 6 material review finding — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c durable cancellation final review` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, branch/plan `feat/remaining-impl-plans` / `p1-dispatch`, `dependsOn: []`, checkpoint `2e7a264f1ec6604a72d1bbdfe80cab9f81ca6220`. `/root` retained serial edit ownership. Read-only Codex agents `integrity`, `adversarial` and `combined` inspected the current tree; their decisions are Step 6 advisory receipts, not formal downstream acceptance. Documentation preimages and intentions are under `tmp/work-runtime-p1-6c-final-review/step6-late-sigint/intents/`; no source or test bytes changed in this attempt.
- The independent integrity review accepted the 973/973 manifest check and exact tracked/untracked membership. Manifest SHA-256 is `d2b675f1bd3fded858997d8345bccaa70e6c19cae8d72b09f977422fb5259140`. Complete source-matched `tmp/work-runtime-p1-6c-final-host/focused.log` has SHA-256 `05bdb645a4b8c3fc95b1389eb5528ef87728cb33ae47417288a8bd6ec1d37dd1` and reports 99/99, zero failures, including real EntryPoint SIGINT and selected-host tests; host exit 0 is runtime-reported. Complete `tmp/work-runtime-p1-6c-final-host/aggregate.log` has SHA-256 `49d18fb95b91f96e889f97089172b1a25e66e83cb47654b6fe9c6f647b8ba115` and reports 2,049 tests, 19 assertion failures (7 unexpected), host exit 1. All 19 test/source-line identities match inventory IDs 1–8 and 14–24 exactly: 1–7 CLI doctor/backend capability, 8 P1-7b examples, 14–21 workflow-command/host readiness, 22 workflow runner admission, 23–24 temporary workflow/host requirements. The broad gate remains **FAILED** and all 19 non-slice follow-ups remain owned.
- Adversarial review rejected slice acceptance with one high material finding in `Sources/RielaCLI/TaskRunCancellation.swift:84-113`, `Sources/RielaWork/WorkStore+Decisions.swift:73-116`, `Sources/RielaWork/WorkStore+Reservation.swift:425-460` and `Sources/RielaCLI/TaskDispatch.swift:355-418`. A SIGINT or external cancel can be committed after an ordinary terminal session is saved while the attempt is still running, or immediately before terminal save without observation. The resulting pending cancellation cannot acknowledge because the exact durable session is completed or failed for a different reason; reconciliation errors and leaves the attempt fenced. Current real SIGINT coverage signals during a sleeping node and does not test this boundary. The read-only combined-tree inspection found no separate code defect but cannot accept this exact tree after the adversarial finding; final documentation and formal Astra review remain pending.
- Repair must arbitrate the cancellation request and exact terminal session in their shared SQLite transaction: terminal-first must avoid inserting an impossible pending request; request-first must persist a valid cancelled terminal only after owned execution has stopped, while preserving strict exact-session acknowledgment and selected-host proof. Add deterministic regressions for both orderings and external cancel, rerun affected focused/host checks and generate a fresh source manifest before renewed integrity, adversarial and Astra reviews. No source repair, staging, commit or push occurred in this attempt. P1-6c is **incomplete**; P1-6d, P1-7a/b and parent P1 remain open. The existing 99/99 and 2,049/19 logs certify only their unchanged source/test bytes, not a future repair.

## P1-6c late-cancellation arbitration Step 6 — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, branch/plan `feat/remaining-impl-plans` / `p1-dispatch`, `dependsOn: []`, HEAD `70ed3082c16ca933aff6901329ac4b82c17684ee`. `/root` made serial edits; read-only Codex agents `store_audit`, `cli_audit`, `evidence_audit`, `integrity_review` and `adversarial_review` investigated and reviewed. Per-edit preimages and intentions are in `tmp/work-runtime-p1-6c-late-cancel-20260924-session-1/edit-intents/`.
- Shared SQLite write transactions now arbitrate both cancellation insertion paths against exact reserved terminal persistence and both snapshot save overloads against pending cancellation. Request-first waits for owned execution and selected-host stop proof, then persists the cancelled terminal before exact acknowledgment; terminal-first rejects a new request, while accepted-decision replay and terminal monotonicity remain. Deterministic production-boundary and independent-connection tests cover SIGINT/external ordering, ordinary success/failure, rollback, stale saves, replay and real EntryPoint SIGINT.
- Current `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` manifest is `tmp/work-runtime-p1-6c-late-cancel-20260924-session-1/final-source-3.sha256`, 973 entries, SHA-256 `18dae6da5d5030168d18eacf6d5ba556b3c4ac677c7fcb0f897ecf8ae40f13bf`; checksum exit 0. Plan-local-cache build exited 0 (`build-final-3.log`); strict SwiftLint on 29 selected changed Swift files exited 0 (`changed-swift-files.nul`, `lint-4.log`); `git diff --check` exited 0. The exact default-cache build failed before compilation on denied Clang module-cache access (`build-final-exact.log`, exit 1); `build-final-3.log` is the completed fallback.
- Final-source local focused selection passed 62/62, zero failures (`focused-final-3.log`, exit 0). Broader affected selection failed 103 tests / 47 assertions (`affected-final-3.log`, exit 1), including listener permission denial and inaccessible mutable registry fixtures. Compatibility selection failed 28 tests / 4 assertions (`compat-final-3.log`, exit 1) on fixture access. Serial sandbox aggregate completed 2,057 tests / 524 assertion failures (`aggregate-final-3.log`, exit 1); listener and fixture failures are not host acceptance, and full failure attribution remains open. The broad gate is **FAILED**. The historical capable-host 99/99 and 2,049/19 receipts predate this repair; the 19 non-slice assertions retain their explicit owners.
- Independent `integrity_review` accepted test design with no high/mid finding after the corrected ordinary-failure regression passed (`failure-boundary-3.log`, 1/1). Independent `adversarial_review` found no high/mid P1-6c source defect on the manifest above. These are advisory Step 6 decisions. Source-matched listener-capable focused and serial aggregate checks, complete current broad-failure classification, formal review/Astra acceptance, final documentation and exact-file commit/non-force push remain open. P1-6c, P1-6d, P1-7a/b and parent P1 are not marked complete.

## P1-6c Step 6 continuation: aggregate and host audit — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, continuation `comm-000010`. `/root` retained serial edit ownership. Read-only Codex agents `aggregate_classification` and `host_capability` audited the outstanding evidence. The 973-entry `final-source-3.sha256` recheck exited 0 (`continuation-2-source-check.log`), so prior final-source build, focused, lint and review receipts still describe the current source/test bytes.
- The read-only aggregate audit classified 393 of 524 failed assertions across 261 failed cases: mutable-registry refusal/cascades 182, Git repository preflight 166, listener denial 33, Doctor JSON date mismatch 7, Unix-socket I/O 3, and unresolved addon requirement 2. The remaining 131 assertions in 63 cases cannot be assigned a direct cause from this sandbox log. The 22 directly P1-6c-related assertions show listener denial or registry refusal/cascade, with no direct cancellation semantic assertion identified. All 19 historical non-slice inventory tests recur; 15 retain exact source lines and four WorkflowCommandRuntimeCapabilityDiagnostics lines shifted. This does not convert the 2,057/524 aggregate to passing evidence; the broad gate remains **FAILED**.
- The host audit found no listener-capable runner exposed in this Step 6 sandbox. Historical operator-host receipts use an older manifest and cannot certify `final-source-3.sha256`. Resume on a capable host with manifest checks before and after foreground focused and serial aggregate commands, complete logs/exits/counts and per-failure classification. The source tree has no new Swift edits in this continuation. Formal Astra acceptance, final documentation, exact-file commit and non-force push remain pending; P1-6c, P1-6d, P1-7a/b and parent P1 remain open.

## P1-6c post-helper Step 6 selected-host stop-proof repair — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub URL or number supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, plan/branch `p1-dispatch` / `feat/remaining-impl-plans`, `dependsOn: []`, checkpoint HEAD `ce703014c3d78113897d640f85825f5115ae0376`. `/root` was the sole serial editor. Read-only Codex agents `evidence`, `integrity` and `adversarial` inspected the same source; per-edit preimages and intentions are under `tmp/work-runtime-p1-6c-after-helper-review-20260924-9fbcc7126181/intents/`.
- The operator-host manifest and complete logs matched recorded SHA-256 digests: 973/973 source entries, focused 136/136 exit 0, and serial aggregate 2,057 tests / 19 assertions (seven unexpected) exit 1. All 19 assertion test/source-line identities exactly match inventory IDs 1–8 and 14–24 in `tmp/work-runtime-p1-6c-host-classification-20260924-076f8c6b4184/reviews/evidence/failures.json`: CLI doctor/backend capability 7, P1-7b examples 1, workflow-command/host readiness 8, runner admission 1, temporary workflow/host requirements 2. The broad gate remains **FAILED**; these pre-repair host logs are historical for the new source bytes.
- Independent adversarial review found a mid timing window: a selected-host cancellation could commit between the runner's pending-request read and joined persistence, allowing cancelled canonical persistence without prior stop proof. The first guard kept the fence but could leave the owner exited with pending cancellation. The bounded repair adds a typed proof-required result, keeps the canonical snapshot untouched until proof, then proves exact selected-host stop and retries the same reserved request once. A deterministic separate-connection store test covers the no-proof transaction; a selected-host integration test injects a request after the runner's observation and checks retry, exact cancelled snapshot and acknowledgment. No unrelated store/schema or package change was made. Renewed read-only adversarial source review accepted the repair on the current manifest with no material source finding; this is advisory, not the formal workflow gate.
- Current `Sources/`, `Tests/`, `Package.swift` and `Package.resolved` manifest is `tmp/work-runtime-p1-6c-after-helper-review-20260924-9fbcc7126181/final-source-after-retry.sha256`, 973 entries, SHA-256 `dc6cec7251a8607b20b65ccaddb775606352b7556556209e6985bf1328409cd6`; exact membership and checksum commands exited 0. With plan-local module caches, selected local tests passed 33/33, zero failures (`affected-local-test-final-2.log`, exit 0); final-source build passed (`build-final.log`, exit 0); strict SwiftLint on the seven exact edited Swift paths passed (`changed-swift-files-final.nul`, `swiftlint-final-3.log`, exit 0). The initial exact-scratch test failed before compilation on denied default Clang cache; its plan-local-cache retry passed. The new selected-host test compiled but its one-test sandbox probe failed before worker registration at `RielaLocalHTTPServer.swift:128` with `Network.NWError error 1 - Operation not permitted` (`late-selected-host-probe-2.log`, exit 1, one unexpected failure). This is not a behavioral pass.
- Step 6 remains **incomplete** until a listener-capable host checks the new manifest before and after the affected focused run and serial aggregate, preserves complete logs and terminal exits, and independently reviews any failures and the new selected-host boundary test. Renew formal test-integrity/adversarial and Astra exact combined-tree review after source-matched evidence. README/design final status, exact-file commit and non-force push remain downstream of acceptance. P1-6d, P1-7a/b, parent P1 and all 19 broad follow-ups remain open; no Step 6 staging, commit or push occurred.

## P1-6c Step 6 continuation: current-source host handoff — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub URL or number); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, communication `comm-000010` (`implementation_continue: true`), step/plan `step6-implement` / `p1-dispatch`, branch HEAD `ce703014c3d78113897d640f85825f5115ae0376`. `/root` made no Swift source or test edit in this continuation. Documentation preimage and intent are under `tmp/work-runtime-p1-6c-after-helper-review-20260924-9fbcc7126181/intents/edit-018/`.
- The current `final-source-after-retry.sha256` manifest still verifies 973/973 (`continuation-source-check.log`, exit 0). No newer source-matched capable-host receipt was present under `tmp/`. The earlier host focused 136/136 exit 0 and serial aggregate 2,057/19 exit 1 remain complete, digest-verified historical evidence for the pre-repair bytes; the broad gate is **FAILED**. The new selected-host test's complete `late-selected-host-probe-2.log` remains exit 1 at listener creation before worker registration, not behavioral acceptance. `git diff --check` exited 0; the staged state remains empty.
- Resume criterion: on a listener-capable host, verify SHA-256 and membership of the exact 973-entry `final-source-after-retry.sha256` manifest before and after foreground execution of the new selected-host test, affected focused suite and serial affected aggregate. Preserve each complete log and terminal exit, then classify every aggregate assertion against IDs 1–8 and 14–24 and renew formal test-integrity, adversarial and Astra exact combined-tree decisions. No sandbox aggregate rerun, staging, commit, push or slice acceptance occurred. P1-6d, P1-7a/b, parent P1 and all 19 broad follow-ups remain open.

## P1-6c selected-host regression Step 6 repair — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `nested-v1-f637160bee4a0e57484a92a8400c09a6a58ec7ccbb82ac72bb5ebee0ca2d68ae`, step `step6-implement`, plan/branch `p1-dispatch` / `feat/remaining-impl-plans`, `dependsOn: []`, checkpoint HEAD `2dce35619b581b0a1d78c3c509e7c60b1f88f12f`. `/root` owned serial edits. Read-only Codex agents `trace_runner`, `trace_store`, `integrity_audit` and `adversarial_audit` investigated and reviewed; their advisory acceptance is not formal workflow review. Per-edit preimages and intentions are under `tmp/work-runtime-p1-6c-repair-0a3605e9eafe/p1-dispatch/attempt-001/intents/` and `attempt-002/intents/`. All preserved tracked/untracked WIP, checkpoint history and staged state remain intact; no staging, commit or push occurred in Step 6.
- The capable-host diagnostic identified `FailClosedSQLiteWorkflowRuntimeStore.markSessionFailed` as the first canonical `adapterFailure` writer after the one-shot throwing test hook had been consumed; live CLI persistence also attempted that terminal snapshot. The worker job itself failed because `/bin/true` is absent on this macOS host (exit 127). The bounded repair uses shell builtin `true`, a test-only nonthrowing canonical terminal deferral, canonical-first live/final CLI projection checks only while that deferral is set, and an explicit one-count proof-required retry probe. The selected regression retains nonterminal/no-request before injection, separate-connection exact cancellation, cancelled failure kind, acknowledged record, reconciled attempt, cancelled task and succeeded remote job assertions. Production unset-hook behavior and shared SQLite request/terminal arbitration remain unchanged. No store schema or transaction change was needed. Diagnostic and repair logs are in `attempt-001/`; an overbroad projection check was corrected after required-loop-gate regressions appeared, with all failed runs retained.
- Final source manifest `tmp/work-runtime-p1-6c-repair-0a3605e9eafe/p1-dispatch/attempt-003/final-source.sha256` has 973 entries, SHA-256 `7883cb40b4c4678588a79071dada6044bec3c38ad2c7cd099183a630eb955ddb`. Before/after checks for each command matched all entries. Complete `attempt-003/` logs and `verification.json` record build exit 0; selected-host 1/1, store/decisions 56/56, focused 104/104, compatibility 28/28, each exit 0 and zero failures; selected-file strict SwiftLint and both diff checks exit 0. The serial aggregate exited 1 after 2,059 tests and 19 assertion failures. `aggregate-classification.json` confirms all 19 test identities exactly match historical non-slice inventory IDs 1–8 and 14–24. The broad gate remains **FAILED**; those owners remain separate from P1-6c. An attempt-002 aggregate had 20 failures including an intermittent live-progress timing test with an unchanged production path; its isolated rechecks failed twice and passed once. The final source aggregate returned to the exact 19 historical identities.
- Step 6 read-only integrity and single adversarial Codex audits recommended acceptance with no demonstrated high/mid P1-6c finding. Formal test-integrity, single adversarial and Astra exact combined-tree review decisions remain pending. Final README/design status, exact-file commit and non-force push belong after formal acceptance. P1-6d, P1-7a/b and parent P1 remain open; the failed broad gate is not reclassified as passing.

## P1-6c Step 8 documentation refresh — 2026-09-24

- Mode `issue-resolution`; issue `Work Runtime P1-6c` (no GitHub issue supplied); workflow `codex-design-and-implement-review-loop-session-1`, plan `p1-dispatch`. The serial implementation owner and formal independent test-integrity (`comm-000011`), single adversarial (`comm-000012`) and Astra exact combined-tree (`integration-review`) reviewers accepted the repaired slice with no material P1-6c finding. This Step 8 note supersedes the pending-review statement in the preceding Step 6 record.
- Current-source evidence is `tmp/work-runtime-p1-6c-repair-0a3605e9eafe/p1-dispatch/attempt-003/verification.json` and its complete logs. The selected-host regression passed 1/1, store/decisions 56/56, focused 104/104, compatibility 28/28, and build and changed-file strict SwiftLint exited 0. The source-matched serial aggregate ran 2,059 tests, exited 1 and retained 19 historical non-slice assertion failures classified in `attempt-003/aggregate-classification.json`. The broad gate is **FAILED**.
- Step 7b skipped browser E2E after `rg -n '"test:e2e"|playwright|headless' web/package.json web/playwright.config.ts`, `git status --short -- web`, `git diff --name-only -- web`, and `git diff --cached --name-only -- web` showed no changed `web/` file. `README.md` and `design-docs/specs/design-work-runtime-consolidation.md` now describe slice acceptance and limits; `.codex/skills/riela-impl-workflow/SKILL.md` was reviewed and its existing documentation-refresh contract needs no behavioral change. P1-6d, P1-7a/b, parent P1, exact-file commit and non-force push remain downstream.

## P1-6d Step 6 implementation attempt 1 — 2026-09-24

- Mode `issue-resolution`; issue `comm-000001` (no GitHub URL/number or codex-agent reference); workflow `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`, step `step6-implement`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, checkpoint `79eea8114b7d407e3fb7ed18faf5de5d86bce1ab`, `dependsOn: []`. `/root` owned serial edits; read-only Codex agents `store_audit`, `runner_audit`, and `test_audit` inspected seams without writes. D0 baseline, per-edit preimages/intent and complete logs are under `tmp/work-runtime-p1/p1-dispatch/p1-6d/attempt-1/`.
- D1 partial: `.director` reservations now require a reconciled same-task non-director judged attempt, configured child workflow, current version, no intervening attempt and normal budget/live fences. The judged ID persists on the ordinary child attempt alongside its own durable task/attempt/session identity in the reservation transaction. A fresh/reopened-store rollback and duplicate test passes. Recovery of an existing pre-launch child and exact terminal cost accounting remain to be proven.
- D2 partial: shared agent decision application requires the exact successful reconciled linked child session and judged work, and reconstructs accept from the judged attempt's durable completion. The typed validator can emit accept; unrelated latest-attempt paths remain unchanged. Real-store positive accept, child-only-success rejection, wrong session, replay and preserved judged evidence tests pass. Missing/foreign linkage, newer work, human acceptance, stale version, gates/findings and budget decision matrix remain to be completed.
- Evidence: initial source manifest SHA-256 `7883cb40b4c4678588a79071dada6044bec3c38ad2c7cd099183a630eb955ddb` (973 tracked entries); current manifest `source-after-tracked.sha256` SHA-256 `f021b7b75a7413d57674f7c02fcd2dd2acd656ce4b354938b6e001706b1bac6a` (974 entries including new test file). Repository lint baseline exit 0; final-source `build-final.log` SHA-256 `991795d3fafd2f2d5790c4d4fd9c328f1e517b938d059e2a8880871f4dd1ced2`, exit 0. First focused build linked stale incremental CLI test objects, exit 1; clean exited 0; next 26-test run exposed the expected old unlinked agent fixture (1 failure), then 28-test run exposed a new subsecond timestamp assertion (1 failure). Both were corrected. Current `store-final.log` SHA-256 `d41da70bfce4318b80281e79baa913aca1d77341ff2e616e11cb780312a66e3b` records 58/58, zero failures, exit 0. Exact eight-file `changed-swift-files.nul` strict SwiftLint `swiftlint-2.log` exit 0. `git diff --check` and `git diff --cached --check` exited 0. Earlier failures remain in their complete logs.
- D3-D5 and P1-6d acceptance are **incomplete**: ordinary child dispatch, actual judged TaskView and `hostCapabilityContext` delivery, durable escalation, full real-runner matrix, source-matched V0-V4/V11 and serial broad gate, complete counts/hashes, independent test-integrity/adversarial/Astra combined-tree decisions and final docs remain open. The P1-6c 2,059-test aggregate remains **FAILED** with 19 classified non-slice assertions and cannot certify this source. P1-7b, P1-7a, parent P1 and broad follow-ups stay open. No staging, commit or push occurred in this Step 6 attempt.

## P1-6d Step 6 implementation continuation 2 — 2026-09-24

- Mode `issue-resolution`; issue `comm-000001` (no GitHub URL/number); workflow `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`, communication `comm-000012` (`implementation_continue: true`), plan `p1-dispatch`, branch `feat/remaining-impl-plans`. `/root` made serial source/test/docs edits; read-only Codex agents `child_seam`, `capability_seam`, `director_review`, `recovery_strategy` and `test_integrity_review` investigated or reviewed. Per-edit preimages/intents and all foreground logs are under `tmp/work-runtime-p1/p1-dispatch/p1-6d/attempt-2/`. No staging, commit or push occurred.
- D3 partial: configured guard/failure escalation now launches one linked `.director` child through the ordinary resolver, placement, reservation and runner. The judged TaskView and finite selected-host `hostCapabilityContext` reach actual child-node `mergedVariables`; the real-runner test asserts the original judged outcome, remaining attempt budget, local host and `.codexAgent` backend. Invalid/forbidden/failed/budget-denied child and pre-admission setup errors durably request human action without a recursive child. Valid reject reports terminal failure and nonzero CLI exit. D2 real-store tests now reject human-required, missing verification, missing linkage, newer work, stale version, missing/foreign causal evidence and child-only success. The older causality fixture was narrowed to human/policy because its live unlinked attempt is no longer a valid agent producer; linked-agent causal rejection is tested separately.
- Current Swift-source focused selections all exited 0 with zero failures: V1 47/47, V2/store 80/80, CLI 35/35, reservation/budget 30/30, decisions/guards 80/80, capability 55/55, V11/distributed 54/54. `build-post-review.log` and exact 15-file strict `swiftlint-final.log` exited 0. Complete logs and SHA-256 digests are in `attempt-2/verification-summary.json`; the pre-documentation 2,750-file source manifest SHA-256 is `40239f50ecc6d9e48ccad068d14af1122d0594867cbd179f306fa79bbd038b2c`. An earlier focused selection failed 10 assertions because an unlinked live-agent causality fixture expected a later causal error; the fixture was corrected and the final selection passed 80/80. The serial broad run on earlier continuation source was interrupted after a long parity test and exited 1; it is **not** a complete passing gate or final-source evidence. Historical P1-6c 2,059/19 remains FAILED and cannot certify P1-6d.
- P1-6d remains **incomplete**. Read-only `director_review` found a material interruption/restart defect: after child reservation but before a terminal session snapshot, `resumeLinkedDirectorIfPresent` throws and cannot recover the same one-use launch token, leaving the task running. `recovery_strategy` recommended a session-lock-guarded same-attempt token rotation only for a `.reserved`/`.created` child with no executions, with uncertain authorized/running work fenced. That repair, reopened/restart/concurrency and exact cost replay tests, full real-runner matrix including nonempty findings/guard inputs, a complete final-source serial broad run and failure classification, formal independent test-integrity/adversarial/Astra combined-tree acceptance, and D5 docs/publication remain open. P1-7b, P1-7a, parent P1 and broad follow-ups stay open.
- Read-only `test_integrity_review` found high gaps in selected-host execution observation and exact child cost/reopened ordinary-dispatch replay. Mid gaps remain in nonempty TaskView guard/finding/completion assertions, an isolated wrong-session negative with durably satisfied work, and persisted escalation/repeat-dispatch assertions. These are open P1-6d D4 criteria, not accepted review decisions. `director_review` also found setup-failure escalation and terminal reject rendering defects; both were repaired and the 12/12 current `TaskRuntimeExampleTests` suite passed. No formal Astra combined-tree decision exists.

## P1-6d Step 6 implementation continuation 3 — 2026-09-24

- Mode `issue-resolution`; issue `comm-000001`; workflow `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`, communication `comm-000014`, step `step6-implement`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, checkpoint `79eea8114b7d407e3fb7ed18faf5de5d86bce1ab`, `dependsOn: []`. Codex `/root` owned serial edits; read-only Codex agents `lock_recovery_audit`, `host_test_audit`, `cost_test_audit`, `recovery_review3`, `host_feasibility`, `final_integrity3`, `final_adversarial3` and `astra_combined3` investigated and reviewed. Per-edit preimages and intended hunks are under `tmp/work-runtime-p1/p1-dispatch/p1-6d/attempt-3/`; no staging, commit, push, reset or worktree operation occurred.
- D1–D4 repairs: a session-lock-held `.reserved/.created` child rotates its one-use launch token and resumes the same linked task/attempt/session after reopen; an uncertain nonterminal authorized child durably waits for a human. The ordinary child reconciles exact usage from persisted backend events even without loop metadata; matching canonical store reconciliation/cancellation prevents double charge. Direct agent rerun/recover admission and CLI recommendation both enforce cumulative durable terminal-session wall-clock budget. Cancellation acknowledgment, newer human state, invalid rerun/recovery target, full nonempty judged TaskView, exact selected authenticated worker delivery, wrong child session, human acceptance, missing gates/blocking findings, failed/forbidden/budget-blocked child, rollback, replay and no recursion have focused store/runner assertions. `Sources/RielaWork/WorkStore+Director.swift` owns the extracted prelaunch recovery transaction; `WorkStore+Reservation.swift` is 970 lines.
- Current Swift source identity: `tmp/work-runtime-p1/p1-dispatch/p1-6d/attempt-3/source-final9.sha256`, 964 Swift files, manifest SHA-256 `c225653a31733d9d4e851c76ad6773f866d01a94c35292e2c21bc1816463b4a4`. Source-matched `build-final9.log` exited 0, SHA-256 `c0c8f404ef574729a3683c90b3162cb3f6ed73c51585801f3a29f43e6f758c42`. Final seven focused selections `final7-focused-{1..7}.log` exited 0 with zero failures: 55/55, 81/81, 43/43, 30/30, 80/80, 55/55 and 55/55; exact filters and hashes are in `attempt-3/final7-focused-exits.txt` and the complete logs. Exact 18-file `changed-swift-files-final2.nul` strict SwiftLint `swiftlint-final9.log` exited 0 with empty diagnostics. `git diff --check` and `git diff --cached --check` exited 0; nothing is staged.
- Complete serial broad `broad-final4.log` exited 1: 2,666 tests, 19 failures, SHA-256 `b5f07276c78ac3481e29bf353074e6f12fde0cb29c068011a775660bcfa5baa6`. `broad4-classification.json` records exact equality with 19 historical non-slice test identities, no unexpected/missing failures, and source manifest SHA-256 `c225653a31733d9d4e851c76ad6773f866d01a94c35292e2c21bc1816463b4a4`. The broad gate remains **FAILED** and its failures remain open; earlier incomplete broad logs do not certify this slice. The prior intermittent live-progress timing limitation remains recorded.
- Review decisions: read-only Codex `final_integrity3` accepted after the shared-store wall-clock bypass was repaired and a positive/negative direct-store test added; `final_adversarial3` accepted after the actual remote adapter variables were asserted; `astra_combined3` accepted after non-loop token accounting and child wall-clock budget repairs. No remaining material high/mid P1-6d finding was reported by these reviewers. Formal workflow review/acceptance and D5 shared documentation, exact-file commit and non-force push remain downstream. P1-7b, P1-7a, parent P1 and the broad failure follow-up remain open.

## P1-6d Step 6 retained implementation assessment — 2026-09-25

- Mode `issue-resolution`; issue `comm-000001`/`comm-000002`, accepted design `comm-000004`, plan `comm-000006`, workflow `nested-v1-fb6c02d68fb5fbe99b59df51970064b8218e39fc08f2cfc322431e14579d0893`, step `step6-implement`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, HEAD `e33d9f103936c8acd8fe44c067f03678ab9a9a3b`. Codex `/root` assessed retained D0–D4; read-only Codex agents `evidence_audit` and `contract_audit` found no material implementation gap. This assessment made no Swift source or test edit. Its preimage, edit intent and check receipts are under `tmp/work-runtime-p1-6d-continuation-20260925-79eea81-comm000006/step6-current/`.
- The retained `source-final9.sha256` matches all 964 current Swift files. The source-matched `build-final9.log` exited 0; seven `final7-focused-{1..7}.log` selections passed 55/55, 81/81, 43/43, 30/30, 80/80, 55/55 and 55/55 with zero failures; exact 18-file strict SwiftLint `swiftlint-final9.log` exited 0. No implementation-phase D0–D4 criterion or behavioral verification remains open. The complete serial `broad-final4.log` remains **FAILED**: 2,666 tests, 19 assertions. All 19 assertion identities match the historical non-slice inventory, with no additional or missing identity; independent formal review must decide slice-only acceptance.
- Formal test-integrity, single adversarial and Astra combined-tree decisions, then D5 shared documentation/status updates and exact-file commit/non-force push, belong to later workflow steps and are not claimed here. P1-7b, P1-7a, parent P1, the 19 broad failures and the prior intermittent live-progress timing limitation remain open. No staging, commit, push, reset or worktree operation occurred in this step.

## P1-6d Step 8 documentation refresh — 2026-09-25

- Mode `issue-resolution`; issue references `comm-000001`, `comm-000002`, accepted design `comm-000004` and plan `comm-000006`; workflow `codex-design-and-implement-review-loop-session-1`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`. Formal independent test-integrity, single adversarial (`comm-000013`) and Codex `/root` Astra combined-tree integration review (`comm-000018`) accepted P1-6d with no material finding. Step 7b `comm-000019` skipped browser E2E because `git status --short -- web` and `git diff --name-only` showed no web change, although `web/package.json` declares `test:e2e: playwright test`.
- Source-matched evidence: `shasum -a 256 -c tmp/work-runtime-p1/p1-dispatch/p1-6d/attempt-3/source-final9.sha256` exited 0 for 964 Swift files. `/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift build` passed; the seven `swift test --scratch-path tmp/work-runtime-p1/build/p1-dispatch --filter <selection>` commands recorded in `attempt-3/final7-focused-exits.txt` passed 399/399. Exact changed-file strict SwiftLint passed (`attempt-3/swiftlint-final9.log`); `git diff --check` and `git diff --cached --check` passed. Complete logs remain under `tmp/work-runtime-p1/p1-dispatch/p1-6d/attempt-3/`.
- The complete `swift test --scratch-path tmp/work-runtime-p1/build/p1-dispatch` log is `attempt-3/broad-final4.log`, exit 1, 2,666 tests and 19 assertions. `attempt-3/broad4-classification.json` independently classifies all 19 as historical non-slice identities with none unexpected. The broad gate remains **FAILED**. `README.md`, `design-docs/specs/design-work-runtime-consolidation.md`, the active plan, `impl-plans/README.md`, and `impl-plans/REMAINING-WORK-HANDOVER.md` were aligned; `.codex/skills/riela-impl-workflow/SKILL.md` was reviewed and its existing Step 8 contract remains applicable. Exact-file commit and non-force push remain pending. P1-7b, P1-7a, parent P1, broad-failure follow-ups and the intermittent live-progress timing limitation remain open.

## P1-7b Step 6 implementation attempt 1 — 2026-09-25

- Mode `issue-resolution`; issue `comm-000002` “Implement Work Runtime P1-7b task-backed replacement examples”; step `step6-implement`, workflow `nested-v1-80c1d11cf24b10bfb1dcf403828b9c63b3ef079f5c43898e5fde4597322c311a`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, checkpoint `380b64e2c48f5fca3ba909da0523ab1a19f929bb`, `dependsOn: []`. Codex `/root` made serial edits; read-only Codex `examples_audit`, `tests_audit`, and `plan_audit` supplied findings. Preimages and intended hunks are under `tmp/work-runtime-p1-7b-20260925-comm000008-59f45c1a/plans/p1-dispatch/attempt-1/intents/`. No staging, commit, push, reset, or worktree operation occurred.
- B0 inventory found both bundles complete, with mock keys matching node IDs. Baseline `TaskRuntimeExampleTests` passed 19/19. B1 kept both executable bundle shapes and mock data; README/expected-result text now distinguishes standalone completion from task acceptance and correctly names the gate-visit guard fixture. B2 added a real rejected-gate/recovery test and strengthened acceptance response/causality, guard replay, capacity store/lease absence, reopened seven-token director accounting, and invalid-output replay assertions. Requirement mapping: acceptance `testRepairExampleRunsTheRequiredGateForARealTaskAttempt`; gate recovery `testRejectedGateRecoveryConsumesPendingRequestAndAcceptsSecondAttempt`; guard stop `testRepairExampleGuardStopsAfterPersistingViolation`; capacity `testRepairExampleCapacityWaitDoesNotReserveAnAttempt`; allowed director `testConfiguredDirectorChildCanAcceptDurablySatisfiedJudgedWork` plus failed-work test; accounting `testReservedDirectorChildResumesSameSessionAfterReopen`; invalid output `testForbiddenOutputAndFailedDirectorChildEscalateWithoutRecursion`.
- **P1-7b remains incomplete.** The recovery test's first rejected required-gate dispatch exits 1 with `terminalSnapshotConflict` instead of the expected successful task-command handoff. Its later ledger assertions show the persisted recovery request, distinct consumed second attempt/session, accepted gate, succeeded task, and replay stability, but a failing CLI assertion is a material P1-7b defect. `WorkflowRunCommand.finalizeRun` changes a completed runner session to failed after required-gate projection, while `SQLiteWorkflowRuntimePersistenceStore` rejects a conflicting terminal rewrite. Production source is outside the accepted write set; the plan requires a bounded amendment and review before any runtime repair. No assertion was weakened and no production edit was made.
- Complete logs under this attempt's `logs/`: final-source build exits 0 (`build-final.log`); final V8 validate/mock commands for both bundles exit 0, with `mock-business-assertions.log` confirming two repair nodes, accepted `verification` with `acceptance.met: true`, and one director `kind: accept`. Final affected store/decision selection passes 115/115 and V11 selected-host selection passes 55/55. Final before-removal V1 selection **fails** 56 tests/1 failure, solely the new recovery assertion; final example selection **fails** 20 tests/1 failure. Exact changed-file strict SwiftLint exits 0 after a responsibility-based test extension, and repository SwiftLint exits 0 with warnings in unchanged files. An intermediate compile/lint failure and their corrections are retained in numbered logs.
- The serial broad `broad.log` exits 1: 2,667 tests, 87 skipped, 582 failures (538 unexpected). It is **FAILED**, not a baseline pass. `broad-classification.json` retains 82 directly reported assertion lines: 11 match earlier non-slice identities, at least 52 cite disk exhaustion, and 19 other non-slice lines need review; the aggregate count also contains failures not individually represented by those lines. The volume reported `No space left on device` during the run; this receipt cannot classify every failure or certify the P1-7b slice. Passing V1 and complete broad classification remain open.
- Final source membership/checksum covers 991 files and passes (`source-final-files.txt`, `source-final.sha256`, `logs/source-check.log`; manifest SHA-256 `f33058f3c8efd0597806fe85f88ed9f24b2b37897c922c1acb3da7414a2e0032`). B0/B1 are complete; B2 and B3 remain open pending the bounded runner repair, source-matched passing recovery/V1 and a usable broad classification. Formal test-integrity, single adversarial and Astra combined-tree review, B4 shared documentation, exact-file commit and non-force push are downstream gates and are not claimed. P1-7a, parent P1, legacy examples, and unrelated broad follow-ups stay open.

## P1-7b Step 6 continuation 1 — 2026-09-25

- Mode `issue-resolution`, issue `comm-000002`, workflow `nested-v1-80c1d11cf24b10bfb1dcf403828b9c63b3ef079f5c43898e5fde4597322c311a`, communication `comm-000013` (`implementation_continue: true`), plan `p1-dispatch`, branch `feat/remaining-impl-plans`. Codex `/root` owns the single test edit; read-only Codex `runner_fix_audit`, `plan_amendment_review`, and `broad_gate_audit` investigated the failure and scope. The new assertion in `TaskRuntimeExampleTests.swift` confirms that the first rejected-gate runner session is durably `.completed`, which is the prerequisite for deterministic recovery. Its preimage/intent is under `tmp/work-runtime-p1-7b-20260925-comm000008-59f45c1a/plans/p1-dispatch/attempt-2/intents/`.
- The exact standalone rejected-required-gate regression passes 1/1 (`attempt-2/logs/standalone-rejected-gate.log`); the exact task-backed recovery test still fails 1/1 at its first CLI exit assertion with `terminalSnapshotConflict` (`attempt-2/logs/task-recovery-current.log`). The remaining recovery ledger assertions pass. The defect is specific to task-backed finalization: `WorkflowRunCommand.finalizeRun` converts a completed runner session to failed after its canonical terminal write, while `SQLiteWorkflowRuntimePersistenceStore.validateTaskTerminalWrite` correctly refuses a completed-to-failed rewrite. Relaxing the store guard would undermine terminal immutability and would make `DeterministicDirector` reject recovery from a failed session.
- `attempt-2/bounded-amendment-request.json` records the exact proposed owner/path `Sources/RielaCLI/WorkflowRunCommand.swift`, a task-reservation-only finalization correction, preserved standalone behavior, focused regressions, and no SQLite guard change. Read-only `plan_amendment_review` confirms the current accepted writePaths do **not** authorize production editing. This proposal is not an accepted workflow amendment; B2/B3 and passing V1 remain open until formal plan amendment/review admits the path and the fix passes verification. No production source, legacy examples, or Git state changed in this continuation.
- `broad_gate_audit` found no high-yield task-owned cleanup: attempt-1 evidence is about 2.7 MiB, the shared build root is about 2.1 GiB, and only about 5.1 GiB was available after the earlier ENOSPC run. No shared cache or other plan's scratch was deleted. The broad gate remains **FAILED** with incomplete classification; rerun after sufficient capacity is available. Formal independent reviews, B4 shared docs, commit and push remain downstream.

### Continuation 1 follow-up evidence

- The serial broad command completed without disk exhaustion on the pre-last-fixture source: 2,667 tests, 2 skipped, 23 assertion failures (seven unexpected), exit 1 (`attempt-2/logs/broad-current.log`). `attempt-2/broad-current-classification.json` matches all 19 prior P1-6d assertion file/line/test identities and identifies four added assertions in the new P1-7b recovery test. This is a **FAILED** gate, not acceptance of the historical failures. The later fixture edit invalidates it as final-source verification.
- The recovery fixture no longer adds an explicit high blocking finding in its rejected mock response; it still rejects the required gate. The current-source targeted run remains **FAILED** at the first CLI status assertion, 1 test/1 failure (`attempt-2/logs/task-recovery-no-finding.log`, repeated in `task-recovery-repeat-1.log`); later recovery assertions pass in these focused runs. No store status was manually pre-seeded. The task finalization defect and formal production-source plan amendment/review remain open. Passing current-source V1 and a source-matched broad run remain B2/B3 gaps.
- The final-source V1 selection also **FAILED**, 56 tests/4 assertions (`attempt-2/logs/before-removal-final.log`): the task finalization conflict plus intermittent second-attempt waiting/no accept and exhausted-budget replay. These latter assertions reveal that a rejected gate can leave a synthetic open finding that the passing second mock does not reliably resolve. The fixture does not manually mark it addressed, because the accepted recovery intent requires ordinary task dispatch after changing only the controlled mock response. This is an additional material recovery gap; B2/B3 remain open.

## P1-7b Step 6 continuation 2 handoff — 2026-09-25

- Mode `issue-resolution`, issue `comm-000002`, workflow `nested-v1-80c1d11cf24b10bfb1dcf403828b9c63b3ef079f5c43898e5fde4597322c311a`, communication `comm-000015`, plan `p1-dispatch`. Codex `/root` checked the unchanged accepted plan and proposed amendment. No workflow plan amendment/review acceptance or added production write path is present; the read-only `plan_amendment_review` decision is advisory only. No further test, bundle or production source edit was authorized in this continuation.
- The required gate-recovery behavior and before-removal V1 remain **FAILED** on the latest source. The complete broad receipt is also **FAILED** and predates the last test-fixture edit. B2/B3 and P1-7b remain open. Resume with a formally accepted bounded amendment for `Sources/RielaCLI/WorkflowRunCommand.swift` and a resolution for the open-finding recovery behavior that preserves the ordinary-dispatch test intent; then rerun focused, V1, affected, V8 and serial broad gates on identical final source. Formal reviews, B4 shared docs, exact-file commit and non-force push follow only after implementation evidence passes.

## P1-7b Step 6 accepted-amendment implementation — 2026-09-25

- Mode `issue-resolution`; issue `comm-000002` “Finish Work Runtime P1-7b after accepted task-gate amendment”; workflow `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`, step `step6-implement`, fanout branch/plan `p1-dispatch`, branch `feat/remaining-impl-plans`, checkpoint HEAD `bffa1da4ab161892ecec54784439e6148860f302`, `dependsOn: []`. Codex `/root` owned serial edits. Read-only Codex agents `inspect_plan`, `inspect_code`, `inspect_examples` and `bounded_selfcheck` inspected separate seams. Per-edit preimages, SHA-256, intended hunks and full logs are under `tmp/work-runtime-p1-7b-20260925-c8840ad-comm000006/plans/p1-dispatch/attempt-1/`. No Git staging, commit or push occurred.
- B0/B1 complete: the seven real task-lifecycle rows map to `TaskRuntimeExampleTests` acceptance, rejected-gate recovery, guard stop, capacity wait, bounded director, reopened seven-token child accounting and invalid-output escalation tests. Both replacement bundles retain repair → required verification and one director worker. `examples/task-repair-loop/workflow.json` drops the redundant `acceptWhen.decision`: it created a persistent synthetic finding on the first rejected task attempt that prevented accepted second-attempt completion. Required-gate task completion still demands acceptance. The repair bundle README and expected results now distinguish standalone success/rejection from task recovery; director bundle docs retain the existing task-versus-mock distinction. Legacy examples remain untouched.
- B2 complete: `Sources/RielaCLI/WorkflowRunCommand.swift` carries actual reservation presence through both `RunFinalizeContext` call sites and skips required-gate failure conversion only for reserved task runs. The existing standalone helper additionally treats a rejected required gate as failed even when no blocking finding exists, preserving standalone failure after the bundle policy correction. `TaskRuntimeExampleTests.swift` proves the initial completed canonical task session and rejected gate, causal pending recovery, distinct consumed second attempt/session, accepted gate, succeeded task, stable ledger on refused replay, and standalone rejected repair evidence in canonical and artifact stores. No direct terminal-state seeding or assertion weakening occurred. Read-only `bounded_selfcheck` found the standalone bundle regression; that finding was repaired and the new exact test passed.
- B3 final-source manifest `source-final-3.sha256` covers 1,674 `Sources/`, `Tests/`, `examples/`, `Package.swift` and `Package.resolved` files; manifest SHA-256 `da0ce627b1d590a53a1e6c96d981607cfc28851940c97894ebe776bacebd91a5`. `logs/final3/source-check-after.log` verifies all entries after tests, exit 0. `verification-evidence-final3.json` records exact serial commands, complete log paths and terminal exits. Build passed. Separate task recovery, bundle-specific standalone rejection and existing standalone required-gate regressions each passed 1/1; examples passed 21/21, before-removal V1 57/57, affected store/decision suites 115/115, selected-host V11 55/55, all zero failures. Both replacement validations and mocks exited 0; `logs/final3/mock-business-assertions.log` confirms two repair nodes with accepted verification and `acceptance.met=true`, plus one director node with `kind=accept`. Exact two-file strict SwiftLint passed (`changed-swift-files.nul`, `logs/swiftlint-final3.log`); `git diff --check` and `git diff --cached --check` passed.
- The complete final-source serial broad command remains **FAILED**, exit 1: 2,668 tests, two skipped, 24 assertions (`logs/final3/broad.log`). `broad-classification-final3-v2.json` matches 19/19 prior P1-6d non-slice assertion identities and classifies five additional assertions in two unrelated timing fixtures: `WorkflowCommandLivePersistenceTests.testSessionProgressReportsActiveStepDuringLiveSecondStep` (one) and `WorkflowCommandTests.testAutoImproveRetriesStalledWorkflowFromActiveStep` (four). Each exact final-source test passed 1/1 immediately afterward (`logs/final3/live-progress-exact.log`, `auto-improve-exact.log`). The prior final2 broad log also failed and is superseded; an intervening same-source rerun was interrupted before a material self-check repair and is not a complete gate. No broad PASS is claimed. Owners/follow-ups are the historical P1-6d broad inventory and the WorkflowCommand live-progress/auto-improve stall timing fixtures; formal review must decide slice-only acceptance. No P1-7b assertion failed in the complete final-source broad run.
- Implementation B0–B3 source, test, bundle documentation and behavioral verification are complete on the stated final source. Formal test-integrity, single Sol adversarial and Astra combined-tree reviews, then B4 shared documentation/index updates and exact-file commit/non-force push are downstream and not claimed. P1-7a legacy removal, parent P1 and unrelated broad follow-ups remain open.

### P1-7b Step 6 final-source correction and handoff — 2026-09-25

- This entry supersedes the preceding `final3` receipt for B2/B3; earlier logs remain immutable failure/history evidence. Codex `/root` addressed read-only `bounded_selfcheck` findings in the same authorized `Sources/RielaCLI/WorkflowRunCommand.swift` path. Standalone required-gate finalization now fails a non-accepted **required** gate, while optional non-accepted gates without findings retain their prior behavior and any blocking finding retains the prior failure path. `TaskRuntimeExampleTests.testStandaloneNonAcceptedRepairGateFailsAndRetainsEvidence` proves rejected and needs-work responses both fail with canonical and artifact evidence. The final read-only self-check accepted the required/optional predicate with no remaining high or mid P1-7b finding.
- Final source manifest is `tmp/work-runtime-p1-7b-20260925-c8840ad-comm000006/plans/p1-dispatch/attempt-1/source-final-5.sha256`, 1,674 files, SHA-256 `1dfde966bf33a843e3d222f0995760aa0bc6bde6811422ffef5439cc32d6e042`. Complete foreground receipts, exact commands and structured counts are in `verification-evidence-final5-v5.json` and `logs/final5/`. Build exit 0; exact task recovery 1/1, bundle non-accepted standalone 1/1 (two scenarios), original standalone rejected-gate 1/1, example suite 21/21, before-removal V1 57/57, affected store/decision 115/115 and selected-host V11 55/55 all passed with zero failures. Both replacement bundle validations and deterministic mocks exited 0; mock business assertions passed. Strict two-file SwiftLint `logs/swiftlint-final5.log`, final diff and cached-diff checks, and 1,674-file source check all exited 0.
- Final-source serial broad `logs/final5/broad.log` is **FAILED**, exit 1: 2,668 tests, two skipped, 20 assertion failures. `broad-classification-final5-v2.json` matches 19/19 prior P1-6d non-slice identities and one unrelated `WorkflowCommandLivePersistenceTests.testSessionProgressReportsActiveStepDuringLiveSecondStep` timing failure; no P1-7b assertion failed. The exact timing test failed 1/1 then passed 1/1 on immediate final-source repeat (`logs/final5/live-progress-exact.log`, `live-progress-exact-repeat.log`), confirming an intermittent follow-up rather than a new P1-7b defect. Owner: WorkflowCommand live-progress fixture; prior 19 owners remain the historical broad follow-ups. Do not call the broad gate passing. Formal test-integrity, Sol adversarial and Astra combined-tree reviews must decide slice-only acceptance before B4 shared docs and exact-file publication. P1-7a, parent P1 and unrelated broad follow-ups stay open.

### P1-7b catalog amendment Step 6 — 2026-09-25

- Mode `issue-resolution`; issue `comm-000002` “Finish P1-7b after integration review found missing example-catalog coverage”; workflow `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`, step `step6-implement`, plan/branch `p1-dispatch` / `feat/remaining-impl-plans`, checkpoint `c1643741e3736aca7a4ef2287552365d81267013`, `dependsOn: []`. Codex `/root` made the serial edits; read-only Codex agents `catalog_audit` and `broad_audit` checked selection and prior failure ownership. Exact preimages and edit intentions are under `tmp/work-runtime-p1-7b-catalog-20260925-bffa1da-comm000006/plans/p1-dispatch/attempt-1/` and `attempt-2/`. No staging, commit or push occurred.
- The material Astra `p1-7b-example-parity-misclassified` finding is repaired in `Tests/RielaCLITests/RielaExampleCatalog.swift` and `Tests/RielaCLITests/RielaExampleParityTests.swift`: both replacement workflows are in the sorted catalog; observed deterministic selection is 41 mocks, including both bundles, with node-runtime count still zero. All assertions remain. Three pre-existing optional UTF-8 conversion diagnostics in the touched parity file were changed to failable `String(bytes:encoding:)` plus `XCTUnwrap` to satisfy strict selected-file SwiftLint. The previous broad inventory's catalog assertion was P1-7b-owned, never a historical non-slice exception.
- Final source identity is `attempt-2/source-final.sha256`, 1,747 files, manifest SHA-256 `c2d91d0d5b6a47efdb8f99faca3ebda009293c0597414cb5bdc5f4915d83529d`; `source-identity-check.log` exited 0 after the tests. Exact commands, complete logs, exits and counts are in `attempt-2/verification-evidence.json` and `catalog-exact-evidence.json`. Build exited 0. Exact catalog validation and deterministic mock tests each passed 1/1 with zero failures; mock log records `parity-start` and `parity-output` for `task-agent-director` and `task-repair-loop`. Exact task recovery and standalone rejected-required-gate tests each passed 1/1; task examples 21/21, before-removal V1 57/57, affected work 115/115 and selected-host V11 55/55 passed. Both replacement validates and mocks exited 0; `mock-business-assertions.log` proves repair completed two nodes with accepted verification and director completed one node with `kind=accept`. Strict four-file SwiftLint and repository-wide SwiftLint exited 0; repository warnings are in unrelated files. `git diff --check` and `git diff --cached --check` exited 0.
- Final-source serial broad regression is **FAILED**, exit 1: 2,668 XCTest cases, two skipped, 18 assertion failures (`attempt-2/broad.log`, `broad-classification.json`). All 18 exact file/line/test identities match the preceding catalog-corrected run; none belongs to P1-7b. Owners and follow-ups remain Doctor/backend-capability (7), WorkflowCommand/host-readiness (8), runner admission (1), and temporary registration (2), in the pre-existing P1-6d broad inventory. The previous 19-count inventory wrongly included the P1-7b catalog assertion; that assertion is now absent. Do not call the aggregate passing. Formal test-integrity, single Sol adversarial, and Astra combined-tree reviews decide slice-only acceptance; B4 shared docs/index and exact-file commit/non-force push follow those gates. P1-7a, parent P1 and unrelated broad follow-ups remain open.

#### P1-7b implementation completion criteria

- [x] Both replacement bundles are in shared catalog validation and deterministic mock execution, with observed count 41 and unchanged assertions.
- [x] Seven real task lifecycle cases, exact recovery and standalone gate regressions, both bundle validates/mocks, before-removal V1, affected suites, build, lint, diff and source identity have final-source evidence with positive counts where applicable.
- [x] Final-source serial broad command completed and its 18 failures are classified as FAILED with non-slice owners and follow-ups; no P1-7b assertion remains.
- [ ] Formal test-integrity, Sol adversarial and Astra combined-tree review acceptance; B4 shared documentation; exact-file commit and non-force push (downstream workflow steps).

## P1-7a Step 6 attempt 1 — A1 deletion barrier remains closed (2026-09-25)

- Mode `issue-resolution`; issue `comm-000001`; Riela execution `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`, step `step6-implement`, plan `p1-dispatch`, checkpoint HEAD `855a99692db80ec17cac055d3c8263473223dbd0`, branch `feat/remaining-impl-plans`, `dependsOn: []`. Codex `/root` owns the sole coupled test edit; read-only Codex `remote_boundary`, `removal_map` and `test_map` investigated separate A0 surfaces. Evidence and per-edit preimages/intents are under `tmp/work-runtime-p1-7a-20260925/plans/p1-dispatch/attempt-1/`.
- A0 inventory found no in-repository receiving `ExecuteWorkflowInput` schema or decoder: `Sources/RielaCLI/WorkflowCommands.swift` is an outbound GraphQL client only. Incoming `autoImprove`/`nestedSuperviser` false/null presence rejection is unverified and needs exact receiving-boundary ownership. `Sources/RielaCLI/TaskRunCancellation.swift` owns the exact-session observer needed for live inactivity but is outside `writePaths`. `Tests/RielaCLITests/SurfaceParityCLITests.swift`, `examples/catalog/chat-persona-and-agent-trio.md`, and three executable `examples/monja-*/` TypeScript callers contain retained references outside `writePaths`. `attempt-1/bounded-amendment-request.json` proposes exact reviewed ownership; it is not an accepted amendment.
- A1 test-first work in `Tests/RielaCLITests/TaskDispatcherIntegrationTests.swift` adds real-dispatch bounded failure retry, last-admitted success and ordinary exact-session cancellation cases and strengthens replay with reopened WorkStore/ledger/accounting comparisons. The exact replay test passes 1/1 (`logs/a1-replay.log`, exit 0), ordinary cancellation passes 1/1 (`logs/a1-cancellation.log`, exit 0), and existing gate recovery and director-child cancellation pass 2/2 (`logs/a1-existing-behaviors.log`, exit 0). Build passes (`logs/a1-v0.log`, exit 0), final changed-file strict SwiftLint passes (`logs/a1-v6-final.log`, exit 0), and baseline repository SwiftLint exits 0 with existing warnings (`logs/baseline-v7.log`).
- **Before-removal V1 is FAILED**, exit 1, 60 tests with four failed assertions in two test cases (`logs/before-removal-v1-final.log`): `TaskCommandMutationTests` 4/4, `TaskDispatcherTests` 8/8, `TaskRuntimeExampleTests` 21/21, and `TaskDispatcherIntegrationTests` 25/27. A mock adapter provider error yields a durable failed attempt with canonical `failureKind: nil`; the two new retry tests cannot prove the required recovery contract. The fail-closed store first persists a failed step with nil failure kind before runner failure finalization, so a repair must preserve terminal immutability and be reviewed at the exact owning path. No legacy implementation, test, option or example file was deleted or disabled. A1 positive inactivity and progress, passing V1, then A2/A3 final-source gates remain open; A4 formal reviews and A5 docs/commit/push remain downstream. Parent P1 remains open.

#### P1-7a implementation completion criteria

- [x] A0 inventory, baseline source identity, retained callers and proposed ownership amendment recorded.
- [ ] A1 named positive replacement tests and complete passing before-removal V1 with source identity.
- [ ] A2 authorized legacy deletion, removed-field rejection and retained-path regressions after the A1 barrier.
- [ ] A3 final-source build, V1–V9, retained-path and serial broad receipts with exact failure ownership.
- [ ] A4 formal test-integrity, Sol adversarial and Astra combined-tree reviews; A5 shared docs and exact-file publication (downstream workflow steps).

### P1-7a Step 6 continuation 1 — exact retry owner (2026-09-25)

- Riela communication `comm-000011` returned `implementation_continue: true` for the same plan and source. Codex `/root` continued the A1 failure investigation; read-only Codex `/root/retry_owner` and `/root/inactivity_owner` made no edits. `tmp/work-runtime-p1-7a-20260925/plans/p1-dispatch/attempt-2/ownership-diagnostic.json` records the exact causal chain and proposed ownership, not an accepted plan amendment. No previous test log is reused as a newly passing check.
- `Sources/RielaCore/RuntimePublication.swift:329` publishes the adapter failure as a failed step; `Sources/RielaCore/RuntimeStore.swift:530` sets the session failed without a failure kind; `Sources/RielaCLI/FailClosedSQLiteWorkflowRuntimeStore.swift:70` durably writes that first terminal snapshot. Later runner finalization requests `.adapterFailure`, but `Sources/RielaCore/SQLiteWorkflowRuntimePersistenceStore.swift:774` correctly rejects changing the terminal failure kind. `WorkEvidenceProjector` therefore observes nil and the bounded retry tests remain failed. A sound atomic failure publication needs reviewed ownership of `RuntimePublication.swift` and `RuntimeStore.swift`; changing the CLI result alone or relaxing terminal immutability is not accepted. The committed P1-7a manifest authorizes neither path.
- The existing `TaskRunCancellation.swift` observer can act on a durable exact-session cancellation decision, but new independent polling in `TaskDispatch.swift` would conflict with this plan's no-new-polling-framework boundary. Running-session inactivity and progress need an exact reviewed observer seam and fresh backend event evidence before implementation. The remote receiving decoder/schema and retained callsites listed in A0 still lack approved ownership. Before-removal V1 remains **FAILED** (60 cases, four assertions, exit 1); no legacy deletion or final-source acceptance is claimed. Resume with a formal bounded amendment, then repair A1 and rerun complete V1 before any A2 removal.

### P1-7a Step 6 resumed A1 attempt 3 — blocked handoff (2026-09-25)

- Mode `issue-resolution`; issue `comm-000001`; execution `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`, step `step6-implement`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, checkpoint `ae8ec0816f6f0638233847fe00501899c4c2e921`, `dependsOn: []`. Codex `/root` owned all edits; read-only Codex `/root/failure_read`, `/root/inactivity_read` and `/root/dependency_read` inspected separate seams. Exact preimages, intended hunks, postimages, source hashes and full foreground logs are under `tmp/work-runtime-p1-7a-20260925/plans/p1-dispatch/attempt-3/`. The accepted plan/manifest now authorize the atomic owner paths and live observer; the previous ownership warning is history.
- A1 atomic repair is partial: `RuntimeStore.swift` accepts an optional typed failure kind on failed-step updates and writes it with the first failed status; `RuntimePublication.swift` supplies `.adapterFailure` for direct and advisory adapter failures. The first focused retry rerun passed the prior nil-kind assertions, but both cases still fail later state assertions: the second failed attempt correctly reaches `wait(human)` at its attempt limit, and the last admitted completed attempt remains waiting because its first failed repair run left open `id:missing-required-gate-verification` evidence. The diagnostics were added to the preserved dirty integration tests. No assertion was removed and no legacy path was deleted.
- Current before-removal V1 is **FAILED**, exit 1, 60 tests and five assertions: `TaskCommandMutationTests` 4/4, `TaskDispatcherTests` 8/8, `TaskDispatcherIntegrationTests` 25/27 (two assertions), `TaskRuntimeExampleTests` 20/21 (three assertions). Complete log: `attempt-3/logs/before-removal-v1.log`. The additional director-child failure is a new P1-7a regression: classified command-node failure now triggers retry instead of the old director escalation. Canonical regression is also **FAILED**, exit 1, 93 tests and three assertions in `TaskCancellationIntegrationTests.testTerminalFirstOrdinaryFailureSurvivesLateSIGINTAndExternalRequest`; its obsolete nil-kind/waiting expectations are in `Tests/RielaCLITests/TaskCancellationIntegrationTests.swift`, outside approved `writePaths`. Exact log: `attempt-3/logs/canonical-regression.log`. The currently owned `TaskRuntimeExampleTests.swift` may be repaired after the ownership amendment, but the unowned cancellation test must not be edited before a bounded plan/manifest amendment decides its intended behavior. The receiving `ExecuteWorkflowInput` schema/service owner, repository, exact paths, authorization and false/null rejection evidence remain absent; A2/A3 acceptance cannot be claimed.
- Exact changed-file strict SwiftLint and `git diff --check` exited 0 (`attempt-3/logs/selected-swiftlint.log`, `diff-check.log`). Source hashes are `attempt-3/source-changed.sha256`. A1 live inactivity/progress tests and passing complete V1 remain open; A2 deletion and A3 final-source gates are blocked behind them. Formal reviews and publication remain downstream. No staging, commit, push, reset, stash or extra worktree occurred. P1-7a and parent P1 remain open.

### P1-7a Step 6 current issue-resolution A1 continuation (2026-09-25)

- Issue `comm-000001`, Riela execution `nested-v1-efd9d550f59df953348382ef9525752475e2f9bcb944e7e9cecb0c5f967b92bb`, step `step6-implement`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, HEAD `e2515a452ad2afa34fd37ff2827bbb0dea5ce807`, `dependsOn: []`. Codex `/root` owns serial edits; read-only Codex `/root/policy_inspect`, `/root/live_inspect`, `/root/tests_inspect` supplied diagnostics. Per-edit preimages/intents and complete foreground logs are in `tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-1/`. The prior four dirty files and failed attempt-3 logs were preserved; no legacy path was deleted.
- `TaskDispatch.swift` and `TaskGuardCoordinator.swift` now persist terminal-only retry-capacity violation through the shared applier, prefer authorized director escalation at the failed-attempt threshold while capacity remains, and supersede only a canonically proven prior synthetic missing required gate after a matching real accepted gate. `TaskCancellationIntegrationTests.swift` explicitly configures non-retry adapter failure and adds retry-enabled terminal-first coverage. `TaskRunCancellation.swift` extends its exact-session observer for live canonical inactivity; the controlled adapter tests prove no-event stop and continuing-progress completion. `WorkflowRunCommand.swift` has a scoped test adapter seam. The prior atomic failure-kind edit remains intact.
- Current source manifest `attempt-1/source-before.sha256` equals `source-after.sha256`, SHA-256 `5e52faef762dec43c683ab3b00e9d63e51db37756a71690b73af48505023ba09`, across final V0/V1/canonical/cancellation-host. V0 `swift build --scratch-path tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/build/p1-dispatch` exited 0 (`logs/v0-final.log`). Before-removal V1 exact four-suite filter exited 0, **63/63**, zero failures (`logs/before-removal-v1-final.log`): mutation 4, dispatcher 8, integration 30, examples 21. Canonical regression exact filter exited 0, **94/94**, zero failures (`logs/canonical-regression-final.log`). Cancellation-host exact filter exited 0, **49/49**, zero failures (`logs/cancellation-host-final.log`). Live two-case filter exited 0, **2/2**, zero failures (`logs/live-focused-2.log`); retry-disabled and historical-cause two-case filter exited 0, **2/2**, zero failures (`logs/policy-extra.log`). Exact changed-file strict SwiftLint exited 0 (`logs/selected-swiftlint-3.log`); `git diff --check` exited 0. Earlier compile/test failures remain in the same numbered attempt logs with terminal exit files and were repaired in later source-matched checks.
- **A1 deletion barrier remains closed.** Required negative matrix cases `testUnclassifiedFailureDoesNotAutomaticallyRetry`, `testCurrentMissingOrRejectedGateAndSubstantiveFindingsStillBlock`, and `testDirectorEscalationRequiresConfigurationThresholdAndCapacity` are not implemented; the live test does not yet cover the separate stops-after-progress variant or warning/replay race cases. The first terminal hook classification assertion and full row-to-test mapping still need completion. V1 is positive but does not waive those named requirements. A2 removal and A3 final-source gates have not run. External receiving boundary `tacogips/rielflow` at `packages/rielflow-graphql/src/schema-contract.ts` remains locally tested at `c2b16ab` but unpublished because the remote is archived and rejected push with 403. Actual published false/null rejection and authenticated acceptance are necessary before A2/A3 acceptance. Formal test-integrity, Sol adversarial and Astra combined-tree reviews, review-dependent docs, exact-file commit and push remain downstream. P1-7a and parent P1 remain open.

#### Current P1-7a completion criteria

- [x] Preserve atomic `adapterFailure`; repair retry limit, director precedence, historical synthetic gate recovery and both terminal-first policy cases.
- [x] Live no-heartbeat and continuing-progress proof; positive complete before-removal V1, canonical regression and cancellation-host on one source identity.
- [ ] Complete the remaining A1 negative matrix, stopped-after-progress/warning/replay cases and first-write hook evidence; seal A1 before deletion.
- [ ] A2/A3 removal, published receiving-boundary evidence and final-source V1–V9/retained paths.
- [ ] A4 formal reviews and A5 documentation/publication (downstream workflow steps); parent P1 separate audit.

The current `Tests/RielaCLITests/TaskDispatcherIntegrationTests.swift` is 1,111 lines. The accepted plan requires a reviewed narrow responsibility split if an owned Swift file exceeds 1,000 lines; the new split path is absent from the committed `writePaths`. Resume A1 test additions after an exact-path amendment authorizes `Tests/RielaCLITests/TaskDispatcherIntegrationTests+LiveInactivity.swift` (or an equivalent focused split). Do not treat passing SwiftLint's repository 1,200-line warning threshold as approval of this plan exception.

### P1-7a A1 Step 6 implementation receipt — GuardPolicy split (2026-09-25)

- Mode `issue-resolution`; issue `comm-000001`; workflow execution `nested-v1-9e41064a52e1d83de2da24f4548f2341589fc6fdb2b864eba8c75e00972c553e`; step `step6-implement`; plan `p1-dispatch`, `dependsOn: []`, accepted design `comm-000004` and accepted plan `comm-000006`. Checkpoint HEAD `757d6f467b76500d79be03972808b37607b0f82b`, branch `feat/remaining-impl-plans`. Codex `/root` owned all edits. Read-only Codex `/root/matrix_audit` and `/root/evidence_audit` mapped gaps and receipts. The nine preserved dirty implementation paths and all earlier logs remain; no reset, deletion, external edit, staging, commit or push occurred.
- Exact preimages and edit intentions are under `tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-2/intents/`. `Tests/RielaCLITests/TaskDispatcherIntegrationTests+GuardPolicy.swift` contains exactly the accepted seven moved test methods and two moved helpers (`DelayedScenarioNodeAdapter`, `failedRepairScenario(in:)`), plus four new named policy/race tests. The original is **837 lines**, the new file **541 lines** (`attempt-2/logs/line-counts-final.log`, exit 0). All eleven moved/added methods appear once in `attempt-2/logs/discovery-final.log` (exit 0) and each passed once under the original `TaskDispatcherIntegrationTests` suite in final V1. The original keeps the harness, placement, admission, dry-run and CLI rerun tests.

| A1 row and subcases | Exact suite/method, owner and asserted outcome |
| --- | --- |
| Bounded retry, last admitted success, unclassified/non-retry | `TaskDispatcherIntegrationTests.testFailedTaskRetriesWithinBudgetAndStopsAtLimit`, `testLastAdmittedRecoveryAttemptCanSucceed`, `testRetryDisabledAdapterFailureWaitsWithoutPendingRequest`, `testUnclassifiedFailureDoesNotAutomaticallyRetry` in `Tests/RielaCLITests/TaskDispatcherIntegrationTests+GuardPolicy.swift`; first canonical failed snapshot and durable attempt have `adapterFailure`, one pending rerun is consumed by a distinct session, capacity exhaustion stops a failed last attempt while a last admitted success can complete; explicit retry-disabled and a durable failed nil-kind outcome wait without pending retry. Nil-kind case uses a persisted failed attempt through the shared coordinator, not a synthetic successful terminal seed. |
| Historical/current gates, substantive findings, human acceptance | `TaskDispatcherIntegrationTests.testLastAdmittedRecoveryAttemptCanSucceed` and `testCurrentMissingOrRejectedGateAndSubstantiveFindingsStillBlock` in the GuardPolicy extension; matching accepted latest gate resolves only the canonical historical synthetic finding with the exact latest gate evidence ID as cause, while old gate evidence and reopened resolution remain durable. Seven real two-attempt dispatch variants assert missing synthetic rejected gate, rejected, needs-work, mismatched source lineage, similar-ID high/mid substantive findings, and required human acceptance block completion; open/superseded status, exact latest gate decision, absent/present resolution cause and reopened findings are checked. |
| Director authorization | `TaskRuntimeExampleTests.testConfiguredDirectorChildRunsOrdinarilyAndCannotAcceptFailedWork` and `testDirectorEscalationRequiresConfigurationThresholdAndCapacity` in `Tests/RielaCLITests/TaskRuntimeExampleTests.swift`; real child follows configured failed work but cannot accept it, while no configuration, below threshold, exhausted capacity and latest director-child entry each deny another child; exhausted capacity has a persisted budget violation and no pending replacement. |
| Live stop and progress | `TaskDispatcherIntegrationTests.testTaskInactivityPersistsViolationAndStopsExactAttempt` and `testTaskProgressPreventsInactivityAction` in the GuardPolicy extension; separate no-heartbeat and one-progress-then-stall runs each persist one inactivity violation and acknowledge cancellation for the exact session before replacement, while continuing backend progress completes without inactivity action. |
| Warning, replay, progress/terminal/cancellation races | `TaskDispatcherIntegrationTests.testTaskInactivityWarningAndReplayPreserveSingleObservation` and `testTaskInactivityProgressTerminalAndCancellationRacesPreservePrecedence` in the GuardPolicy extension; warning persists one immutable observation without cancellation/forced decision; progress recheck avoids a false stop, a late signal after terminal persistence preserves completed outcome, cancellation before execution receives exact-session acknowledgment and no pending retry; reopened/refused replay preserves row counts without duplicate evidence/decisions/attempts. |
| First terminal-hook classification | `TaskCancellationIntegrationTests.testTerminalFirstOrdinaryFailureSurvivesLateSIGINTAndExternalRequest` and `testTerminalFirstRetryableFailurePreservesOnePendingRequestAfterLateCancellation` in `Tests/RielaCLITests/TaskCancellationIntegrationTests.swift`; both first terminal-persistence hooks assert `adapterFailure`, both signal/external request branches retain terminal-first precedence, explicit retry-disabled/enabled policy gives waiting/no pending versus one durable rerun request, and reopened canonical outcome stays failed rather than cancelled. |

- Final source identity covers `Sources/**/*.swift`, `Tests/**/*.swift`, `Package.swift` and `Package.resolved`: `attempt-2/source-before.sha256` equals `source-after.sha256`, manifest SHA-256 `414f30161a721c98f86e2be7701b7ced7fc8381db00bc579ab7a4cd52ddef66f`; `source-check-final` and `source-identity-final` both exit 0. Final complete foreground logs and paired `.command`/`.exit` receipts are under `attempt-2/logs/`: V0 `v0-final` exit 0; discovery `discovery-final` exit 0; policy-focused `policy-final` **74/74**, zero failures, exit 0; complete before-removal V1 `v1-final` **68/68**, zero failures, exit 0; canonical regression `canonical-final` **94/94**, zero failures, exit 0; cancellation-host `cancellation-host-final` **49/49**, zero failures, exit 0; four live cases `live-final` **4/4**, zero failures, exit 0. Strict exact changed-file SwiftLint `selected-swiftlint-final` and selected non-strict baseline `selected-swiftlint-baseline-final` exit 0 using `changed-swift-files-final.nul`; `diff-check-final` exits 0. The before-final-repair run is retained under `logs/pre-gate-repair/`; failed gate assertions (`gate-real-2.log`) and the ineffective timeout fixture (`unclassified-runtime-1.log`) remain immutable diagnostic evidence, superseded by current-source full gates.
- **A1 implementation-phase criteria met for formal review:** split, all named matrix/race/first-hook cases, positive discovery/execution, final-source V0/V1/policy/canonical/cancellation-host/live gates, strict selected lint and source identity. Formal test-integrity, single Sol adversarial and Astra integration decisions are pending downstream; no self-acceptance or commit/push is claimed. A2–A5 and parent P1 remain open. A2/A3 still require published `tacogips/rielflow` receiving-side false/null rejection and authenticated acceptance; local unpublished `c2b16ab` and archived-remote 403 do not meet that dependency. No legacy path was removed.

### P1-7a A1 Step 6 fresh verification receipt — 2026-09-25

- Mode `issue-resolution`; issue `comm-000001`; workflow `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`; step `step6-implement`; fanout branch/plan `p1-dispatch`, `dependsOn: []`; Codex `/root` integrated this receipt on `feat/remaining-impl-plans` at checkpoint HEAD `352291aca2cd4af6eefd71ca81600c658e03eb13`. The accepted §17.10 split and all eleven existing dirty paths were retained. No Swift source or test was edited in this attempt; no legacy path was removed.
- Fresh source membership and SHA-256 manifests under `tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-3/` cover 978 `Sources/`, `Tests/`, `Package.swift`, and `Package.resolved` paths, including the untracked GuardPolicy extension. `source-before.sha256` has SHA-256 `1868f83e4c060dbf22b03fcdb36e3e1f93372d85c7b8e349368a65b7c6ca5e3d`; before/after manifests and membership compare equal, and `shasum -a 256 -c` exits 0. Complete foreground logs, exact `.command` records and terminal `.exit` records are in `attempt-3/logs/`.
- Fresh Xcode V0 `v0` exit 0; `discovery` exit 0 is discovery only. `policy` passed **74/74**, `v1` passed **68/68** (mutation 4, dispatcher 8, integration 34, examples 22), `canonical` passed **94/94**, `cancellation-host` passed **49/49**, and `live` passed **4/4**; each exited 0 with zero failures. `discovery-execution-audit.log` confirms each of the seven moved and four added GuardPolicy methods has one source definition, one discovery entry, and one passing execution in each policy and V1 suite; both moved helpers have one definition and callers. The original test file is 837 lines and the extension is 541 (`line-counts`, exit 0). Strict selected-file SwiftLint via `changed-swift-files.nul` and `git diff --check` each exit 0.
- A1 implementation and behavioral evidence are complete for formal test-integrity, one Codex Sol adversarial review and Codex Astra integration review. Those review decisions, review-dependent documentation, scoped commit and non-force push remain downstream and are not claimed here. A2–A5 and parent P1 remain open; A2/A3 require native Riela receiving evidence and separate implementation/review. No external repository operation occurred.

### P1-7a A1 Step 6 adversarial redispatch blocker — 2026-09-25

- Mode `issue-resolution`; issue `comm-000001`; workflow `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`; step `step6-implement`; Codex `/root` reviewed Step 7 communication `comm-000013`. The prior test-integrity acceptance remains valid for its reviewed source, but Step 7 adversarial review **rejected** A1 with one **mid** finding: backend progress can commit after `TaskRunCancellation.observeInactivity` rechecks `lastBackendEventAt` and before `WorkStore.applyDecision` durably stops or reruns the attempt. The preceding A1 implementation-complete receipt is superseded for the current review decision. Attempt-3 build, test, lint and source-identity receipts remain immutable historical evidence, not proof that this race is fixed.
- `Sources/RielaCLI/TaskRunCancellation.swift` rechecks the canonical session before `TaskGuardCoordinator.evaluateAndApply`; that coordinator saves guard evidence and then calls `WorkStore.applyDecision`. The runner and WorkStore share the SQLite database, but the decision transaction checks task version and cancellation state without checking the observed execution ID and progress timestamp. A second observer read cannot close the gap. The bounded repair is to condition the inactivity application inside that transaction on the same execution and progress value, then discard/re-evaluate stale observation without committing a stop/rerun. `SQLiteWorkflowRuntimePersistenceStore.load(sessionId:in:)` already supports a transaction-scoped read.
- The accepted `p1-dispatch` manifest excludes `Sources/RielaWork/WorkStore+Decisions.swift`, the owner of `WorkStore.applyDecision`; `attempt-4/logs/ownership-audit.log` records the path check. A repair in `TaskRunCancellation.swift` alone cannot provide the required atomicity without duplicating or bypassing the shared applier. **A1 is implementation-blocked** pending a reviewed exact-path amendment adding `Sources/RielaWork/WorkStore+Decisions.swift` to this plan's `writePaths`. After acceptance, implement the transactional check with a deterministic progress-between-recheck-and-decision regression, then run fresh source-matched live, policy, V0, V1, canonical, cancellation-host and strict selected-file SwiftLint gates. No source/test edit, legacy deletion, external operation or success-shaped verification occurred in this redispatch. A2–A5 and parent P1 remain open.

### P1-7a A1 Step 6 atomic inactivity implementation receipt — 2026-09-25

- Mode `issue-resolution`; issue `comm-000001`; workflow `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`; step `step6-implement`; plan `p1-dispatch`, `dependsOn: []`, branch `feat/remaining-impl-plans`, HEAD `e7098b6f2eb558cce250318fbeb6d3ac1aeeaa77`. The reviewed exact-path amendment includes `Sources/RielaWork/WorkStore+Decisions.swift`; the previous attempt-4 ownership blocker is resolved for this dispatch. Codex `/root` owned all edits. Read-only Codex `/root/atomic_read_review` identified a millisecond observation-ID collision and missing race assertions; Codex `/root/test_coverage_read` mapped the test gaps. Both findings were repaired before final verification. Per-edit preimages/intents and immutable failed preliminary logs are under `tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-5/`.
- `WorkStore+Decisions.swift` requires `LiveInactivityObservation` for any new action citing a causal inactivity guard violation. After replay-first handling, its existing SQLite transaction reads the exact canonical session on the same connection and checks task/version, latest running attempt, session, running execution, full-precision progress, and cancellation before any decision, decision evidence, version, cancellation, reservation, or lease mutation. `TaskGuardCoordinator.swift` passes that observation; `TaskRunCancellation.swift` supplies it after the observer recheck, ignores a rejected stale action without interrupting, and uses full-precision progress in observation IDs so new progress remains eligible after immutable stale evidence. `TaskDispatch.swift` and the test harness forward one internal post-recheck barrier. No new polling service or schema was added.
- `Tests/RielaCLITests/TaskDispatcherIntegrationTests+GuardPolicy.swift::testTaskProgressBetweenObserverRecheckAndDecisionPreventsInactivityAction` uses the barrier to commit and read back exact-execution progress before the decision transaction under stop and bounded-rerun policies. It asserts completed single attempts, no inactivity decision/cancellation/pending reservation and reopened state. The shared-applier variants reject missing and changed-execution observations twice each while checking unchanged task version, attempt state and SQLite lease, cancellation, pending, decision, application and evidence row counts. The same seam waits for a real terminal commit, durably commits cancellation before release, and proves a genuine idle observation still acts once. `testTaskInactivityPersistsViolationAndStopsExactAttempt` now replays the committed decision after terminal acknowledgment with identical application and row counts. Existing warning, progress, first-terminal, policy, gate, director and retry assertions remain.
- Final stable-source evidence is `attempt-8/`: 978 source/package members; `source-before.sha256` and `source-after.sha256` plus membership files compare equal; manifest SHA-256 `873daf08f6aa1bbc2d36679abd52e50c7d9d5ab053d08ad4530ff592492e2b6e`; `shasum -a 256 -c` exits 0. Complete foreground `.command`, `.log` and terminal `.exit` receipts are in `attempt-8/logs/`. Xcode V0 and discovery exit 0; discovery is not behavioral execution. Policy **75/75**, before-removal V1 **69/69**, canonical **94/94**, cancellation-host **49/49**, and live **5/5** each exit 0 with zero failures. The discovery/execution audit exits 0: all 12 GuardPolicy methods have one definition, one discovery entry and one passing execution in both policy and V1; the seven moved tests and two private helpers remain single-defined. Original suite 839 lines; GuardPolicy extension 817. Strict selected-file SwiftLint using `attempt-8/changed-swift-files.nul` and `git diff --check` exit 0. Earlier attempt-5 focused failures and attempt-6/7 passing superseded-source logs remain diagnostic history, not final-source evidence.
- **A1 implementation-phase criteria complete for formal review.** The six accepted A1 matrix rows retain the exact owner/method mapping in the earlier table and final V1/policy logs; the new atomic race subcases are in the named GuardPolicy method above. Formal test-integrity, one Codex Sol adversarial review and Codex Astra integration acceptance, followed by A1-only documentation/scoped commit/non-force push, remain downstream workflow gates and are not claimed here. A2–A5 and parent P1 remain open; A2/A3 still require native Riela receiving evidence and separate implementation/review. No legacy deletion, external repository operation, staging, commit, push, reset or stash occurred.

#### Current A1 completion criteria

- [x] Preserve seven moved tests/two helpers, six-row matrix, retry/gate/director/terminal/live behavior and file-size limits.
- [x] Enforce canonical exact-attempt inactivity precondition in the decision transaction, with stale/no-input/identity/terminal/cancellation/idle/replay regression evidence.
- [x] Source-matched V0, discovery and positive policy/V1/canonical/cancellation-host/live execution, strict selected-file SwiftLint, unchanged source membership/hashes and complete terminal logs.
- [x] Formal test-integrity, Codex Sol adversarial (`comm-000013`) and Codex Astra integration (`comm-000018`) acceptance. The prior Sol rejection remains historical.
- [x] Step 8 A1 documentation refresh in `README.md`, `design-docs/specs/design-work-runtime-consolidation.md` and this progress record; `.codex/skills/riela-impl-workflow/SKILL.md` reviewed with no contract change needed.
- [ ] Exact-file commit and non-force push (downstream steps); A2–A5 and parent P1 remain open.

### P1-7a A1 Step 8 documentation refresh — 2026-09-25

- Mode `issue-resolution`; issue `comm-000001`; workflow `codex-design-and-implement-review-loop-session-1`, step `step8-docs-refresh`, plan `p1-dispatch`. Codex `/root` refreshed the README, design status and progress after independent test-integrity acceptance, Codex Sol's repaired A1 adversarial acceptance (`comm-000013`) and Codex Astra's combined-tree acceptance (`comm-000018`). The earlier Sol mid-finding and rejection remain in the attempt-4 history; the atomic transaction precondition and deterministic regression in `Sources/RielaWork/WorkStore+Decisions.swift` and `Tests/RielaCLITests/TaskDispatcherIntegrationTests+GuardPolicy.swift` resolve that finding on the reviewed tree.
- Final Swift evidence is `tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-8/verification-evidence.json`, with complete foreground logs and terminal exits under `attempt-8/logs/`. The Xcode `swift build --scratch-path tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/build/p1-dispatch` exited 0. The Xcode `swift test` command with that scratch path passed `--filter 'TaskDispatcherIntegrationTests|TaskRuntimeExampleTests|TaskCancellationIntegrationTests'` 75/75, `--filter 'TaskCommandMutationTests|TaskDispatcherTests|TaskDispatcherIntegrationTests|TaskRuntimeExampleTests'` 69/69, `--filter 'RuntimeStoreTests|RuntimePublicationTests|DeterministicWorkflowRunnerTests|TaskCancellationIntegrationTests'` 94/94, `--filter 'TaskCancellationIntegrationTests|DistributedJobControllerTests|DistributedWorkerHTTPTests'` 49/49, and the five exact live `TaskDispatcherIntegrationTests` methods listed in the evidence JSON 5/5. `xargs -0 swiftlint lint --strict --quiet --no-cache < tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-8/changed-swift-files.nul`, `shasum -a 256 -c tmp/work-runtime-p1-7a-resume-comm000006-37c80ecd/plans/p1-dispatch/attempt-8/source-before.sha256`, the before/after source and membership `cmp` checks, and `git diff --check` each exited 0. Discovery was audited separately from execution.
- Step 7b (`comm-000019`) skipped `cd web && bun run test:e2e`: `web/package.json` declares Playwright, but `git diff --name-only e7098b6f2eb558cce250318fbeb6d3ac1aeeaa77` contains no `web/` path. Browser E2E remains unexercised for these Swift runtime changes. A2–A5 and parent P1 remain open; A2/A3 require native Riela receiving evidence and separate implementation/review. Exact-file commit and non-force push remain downstream; no legacy deletion or external repository operation is claimed.
- Step 8 `git diff --check` exited 0; its complete log is `tmp/p1-7a-a1-doc-refresh/git-diff-check.log`.

### Current P1-7a A2–A5 planning handoff — 2026-09-25

- Step 3 `comm-000004` accepted the current native-receiver design without findings. Accepted A1 and NRE contracts are incorporated at `a373a6040bff11ef7e86b8f6789321fefc7596f1`; earlier A1 failure/publication statuses above are historical.
- Current executable contract is the leading section of `impl-plans/active/work-runtime-p1-dispatcher-guard-director.md`; current manifest is `impl-plans/active/work-runtime-p1-7a-native-a2-a5-dispatch.json`. Archived rielflow publication is not a dependency.
- [x] Step 5 plan review accepted in comm-000006 without findings.
- [ ] Serial planning checkpoint publication.
- [ ] A0 fresh before-removal proof, A2 bounded removal and A3 final-source V0–V9/native/retained verification.
- [ ] A4 independent reviews and A5 docs/exact-file publication to Draft PR #109.
- P1-7b exact-file publication and parent P1 closure remain separate and incomplete here. No behavioral gate was run in Step 4.

### P1-7a A2/A3 Step 6 partial implementation and bounded stop — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` (no issue number), Draft PR #109; plan `p1-dispatch`; codex agent `/root`; checkpoint `911428e2ab6df6776627a64edd7c1d76e4622c26`. A0 before-removal source inventory was sealed unchanged (`source-before.sha256` equals `source-a0-seal.sha256`, manifest SHA-256 `b8edaaedf51620cecaf90f9d3c8cac57ba160c253483fdbea85002cd8178771d`). The Xcode build exited 0; focused 69/69, policy 75/75, canonical 94/94, cancellation host 49/49 and atomic race 1/1 each exited 0. Complete command/log/exit receipts are under `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-1/before-removal/`.
- A2 removed the obsolete local workflow auto-improve path, policy/options, remote emission, result supervision, scenario adapter and eight named example files. Rejection tests replace legacy success tests; retained Monja arguments, example catalog and task examples were updated. Native receiving code remained unchanged because it already rejects removed top-level keys by presence and accepts opaque `runtimeVariables`. Per-edit preimages, hashes, intended hunks and postimages are under `attempt-1/intents/`; no out-of-allowlist source file was edited.
- Final-source receipts under `attempt-1/after-removal/` include Xcode build exit 0, focused V1 69/69, V2 reservation 30/30 and supplement 35/35, V4 capability 55/55, native/CLI boundary 37/37, task cancellation 75/75, canonical 94/94, cancellation host 49/49, task repair/director mock runs accepted with 2/1 nodes, example parity 9/9, surface parity 8/8, remote 3/3, gate failure 2/2, atomic race 1/1, changed-file strict SwiftLint exit 0, and V5 adapters/server/GraphQL/AppSupport 455 tests with two skipped and zero failures. The V3 guard aggregate failed 1/80 and the current CLI aggregate failed 8/172; checkpoint-source baselines reproduce the exact named failure sets, with `identical: true` and empty differences in their comparison JSON. These are failed receipts pending independent disposition, never passing gates. V5 Work/CLI/Core failed 22 assertions across 2060 tests; seven Doctor tests, eight known WorkflowCommand tests, one WorkGuard test and other failures require classification. Monja collaboration test failed 6/86 because `.build/debug/riela` is absent; both Monja typechecks failed because `@types/bun` is unavailable; Monja project tests passed 23/23.
- **Implementation blocked at approved write-path boundary.** `Tests/RielaCLITests/SurfaceParitySkillTests.swift::testEveryDocumentedFlagIsAnOptionSomeParserAccepts` fails because `Resources/skills/riela-workflow-run/SKILL.md:56` still documents removed `--auto-improve`. That skill path is absent from the committed manifest `writePaths`; changing the parser or weakening the test would violate the accepted removal contract. Resume only after bounded plan/manifest approval assigns this exact path to the serial owner, then update the stale skill reference and renew affected source-matched gates. Preserve all current work and receipts. A3's remaining V0–V9 supplemental/broad commands, Monja prerequisites, full assertion attribution and final source seal remain incomplete. Formal A4 reviews, review-dependent A5 docs, exact-file commit/non-force push and Draft PR #109 handoff remain downstream; P1-7b and parent P1 stay open.

#### Current A2/A3 completion criteria

- [x] A0 sealed positive before-removal replacement proof before legacy deletion.
- [x] Obsolete workflow supervision source/options/examples removed within approved paths; focused native receiving and task replacement behavior proved.
- [ ] Repair stale `Resources/skills/riela-workflow-run/SKILL.md` after exact write-path authorization and rerun SurfaceParitySkillTests.
- [ ] Complete all final-source A3 commands, classify each failed broad assertion against checkpoint source, restore blocked Monja prerequisites, and seal unchanged final source inventory.
- [ ] A4 independent reviews and A5 docs/publication (downstream workflow steps).

#### Evidence qualification for attempt-1

The A0 commands did run before A2 deletion with exact `.command`, complete `.log`, terminal `.exit`, cwd, checkpoint HEAD and unchanged source hashes, but the first receipt wrapper did not save a per-command full environment snapshot. It was amended before later final-source commands. Treat A0 as positive chronological behavioral proof with an environment-metadata gap, not as fully complete receipt compliance. Do not erase or upgrade those original receipts. The current source hash/membership seal (`source-final-before.sha256` versus `source-final-after.sha256`) and `git diff --check` both exit 0, but neither resolves the unapproved skill path or remaining A3 gates.

### P1-7a A2/A3 Step 6 continuation — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` (no issue number), Draft PR #109; workflow execution `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`; plan `p1-dispatch`, Codex agent `/root`, branch `feat/remaining-impl-plans`, checkpoint HEAD `c97370cee30857068654c4323fa0c7a60bf3f9ee`, `dependsOn: []`. The current committed plan and native receiving design remain aligned. Per-edit preimages, hashes, intended hunks and postimages are in `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-2/intents/`; no staging, commit, push, reset, stash or worktree operation occurred in Step 6.
- The exact-path amendment authorized `Resources/skills/riela-workflow-run/SKILL.md`. Removed only its stale `--auto-improve` remote-run mention; retained `--from-registry`, `--mock-scenario`, `--supervisor-mode` and every other skill instruction. The Xcode `swift test --filter SurfaceParitySkillTests` receipt `attempt-2/surface-skill/` exited 0 with 7/7 tests and zero failures. `attempt-2/prior-source-recheck/` exited 0 over all 1,671 code/test/example entries in the attempt-1 source manifest, proving those earlier positive behavioral receipts still use unchanged bytes; the new final source seal at `attempt-2/source-final.sha256` includes this skill.
- Final-source A2/native/retained evidence remains the complete attempt-1 positive build, V1/V2/V4, native/CLI boundary, adapter/server/GraphQL/AppSupport, canonical, cancellation, remote, parity, task-bundle and atomic-race receipts listed above. The source recheck confirms no affected implementation file changed after them; the skill-specific test was renewed. The V3 guard 1/80 and focused CLI 8/172 failures retain their exact checkpoint-equal comparison JSON and nonzero exits. A0's accepted before-removal chronological proof still has missing per-command environment snapshots; this historical limitation is explicit and no metadata was invented.
- Repaired-source V5 Work/CLI/Core `attempt-2/V5-work-cli-core/` exited **1**, 2,060 tests, 21 failures (9 unexpected). The checkpoint `911428e2ab6df6776627a64edd7c1d76e4622c26` archive under `tmp/p1b` was checked against all 2,776 commit blobs before and after execution. Its fresh short-path build passed four path-sensitive tests; the full checkpoint V5 `attempt-2/baseline-shortbuild-V5/` exited **1**, 2,066 tests, 21 failures (9 unexpected). `attempt-2/V5-work-cli-core/comparison-shortbuild.json` reports `identical: true`, 21/21 assertions and empty one-sided sets. The same causes are seven Doctor `observedAt` decoding errors, eight WorkflowCommand validation/fixture errors, two missing root `.build/debug/riela` assertions, two unresolved temporary add-on assertions, one `StaleInactivityObservation`, and one missing WorkflowSession. These remain **FAILED** broad gates pending independent baseline disposition, never passing checks. Earlier unequal comparisons and their complete logs remain under `attempt-2/`; long snapshot paths caused one SQLite message-length assertion and three Unix-socket path failures, resolved only by the byte-identical short archive and fresh build.
- Current whole-suite `swift test --no-parallel`, `attempt-2/full-suite-no-parallel/`, exited **1**, 2,716 tests, two skipped, 21 failures (9 unexpected). The byte-verified checkpoint whole-suite `attempt-2/baseline-full-suite-no-parallel/` exited **1**, 2,722 tests, two skipped, 21 failures (9 unexpected). `attempt-2/full-suite-no-parallel/comparison-full.json` reports `identical: true`, 21/21 assertions and empty one-sided sets. Both failed gates require independent disposition at A4.
- Both Monja projects were copied byte for byte under `attempt-2/monja-snapshot/`; frozen lockfile dependencies and generated files stayed under `tmp/`. Both typechecks exited 0, collaboration tests passed 86/86 and project-orchestrator tests passed 23/23; `attempt-2/monja-source-check/` verified 60/60 original/copied source files after execution. Strict SwiftLint on the NUL-delimited set of 21 modified Swift files exited 0; `git diff --check`, `git diff --cached --check` and test discovery exited 0. The targeted retired-reference `rg` exited 1 with no matches as expected; the broader reference inventory exited 0. The manifest's repository-wide `swiftlint lint --quiet --no-cache` command was not invoked because the current Step 6 instruction forbids SwiftLint without explicit file paths; the exact changed-file strict command is the required lint gate. Each attempt-2 receipt directory has an exact command, full environment snapshot, complete log, terminal exit and HEAD metadata; `attempt-2/verification-evidence.json` associates them with source manifests.

#### Current A2/A3 completion criteria for this continuation

- [x] A0 preserved with its accepted environment-metadata limitation; A2 removal, retained behavior and exact-path skill repair have positive behavioral proof.
- [x] A3 required behavioral commands, Monja checks, selected-file lint and source checks have complete final-source or source-matched receipts. Failed V3/CLI/V5/whole-suite gates retain nonzero exits, equal positive checkpoint assertion comparisons and independent-disposition candidates.
- [ ] A4 independent test-integrity, adversarial and integration decisions with no unresolved material finding (downstream workflow steps).
- [ ] A5 review-dependent shared documentation, exact reviewed-file commit and non-force push to Draft PR #109 (downstream workflow steps).

### P1-7a A2/A3 Step 6 checkpoint reconciliation — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` (no issue number), Draft PR #109; workflow `nested-v1-482674a22af4efbc9e81c6fa4c01c7ffb0337d73579b26c17111269eb1e8ba31`; plan `p1-dispatch`; Codex `/root`; checkpoint HEAD `6e7475d4216e4cb96c2a04e1d3337cba3f53fb44`. No implementation code, tests, examples or skill bytes changed in this continuation. The attempt-2 whole-tree source check now exits 1 only for four committed checkpoint files: both design documents, the active plan and the active manifest. `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-3/implementation-source-check.log` exits 0 for the other 2,762 entries, including the repaired skill and prior behavioral test source. Preserve attempt-2 complete receipts and nonzero broad gates as recorded above.
- The checkpointed plan now requires repository-wide `swiftlint lint --quiet --no-cache` and a source-matched baseline comparison as V7. This Step 6 instruction forbids invoking SwiftLint without explicit file paths. The exact V7 command was **not run**, and changed-file strict lint does not satisfy it. A3 is incomplete pending a compliant resolution of that command conflict and its baseline comparison. Prior A0 environment metadata remains limited as recorded; V3, CLI, V5 and whole-suite failures remain failed baseline-disposition candidates for independent A4 review.

#### Current A2/A3 completion criteria after checkpoint reconciliation

- [x] A2 removal, native rejection and opaque variables, task-backed retained behavior, skill repair and source-matched positive behavioral receipts.
- [ ] A3 V7 repository-wide lint with complete final/baseline receipts and diagnostic comparison; preserve failed broad gates for independent disposition.
- [ ] A4 independent reviews and A5 documentation/publication (downstream workflow steps).

### P1-7a A2/A3 Step 6 V7 completion — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` (no issue number), Draft PR #109; plan `p1-dispatch`; Codex agent `/root`; workflow `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`; checkpoint `6e7475d4216e4cb96c2a04e1d3337cba3f53fb44`. This entry supersedes only the preceding statement that V7 was unavailable. No Swift source, test, example or skill bytes changed in this continuation.
- V7 evidence is under `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-4/v7/`. The checkpoint snapshot's 982 copied source/configuration files match both the prior checkpoint implementation-source manifest and the final tree byte for byte; 970 included Swift paths and `.swiftlint.yml` SHA-256 `39c58ae1362ebd1cb38094c607ee2fd6aab85649d2486b1c0d151fdff0def4b5` match. Source manifest SHA-256 is `5bbcf72a2fc9d89e80bc8d509169100b8b3159cd160791eef481f814f6b20c00` on both sides. The exact `/usr/bin/arch -arm64 /usr/bin/xcrun swiftlint --quiet --no-cache` command, SwiftLint 0.65.0, relevant toolchain environment, cwd, complete logs and terminal exits are in `checkpoint/` and `final/`: both exit 0 with 24 warnings. `comparison.json` records all diagnostics by repository-relative file, rule, message/cause, severity and raw location; 24 unchanged, zero added, removed or changed, with equivalent included-file scope. Repository lint is a diagnostic inventory, separate from the strict gate.
- Strict changed-file `swiftlint lint --strict --quiet --no-cache` on the NUL-delimited exact 21 modified Swift paths exited 0 with no diagnostics. Its complete command, environment, log, exit and manifest are under `attempt-4/strict-changed-2/`. The prior attempt-2 positive behavioral and Monja receipts remain source-matched by `attempt-3/implementation-source-check.log`; `Resources/skills/riela-workflow-run/SKILL.md` remains in the source seal and its 7/7 SurfaceParitySkillTests receipt remains valid. Preserve the A0 historical environment-metadata limitation. The V3, focused CLI, V5 and full-suite nonzero receipts remain failed baseline-disposition candidates for independent A4 review, with no Step 6 approval implied.

#### Current A2/A3 completion criteria after V7

- [x] A2 removal, native rejection, opaque runtime variables, task-backed retained behavior and repaired skill have positive source-matched proof.
- [x] A3 V7 checkpoint/final repository lint inventory, equivalent scope and diagnostic comparison; separate strict changed-file lint passed. All other final-source behavioral checks remain as previously recorded, including nonzero aggregate candidates.
- [ ] A4 independent test-integrity, Sol adversarial and Astra integration decisions (downstream workflow steps).
- [ ] A5 review-dependent documentation, exact-file commit and non-force push to Draft PR #109 (downstream workflow steps).

### P1-7a A4 handoff: Step 6 A0–A3 source reconciliation — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` Draft PR #109 / `comm-000002`; workflow `nested-v1-fb6c02d68fb5fbe99b59df51970064b8218e39fc08f2cfc322431e14579d0893`, step `step6-implement`, plan `p1-dispatch`, Codex owner `/root` with read-only `/root/evidence_audit`, `/root/plan_audit` and `/root/source_audit`. Runtime `dependsOn: []`, checkpoint `d1afe6e19439ad60daf631ae4fc4a5c05c538df2`; historical checkpoint Git object `2f472b3bd4d203f21bf72497900e58b88c2ece3b` remains for Astra's runtime-projection comparison. No A2/A3 source, test, example or skill bytes were edited in this handoff.
- Fresh index `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-5/reconciliation.json` and complete `reconcile.log` verify 2,761/2,766 attempt-2 source-manifest entries still match. The five differences are only `design-docs/specs/design-native-remote-workflow-execution.md`, `design-docs/specs/design-work-runtime-consolidation.md`, `impl-plans/active/work-runtime-p1-7a-native-a2-a5-dispatch.json`, `impl-plans/active/work-runtime-p1-dispatcher-guard-director.md` and this progress log. All A2/A3 implementation/test/example/skill inputs remain byte matched. All 982 V7 source/configuration entries match; its 970-file checkpoint/final comparison remains exit 0/0 with 24 identical warnings and zero added/removed/changed diagnostics. The strict lint manifest matches the 21 currently modified Swift files; its separate exit remains 0. The attempt-2 receipt index has no missing logs. Source-matched focused tests, build, Monja checks and retained behavior are indexed by `attempt-2/verification-evidence.json` and prior progress.
- The `V5-work-cli-core` current aggregate remains **FAILED**, exit 1, 2,060 tests and 21 failed assertions; its source-matched preimplementation baseline is exit 1, 2,066 tests and 21 failed assertions. The full nonparallel aggregate remains **FAILED**, exit 1, 2,716 tests and 21 failed assertions; its baseline is exit 1, 2,722 tests and 21 failed assertions. Complete logs and exact commands are in `attempt-2/verification-evidence.json`. `attempt-2/V5-work-cli-core/comparison-shortbuild.json` and `attempt-2/full-suite-no-parallel/comparison-full.json` each report `identical: true`, 21/21 exact assertion identities, and empty aggregate-only/baseline-only lists. These are pending independent baseline-disposition candidates, never passing gates. The A0 before-removal receipts lack historical per-command environment snapshots; no metadata was reconstructed.
- Step 6 A0–A3 reconciliation is complete on the current implementation tree with no new material finding. A4 still requires separate typed test-integrity, Sol adversarial and Astra integration decisions for **each** failed aggregate, explicit A0 limitation, and Astra's runtime-owned checkpoint projection/Git-object check. A5 review-dependent documentation, exact reviewed allowlist, commit, non-force push and Draft PR #109 head check remain downstream. Neither A4 nor A5 acceptance is claimed here.

#### Current P1-7a Step 6 completion criteria

- [x] A2/A3 source/test/example/skill bytes and receipt logs reconciled with the current tree; positive behavioral receipts remain source matched.
- [x] V7 repository lint comparison and separate strict 21-file SwiftLint gate remain source matched and passing.
- [x] Both failed broad aggregates retain exit 1, positive counts and exact assertion-level comparison paths for independent A4 review; A0 historical metadata limitation remains explicit.
- [ ] A4 independent decisions and checkpoint projection verification (downstream workflow steps).
- [ ] A5 review-dependent docs and exact-file publication (downstream workflow steps).

### P1-7a Step 6 test-integrity repair — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` Draft PR #109 / `comm-000002`; workflow `nested-v1-fb6c02d68fb5fbe99b59df51970064b8218e39fc08f2cfc322431e14579d0893`, plan `p1-dispatch`, Codex `/root`. Test-integrity `comm-000014` rejected the prior handoff with one mid finding: plain CLI cancellation terminal-state coverage was lost from `Tests/RielaCLITests/WorkflowCommandAutoImproveTests.swift`. Read-only Codex `/root/cancel_audit` and `/root/receipt_audit` checked the minimal repair and evidence renewal. This entry supersedes attempt-5 source identity and aggregate counts while retaining its historical receipts.
- Restored `testCancellationWithoutAutoImprovePersistsTerminalFailure` without the retired `--no-auto-improve` option, using its local command workflow and persisted-session wait. The test asserts failure exit, persisted `.failed` status, `.cancelled` failure kind, no running executions, and cancelled failure kind in JSON output. It passed 1/1, zero failures, exit 0 with complete `attempt-6/cancellation-focused.log`. The existing retired-option rejection and task-free ordinary-run tests were preserved.
- The renewed 21-file NUL manifest and strict selected-file SwiftLint under `attempt-6/` exited 0. Renewed repository V7 lint exited 0 across the same 970 included Swift files; `attempt-6/v7-comparison.json` matches all 24 checkpoint warnings by file, location, severity, rule and message, with zero added/removed diagnostics and unchanged `.swiftlint.yml` SHA-256 `39c58ae1362ebd1cb38094c607ee2fd6aab85649d2486b1c0d151fdff0def4b5`. The renewed V7 source manifest SHA-256 is `9dcbc2f712929702bc4a4f9a65361eef2e7c99ec5f37d34e96f259b6acb5aa15`; only this test file differs from the prior V7 source set.
- Because the test edit changed aggregate selection, both broad suites were rerun to terminal exit. `attempt-6/V5-work-cli-core.log` is **FAILED**, exit 1, 2,061 tests and 21 failures; `attempt-6/full-suite-no-parallel.log` is **FAILED**, exit 1, 2,717 XCTest cases, two skipped, and 21 failures. `attempt-6/V5-comparison.json` and `attempt-6/full-comparison.json` each compare against the preserved source-matched preimplementation baseline and report `identical: true`, 21/21 assertions, empty aggregate-only/baseline-only lists. They remain pending independent baseline dispositions, never green gates. `attempt-6/source-final.sha256` records the renewed source inventory; its pre-progress SHA-256 is `326d1f83ee2fce4ff15dd9593a0bdb6d1038f0ceee62999e178c6729c925a3ab`. A0 historical per-command environment snapshots remain absent and are not reconstructed.
- The `comm-000014` mid finding is repaired and the implementation-phase checks are complete. Independent typed test-integrity, Sol adversarial and Astra integration decisions for each failed aggregate, Astra checkpoint-projection verification, and A5 review-dependent documentation/exact-file commit/non-force push remain downstream. No formal review acceptance or publication is claimed.

#### Current P1-7a Step 6 completion criteria after repair

- [x] Plain CLI cancellation has positive current-source persisted-terminal-state coverage, 1/1 tests and zero failures.
- [x] Strict 21-file SwiftLint and renewed V7 checkpoint/final diagnostic comparison pass on current Swift source.
- [x] Both renewed broad aggregates retain exit 1, positive counts and exact 21/21 baseline assertion comparisons for independent disposition.
- [ ] A4 independent decisions and checkpoint projection verification (downstream workflow steps).
- [ ] A5 review-dependent docs and exact-file publication (downstream workflow steps).

### P1-7a A4 acceptance and Step 8 documentation refresh — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` Draft PR #109 / `comm-000002`; plan `p1-dispatch`; Codex `/root` Step 7b and Step 8. Independent typed test-integrity and Sol adversarial reviews accepted the renewed source and each failed broad aggregate as baseline-attributed. Astra integration accepted the combined A4 handoff and runtime-owned checkpoint projection against Git object `2f472b3bd4d203f21bf72497900e58b88c2ece3b`; the manifest remains runtime-owned. No material finding remains. The renewed Work/CLI/Core aggregate is **FAILED**, exit 1, 2,061 tests; the full nonparallel aggregate is **FAILED**, exit 1, 2,717 cases and two skips. Each comparison matches all 21 assertions across 19 failed cases with no one-sided failures. A0 historical per-command environment snapshots remain unavailable.
- Step 7b `comm-000024` skipped Playwright because the reviewed diff changes no `web/` file. Step 8 refreshed `README.md`, `examples/README.md`, the QA decision document, this progress record, the active plan status and `impl-plans/README.md`; reviewed `.codex/skills/riela-impl-workflow/SKILL.md` and the already corrected `Resources/skills/riela-workflow-run/SKILL.md`. Parent P1 remains active with final evidence outstanding; the plan contains 36 unchecked boxes including superseded historical sections. A5 exact-file commit, non-force push and live Draft PR #109 tip verification remain for the serial finalization owner.

## Parent P1 final acceptance Step 6 R1–R3 — 2026-09-25

- Mode `issue-resolution`; issue `tacogips/riela` Draft PR #109 / intake `comm-000002`; workflow `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`, `step6-implement`, plan `p1-dispatch`, branch `feat/remaining-impl-plans`, checkpoint `f8b0d886d277a3f3517f60c344cf6ce9df9b3799`. Codex `/root` was the serial editor; read-only Codex agents `ledger_audit` and `evidence_audit` investigated separate inventory and prior receipts. There are no prerequisite plan IDs. Preimages, per-edit intents and postimage hashes are under `tmp/work-runtime-p1-final-acceptance-20260925-comm000006/p1-dispatch/attempt-1/`. No source, test, shared status/index, Git stage/commit/push or other worktree was edited in this Step 6.
- R1: the active plan now classifies all 36 original boxes, retains each original text/line, maps compound/historical process rows to published replacements, and adds C01–C12 for current behavior, native receiving, retained paths, A0 limitation, failed aggregates and final publication. P1-6a/6b/6c/6d/7a/7b source/test anchors, publication commits, prior receipts and owners are linked there. No unchecked original box was silently closed. Current behavioral rows remain `materially open` only for fresh parent R4 acceptance; that pending review is downstream, not a demonstrated code defect.
- R2: `attempt-1/source-reconciliation.json` recomputes all 2,766 attempt-6 seal inputs: 2,758 match, zero missing, eight documentation-only mismatches (`README.md`, both designs, P1-7a QA, `examples/README.md`, `impl-plans/README.md`, active plan, this progress file); zero source/test/package/config mismatch. `git diff --name-only adde89d4af122d54e1311aad1c4ded12cddbe5c6 HEAD` shows only the two designs, active plan and new checkpoint dispatch manifest. `attempt-1/receipt-integrity.json` verifies all 50 attempt-2 indexed logs exist and match recorded SHA-256. The current source/test tree therefore retains prior focused/native/Monja behavioral evidence; no material invalidation or new source/test failure was demonstrated. A0 historical per-command environment snapshots remain unavailable.
- Actual predecessor A4 typed decisions were retrieved and line-hashed from `/Users/taco/.codex/sessions/2026/09/25/`: integrity `rollout-2026-09-25T22-00-55-01a0d8a7-8ff9-7c62-bcc3-291bae9f250f.jsonl:404`, Sol adversarial `rollout-2026-09-25T22-46-15-01a0d8d1-11c2-7223-9f54-16dfe9e621ac.jsonl:101`, Astra integration `rollout-2026-09-25T22-53-01-01a0d8d7-44e4-7680-ac0d-83cca448c0fc.jsonl:117`. Each accepted the predecessor A4 with no findings and separately kept V5/full baseline-attributed **FAILED**; precise line hashes and current-source comparison are in `attempt-1/evidence-reconciliation.json`. These do not replace new parent R4 decisions.
- The preserved attempt-6 V5 Work/CLI/Core aggregate is **FAILED**, exit 1, 2,061 tests, 21 assertions across 19 failed cases; full nonparallel Swift is **FAILED**, exit 1, 2,717 cases, two skipped, 21 assertions across 19 cases. Complete logs and comparisons are `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/{V5-work-cli-core.log,V5-comparison.json,full-suite-no-parallel.log,full-comparison.json}`; baseline logs are `attempt-2/baseline-shortbuild-V5/log.txt` and `attempt-2/baseline-full-suite-no-parallel/log.txt`. Each comparison has `identical:true`, positive equal 21/21 assertion counts, empty one-sided sets. `attempt-1/evidence-reconciliation.json` records log SHA-256, terminal summaries and six assertion-owner groups. These remain pending separate parent integrity/Sol/Astra attribution decisions, not passing gates. No broad rerun is triggered by documentation-only changes.
- R3: no current production or test gap was demonstrated; no ownership amendment or source/test repair was required. Step 6 ledger/evidence handoff is ready for independent R4. R4-integrity, R4-adversarial, R4-integration, then serial R5 designs/plan/progress/index and R6 exact-file non-force publication remain downstream. Do not mark parent P1 complete or either failed aggregate green.
- Read-only audit commands, cwd, start/end, full logs, SHA-256 and terminal exits are in `attempt-1/audit-commands/index.json`: `git rev-parse HEAD`, `git symbolic-ref --short HEAD`, `git status --porcelain=v1 --untracked-files=all`, `git diff --cached --name-status`, `git show adde89d4af122d54e1311aad1c4ded12cddbe5c6:impl-plans/active/work-runtime-p1-dispatcher-guard-director.md`, `git log --format=%H\ %s -- impl-plans/progress/p1-dispatch.md Sources/RielaWork Sources/RielaCLI/TaskDispatch.swift`, the three exact `cat` comparisons and attempt-2 index, `git diff --check`, `git diff --cached --check`, `git ls-remote --heads origin feat/remaining-impl-plans`, and `gh pr view 109 --repo tacogips/riela --json number,isDraft,headRefName,headRefOid` all exited 0. At checkpoint, local HEAD, remote and Draft PR head were all `f8b0d886d277a3f3517f60c344cf6ce9df9b3799`; final commit equality is R6's later obligation.
- R2 predecessor receipt availability correction: older P1-6c/P1-6d/P1-7b plan-local `tmp/` receipt directories named in historical progress are absent in this checkout; their exact old logs/seals are **not** revalidated. `attempt-1/predecessor-seal-comparison.json` records missing manifests. Available later A2 `attempt-1/after-removal/` receipts are indexed in `attempt-1/a2-receipt-audit.json` with commands, full logs, exits and positive counts: V1 69/69, V2 reservation 30/30 and supplement 35/35, V4 capability 55/55, task cancellation 75/75, cancel host 49/49, canonical 94/94, example parity 9/9 and native/adapter/server/GraphQL 455 tests with two skipped, all zero failures and exit 0. The A2 source seal differs in one later edited `WorkflowCommandAutoImproveTests.swift` test; A4 attempt-6 cancellation-focused 1/1 exit 0 and latest source seal address that change. R4 must independently decide whether this later source-matched coverage and primary A4 decisions suffice; old missing logs are not represented as present or passing.
- R2 primary predecessor review audit: `attempt-1/predecessor-reviews.json` records 15 actual typed decision lines (integrity, Sol adversarial and Astra for each P1-6a, P1-6b, P1-6c, P1-6d and P1-7b) under `/Users/taco/.codex/sessions/2026/09/{24,25}/`; all parsed payloads report `accepted:true`, zero findings, and line SHA-256. These historical slice decisions support provenance but do not decide new parent R4 or turn any FAILED aggregate green.

## Parent P1 native N1 branch handoff — 2026-09-26

- Mode `issue-resolution`; issue `tacogips/riela` Draft PR #109 / `comm-000002`; workflow execution `nested-v1-28c60de85b4984818087e57d959e88930453e13e7385a0a09b17d69a9fc519de`, `step6-implement`, branch ID and plan `p1-dispatch`, fanout group `shared-branch-implementation`, index 0, `dependsOn: []`, `acceptedPlanIds: []`. Codex `/root` was the serial edit owner; read-only Codex `/root/plan_audit` and `/root/evidence_audit` checked plan ownership and retained receipts. Branch `feat/remaining-impl-plans` was at checkpoint `ffef134b40e8209f78eaa416f4175c78d1e682b3`. Native pre-node snapshot `tmp/riela-fanout/338A0EEF-2562-43FA-8AAF-C9EE72C77D1E/EC6207C3-96D5-4B15-9BE8-C3AC4B8374DE.json` identifies `p1-dispatch` and `before:step6-implement`; it is a branch preimage, not a join outcome.
- The snapshot SHA-256 for the active plan is `02a6be86d9377ad5816167046fca1138ff1a28e6044593828b350f9f92f26406` and for this progress file is `cda50a5fdaa49d4a9f80c067fca6b94c0b88ba12d87e746711e14cef72087af7`; both matched the fresh pre-edit bytes. `tmp/work-runtime-p1-native-parent-20260926-comm000006/p1-dispatch/attempt-1/progress-preimage.md`, `progress-edit-intent.json`, `pre-node-snapshot-check.json` and the postimage record preserve the exact inherited two-file ledger and this append's attribution. The active plan was not edited; all earlier progress bytes, including the R1–R3 ledger, were retained.
- Fourteen foreground read-only commands all exited 0 with exact argv, cwd, start/end, complete logs and log hashes in `tmp/work-runtime-p1-native-parent-20260926-comm000006/p1-dispatch/attempt-1/command-index.json`. `git status --porcelain=v1 --untracked-files=all` showed only the inherited dirty progress file; the staged diff was empty. The checkpoint-to-current source/test/package diff was empty. `current-source-seal.json` checked 2,766 source-seal entries: eight documentation-only mismatches, zero missing source/test/config/dependency files or nondocumentation mismatches. No material input invalidation calls for a broad test rerun.
- Retained final-source focused receipt: `swift test --scratch-path tmp/work-runtime-p1-7a-native-a2-a5/build/p1-dispatch --filter 'TaskDispatcherIntegrationTests|TaskRuntimeExampleTests'`, exit 0, 69/69 tests, zero failures; complete log `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-1/after-removal/V1-focused/log.txt`. Retained Work/CLI/Core aggregate: `swift test --scratch-path tmp/work-runtime-p1-7a-native-a2-a5/build/p1-dispatch --filter 'RielaWorkTests|RielaCLITests|RielaCoreTests'`, **FAILED** exit 1, 2,061 tests, 21 baseline-matched assertions; complete log `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/V5-work-cli-core.log`, direct comparison `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/V5-comparison.json`. Retained full aggregate: `swift test --scratch-path tmp/work-runtime-p1-7a-native-a2-a5/build/p1-dispatch --no-parallel`, **FAILED** exit 1, 2,717 cases, two skipped, 21 baseline-matched assertions; complete log `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/full-suite-no-parallel.log`, direct comparison `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/full-comparison.json`. Each direct comparison reports `identical:true`, 21/21 assertions and empty one-sided sets. These are candidates for separate current-tree independent dispositions, never passing aggregates.
- N1 branch handoff is complete. The runtime must supply actual current-wave `dispatchedBranchIds`, terminal outcomes and change-evidence/snapshot references at N2; this Step 6 cannot certify its future join. Independent test-integrity, Codex Sol adversarial, Codex Astra integration, `P1-CURRENT-WAVE-PROVENANCE` resolution, reviewed P1-only designs/index status and exact-file non-force publication remain ordered N3–N7 tasks. A0 environment snapshots and older slice-local tmp receipts remain unavailable, as disclosed in the preserved ledger. The active plan's N0–N7 completion criteria remain open for those downstream gates; no source, test, shared design/index, stage, commit or push was changed here.

## Parent P1 Step 8 documentation refresh — 2026-09-26

- Mode `issue-resolution`; issue `tacogips/riela` Draft PR #109 / `comm-000002`; workflow execution `codex-design-and-implement-review-loop-session-1`. The workflow routed to Step 8 after current-tree independent test-integrity, Codex Sol adversarial and Codex Astra integration acceptance. Step 7b `comm-000019` accepted skipping browser E2E because the change has no browser-facing path. No source, test, package or dependency path changed in this documentation continuation.
- Reviewed `README.md` and `.codex/skills/riela-impl-workflow/SKILL.md`. Updated the README's parent P1 status, both affected design status headers, the implementation plan and `impl-plans/README.md`. The skill's existing Step 8 contract needs no edit. `impl-plans/progress/plans-index.json` has no `p1-dispatch` entry in its current schema, so no unrelated index record was changed.
- Retained verification: `swift test --scratch-path tmp/work-runtime-p1-7a-native-a2-a5/build/p1-dispatch --filter 'TaskDispatcherIntegrationTests|TaskRuntimeExampleTests'` exited 0, 69/69; complete log `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-1/after-removal/V1-focused/log.txt`. `swift test --scratch-path tmp/work-runtime-p1-7a-native-a2-a5/build/p1-dispatch --filter 'RielaWorkTests|RielaCLITests|RielaCoreTests'` remains **FAILED**, exit 1, with 21 baseline-matched assertions; complete log `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/V5-work-cli-core.log`, direct comparison `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/V5-comparison.json`. `swift test --scratch-path tmp/work-runtime-p1-7a-native-a2-a5/build/p1-dispatch --no-parallel` remains **FAILED**, exit 1, with 21 baseline-matched assertions; complete log `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/full-suite-no-parallel.log`, direct comparison `tmp/work-runtime-p1-7a-native-a2-a5/plans/p1-dispatch/attempt-6/full-comparison.json`.
- Step 9 `comm-000021` found the accepted implementation plan still active. Step 8 archived its original identity at `impl-plans/completed/work-runtime-p1-dispatcher-guard-director.md` and updated `impl-plans/README.md`; historical unchecked boxes remain intact. N7 exact reviewed-file commit, non-force push and clean local/remote/Draft PR #109 head equality remain pending workflow publication gates. Historical A0 environment snapshots and older slice-local receipts remain unavailable.

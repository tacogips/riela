Return concise business JSON only. Preserve unrelated dirty worktree changes. Do not commit or push. Do not start nested Riela or Codex processes.

Implement package manifest loop promotion artifact diagnostics. Promotion-ready manifests must require a minimum evidence schema version, and loader validation must report missing required mock scenario and expected-results artifact files under the package root. Add focused tests and return changed files plus verification commands.

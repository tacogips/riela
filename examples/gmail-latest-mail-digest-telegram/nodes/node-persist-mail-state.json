{
  "id": "persist-mail-state",
  "description": "Persists fetched Gmail message ids so later runs only notify about first-time-seen messages.",
  "nodeType": "command",
  "variables": {},
  "command": {
    "scriptPath": "scripts/persist-mail-state.sh",
    "envTemplate": {
      "RIELA_GMAIL_DIGEST_STATE_FILE": "{{workflowInput.stateFile}}"
    }
  },
  "output": {
    "description": "Output envelope preserving should_send_telegram and replyText for conditional Telegram delivery."
  }
}

{
  "workflowId": "seatbelt-sandboxed-worker",
  "description": "Single Claude worker that opts into riela's macOS Seatbelt enforcement (RIELA_SANDBOX_SEATBELT) with a read-only agent sandbox.",
  "defaults": {
    "maxLoopIterations": 3,
    "nodeTimeoutMs": 120000
  },
  "prompts": {
    "workerSystemPromptTemplate": "Work only on the current node responsibility and return concise business JSON."
  },
  "entryStepId": "main-worker",
  "nodes": [
    {
      "id": "main-worker",
      "nodeFile": "nodes/node-main-worker.json"
    }
  ],
  "steps": [
    {
      "id": "main-worker",
      "nodeId": "main-worker",
      "role": "worker"
    }
  ]
}

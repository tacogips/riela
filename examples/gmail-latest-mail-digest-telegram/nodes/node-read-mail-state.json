{
  "id": "read-mail-state",
  "description": "Reads the persisted Gmail message-id cursor and prepares a latest-10 Gmail fetch request.",
  "nodeType": "command",
  "variables": {},
  "command": {
    "scriptPath": "scripts/read-mail-state.sh",
    "envTemplate": {
      "RIELA_GMAIL_DIGEST_STATE_FILE": "{{workflowInput.stateFile}}",
      "RIELA_GMAIL_ACCOUNT_ID": "{{workflowInput.accountId}}",
      "RIELA_GMAIL_SEARCH_QUERY": "{{workflowInput.gmailSearchQuery}}",
      "RIELA_GMAIL_MAX_MESSAGES": "{{workflowInput.maxMessages}}"
    }
  },
  "output": {
    "description": "Output envelope with state, account id, Gmail search query, and max-10 fetch parameters."
  }
}

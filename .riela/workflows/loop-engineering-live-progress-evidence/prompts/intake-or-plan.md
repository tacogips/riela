You are planning the Riela loop-engineering live-progress evidence persistence slice.

Return concise JSON with:
- the implementation target in `target`;
- the expected compatibility constraints in `constraints`;
- the verification commands in `verification`.

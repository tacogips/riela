Return concise business JSON only. Preserve unrelated dirty worktree changes. Do not commit or push. Do not start nested Riela or Codex processes.

Plan a focused implementation slice for loop runtime policy enforcement. The slice should add a deterministic policy evaluator and wire required-loop preflight into the deterministic runner without broad stdio executor rewrites.

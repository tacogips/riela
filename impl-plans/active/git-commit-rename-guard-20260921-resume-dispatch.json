{
  "taskId": "git-commit-rename-guard-20260921-resume",
  "workflowMode": "single-work-package",
  "implementationBranch": "fix/git-commit-rename-guard",
  "baseBranch": "fix/git-commit-rename-guard",
  "remote": "origin",
  "originalHead": "99ce41463ea565e8b4a5f42343ea602d2d901802",
  "worktreeRoot": "/Users/taco/gits/tacogips/riela-worktrees/git-commit-rename-guard",
  "evidenceRoot": "tmp/git-commit-rename-guard-20260921-resume",
  "hasFeatureFanout": false,
  "supersedes": "impl-plans/active/git-commit-rename-guard-20260921-dispatch.json",
  "resumeContext": {
    "reason": "The prior attempt of this exact work package was killed by host OOM after implementing T1-T3. Its output is on disk as an uncommitted diff in the three code/test files and was audited line-by-line against the accepted plan on resume.",
    "priorDispatchOriginalHead": "ca1ce34235e5d2d358ea32a45e7d435d272763b4",
    "checkpointCommitOfPriorRun": "99ce41463ea565e8b4a5f42343ea602d2d901802",
    "doneOnDisk": [
      "T1 validateCommitPaths HEAD probe + stagedPaths --no-renames (Sources/RielaCLI/ProductionNodeAdapter+GitRepository.swift)",
      "T2 GitTestRepository.init(commitInitialFile:) fixture (Tests/RielaCLITests/GitWorkflowAddonTests.swift)",
      "T3 three GitWorkflowAddonContractTestsRenameGuard tests (Tests/RielaCLITests/GitWorkflowAddonContractTests.swift)"
    ],
    "outstanding": [
      "T4 contract-test filter + SwiftLint to clean",
      "T5 full swift build && swift test vs the 10-failure environmental baseline, complete log retained",
      "T6 commit code + tests + both design-docs/specs artifacts on fix/git-commit-rename-guard and push only that branch"
    ],
    "immutablePreState": "Keep the uncommitted T1-T3 diff exactly; do not re-implement or revert it. Fixes are allowed only where verification proves a real defect, and only inside writePaths."
  },
  "designDocPaths": [
    "design-docs/specs/design-git-commit-rename-guard.md"
  ],
  "plans": [
    {
      "planId": "git-commit-rename-guard-plan-1",
      "planPath": "design-docs/specs/impl-plan-git-commit-rename-guard.md",
      "dependsOn": [],
      "writePaths": [
        "Sources/RielaCLI/ProductionNodeAdapter+GitRepository.swift",
        "Tests/RielaCLITests/GitWorkflowAddonContractTests.swift",
        "Tests/RielaCLITests/GitWorkflowAddonTests.swift",
        "design-docs/specs/design-git-commit-rename-guard.md",
        "design-docs/specs/impl-plan-git-commit-rename-guard.md"
      ],
      "sharedPaths": [],
      "progressPath": "impl-plans/progress/git-commit-rename-guard-20260921-resume-git-commit-rename-guard-plan-1.json",
      "evidenceDir": "tmp/git-commit-rename-guard-20260921-resume/git-commit-rename-guard-plan-1",
      "acceptanceCriteria": [
        "AC1: testCommitSupportsStagedRenameAcrossOldAndNewPaths commits a `git mv` pair end to end - one new commit whose tree drops the old path and carries the new one (rev-parse HEAD^ == pre-rename HEAD, ls-tree --name-only HEAD == 'renamed.txt', show HEAD:renamed.txt == 'initial'; never assert on show --name-status, which may render R100).",
        "AC2: testCommitRejectsMissingPathAbsentFromIndexAndHead keeps refusing a path tracked in neither working tree, index nor HEAD with the unchanged 'riela/git-commit missing path is not an exact tracked deletion' policy error (.policyBlocked), HEAD and index bytes unchanged, no finalization journals.",
        "AC3: Non-regression - existing testCommitSupportsExactTrackedDeletion (plain tracked deletion) stays green, and testCommitRejectsMissingPathOnUnbornHeadWithoutCrashing refuses on an unborn HEAD without crashing (rev-list --all --count == 0, no journals).",
        "AC4: `arch -arm64 /bin/zsh -lc 'swift test --filter GitWorkflowAddonContractTests'` is green, and the full `arch -arm64 /bin/zsh -lc 'swift build && swift test'` reports no failure beyond the ten accepted environmental ones, evidenced from the complete log.",
        "AC5: SwiftLint clean on the three changed sources.",
        "AC6: All work committed on fix/git-commit-rename-guard and pushed to that branch only; main untouched; no force push, reset or branch deletion; worktree clean afterwards."
      ],
      "verification": {
        "shellPrefix": "arch -arm64 /bin/zsh -lc",
        "iterate": [
          "arch -arm64 /bin/zsh -lc 'swift test --filter GitWorkflowAddonContractTests'"
        ],
        "lint": [
          "arch -arm64 /bin/zsh -lc 'swiftlint lint --strict Sources/RielaCLI/ProductionNodeAdapter+GitRepository.swift Tests/RielaCLITests/GitWorkflowAddonContractTests.swift Tests/RielaCLITests/GitWorkflowAddonTests.swift'"
        ],
        "acceptanceGate": [
          "arch -arm64 /bin/zsh -lc 'swift build && swift test' > tmp/git-commit-rename-guard-20260921-resume/git-commit-rename-guard-plan-1/attempt-1/full-test.log 2>&1; echo \"exit=$?\" | tee -a tmp/git-commit-rename-guard-20260921-resume/git-commit-rename-guard-plan-1/attempt-1/full-test.log",
          "cp tmp/git-commit-rename-guard-20260921-resume/git-commit-rename-guard-plan-1/attempt-1/full-test.log /tmp/git-commit-rename-guard-full-test.log"
        ],
        "fullLogPath": "tmp/git-commit-rename-guard-20260921-resume/git-commit-rename-guard-plan-1/attempt-1/full-test.log",
        "operatorLogMirror": "/tmp/git-commit-rename-guard-full-test.log",
        "baselineFailures": [
          "RielaAppUXOnboardingControllerTests (AppKit view hierarchy, headless)",
          "RielaAppSettingsEditorNavigationTests (AppKit view hierarchy, headless)",
          "RielaAppWindowContentInsetTests (AppKit view hierarchy, headless)",
          "WorkflowRound7AdversarialTests (3 unix-socket-unlink cases)",
          "WorkflowCommandTests.testPackageAppEnvironmentEnablementRunAndMonitoringScenario"
        ],
        "baselineFailureCount": 10,
        "rules": [
          "Every Swift command must run through an arm64 login shell; the default shell is Rosetta and the xctest bundle refuses to dlopen there.",
          "Never tail the full-suite output: keep the complete log and record its final exit status. An incomplete log is not a passing check.",
          "Run all verification in the foreground; no background, detached or daemonized commands. The prior attempt died of host OOM - run the full suite once, with nothing else running.",
          "Diff the observed failure set against baselineFailures and list every difference explicitly; any other failure is in scope to fix.",
          "Never use bare `git stash` / `git stash pop`: the stash stack is shared across worktrees and the uncommitted T1-T3 diff must survive until T6 commits it.",
          "Confirm `git status` clean after T6 and that only fix/git-commit-rename-guard was pushed."
        ]
      },
      "notes": [
        "RESUME: T1, T2 and T3 are DONE on disk as an audited uncommitted diff. Keep them exactly; do not re-implement or revert. Verification has never run in any session, so the first T4 run is the earliest evidence point and may reveal compile or assertion fixes - apply them minimally and stay inside writePaths.",
        "T1 guard: in the .missing branch of validateCommitPaths, after `ls-files --error-unmatch` exits nonzero, probe `runRepositoryGitResult([\"cat-file\", \"-e\", \"HEAD:\\(path)\"], repository:)` and accept on exit 0; otherwise throw the unchanged policy error. An unborn HEAD fails closed through the same nonzero exit.",
        "T1 companion: `stagedPaths` passes `--no-renames` to its `diff --cached --name-only -z` read so the staged set stays literal ({old:D, new:A}). Exactly two callers, both in the git-commit pipeline (GitCommit.swift:67 pre-staged allowlist, GitCommit.swift:84 requireExactStagedPaths); the reported set can only get more explicit, so no refusal weakens.",
        "T2 is a test-only fixture change: GitTestRepository.init gains `commitInitialFile: Bool = true`; when false it still inits, configures the user, writes tracked.txt and `git add`s it (the index file must exist for loadGitRepository's regularPathEntryIdentity preflight) but skips the initial commit. Never combine commitInitialFile: false with withBareRemote.",
        "No back-compat shim: the check changes in place - no alias flag, no opt-out configuration, no message change.",
        "Do not weaken any other validation, widen allowlist semantics, or touch the finalization journal. A HEAD-tracked directory path passes the probe but is refused downstream by requireExactStagedPaths - do not 'fix' that by weakening requireExactStagedPaths.",
        "Documented fallback if `cat-file -e HEAD:<path>` hits an unforeseen literal-path edge: non-empty `ls-tree -z HEAD -- <path>` check.",
        "T6 commit message must end with 'Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>'.",
        "Work only inside /Users/taco/gits/tacogips/riela-worktrees/git-commit-rename-guard; never modify the main checkout or another worktree."
      ]
    }
  ],
  "reconciliation": {
    "serialPhase": "Not required: a single plan with exclusive ownership of its files, no shared indexes, lockfiles or broad formatting.",
    "sharedFiles": []
  },
  "preservedWorkingState": {
    "stagedEntries": [],
    "trackedModificationsLeftInPlace": [
      "Sources/RielaCLI/ProductionNodeAdapter+GitRepository.swift",
      "Tests/RielaCLITests/GitWorkflowAddonContractTests.swift",
      "Tests/RielaCLITests/GitWorkflowAddonTests.swift"
    ],
    "untrackedOutsideCheckpoint": [],
    "note": "The three code/test modifications are the resumed run's in-flight implementation (T1-T3). They are deliberately NOT part of this checkpoint commit and must remain on disk untouched; the implementation step commits them in T6. No staged entries existed at checkpoint time, so the scoped commit is exact."
  }
}

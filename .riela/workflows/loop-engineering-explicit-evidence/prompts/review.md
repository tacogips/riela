Review the implementation for compatibility, determinism, and scope.

Check that implicit run persistence and session/loop status do not change for
legacy workflows, explicit evidence output uses stored workflow resolution, and
the code does not widen runtime store APIs unnecessarily.
